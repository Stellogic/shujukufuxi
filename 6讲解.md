# 第6章 数据库安全性 — 题目讲解

---

## 一、题目分析

本章10道题（8单选+2判断）围绕**数据库安全性**展开，涵盖安全性控制手段的分类、强制存取控制规则、数据控制语句(GRANT/REVOKE)、角色(ROLE)定义，以及安全性与其他概念的区分。

> **题目类型：** 概念辨析型 + 判断题
> **考点：**
> - 安全性控制手段：用户标识与鉴别、自主存取控制(DAC)、强制存取控制(MAC)、审计
> - 强制存取控制规则："向下读、向上写"（许可证级别 ≥ 密级才能读；许可证级别 ≤ 密级才能写）
> - 数据控制语句：GRANT（授予权限）、REVOKE（回收权限）
> - 角色(ROLE)的定义与作用
> - 安全性 vs 完整性 vs 并发控制的区分
> - 视图与授权的安全作用

---

## 二、相关知识点梳理

```
📚 数据库安全性控制手段（层次由外到内）
├── 📍 用户标识与鉴别 — 最外层，验证用户身份
│   └── 方法：用户名+密码、生物识别等
├── 📍 自主存取控制(DAC) — 用户自主决定权限
│   ├── 通过GRANT和REVOKE实现
│   └── 较灵活但安全性弱于MAC
├── 📍 强制存取控制(MAC) — 系统强制执行安全规则
│   ├── 核心概念：主体(用户)和客体(数据)各有安全级别
│   ├── 读规则（向下读）：
│   │   主体的许可证级别 ≥ 客体的密级时，才能读该客体
│   │   （只能读比自己级别低或同级的数据）
│   ├── 写规则（向上写）：
│   │   主体的许可证级别 ≤ 客体的密级时，才能写该客体
│   │   （只能写比自己级别高或同级的数据——防止数据泄露到低级别）
│   └── MAC不是所有DBMS的必备功能
├── 📍 审计 — 记录所有对数据的操作，事后分析
│   ├── 较耗时耗资源，作为可选安全性手段
│   └── 不是所有DBMS必备功能
└── 📍 不属于安全性手段的：
    └── 触发器(TRIGGER) — 属于完整性机制

📚 数据控制语句(DCL)
├── 📍 GRANT — 授予权限
│   └── 格式：GRANT 权限 ON 对象 TO 用户 [WITH GRANT OPTION]
├── 📍 REVOKE — 回收权限
│   └── 格式：REVOKE 权限 ON 对象 FROM 用户 [CASCADE|RESTRICT]
└── CREATE/ALTER/DROP属于DDL，INSERT/UPDATE/DELETE属于DML，不是数据控制

📚 角色(ROLE)
├── 📍 定义：CREATE ROLE 角色名
├── 📍 角色是一组权限的集合，不是数据库用户
├── 📍 用户可以被授予角色，从而获得该角色包含的所有权限
└── 📍 属于安全性范畴

📚 四大概念的区分
├── 📍 安全性(Security) — 保护数据不被非法访问
│   ├── 手段：用户标识、DAC、MAC、审计、视图、授权
│   └── 目标：防止非法用户和合法用户的非法操作
├── 📍 完整性(Integrity) — 保证数据正确、有效、相容
│   └── 手段：约束、断言、触发器
├── 📍 并发控制(Concurrency) — 保证多用户同时操作的正确性
│   └── 手段：封锁、时间戳、多版本
└── 📍 恢复(Recovery) — 故障后将数据库恢复到正确状态
    └── 手段：日志、数据转储、镜像
```

**强制存取控制的核心规则（必考！）：**
- **读规则：** 主体级别 ≥ 客体级别 → 可读（"高看低"，向下读）
- **写规则：** 主体级别 ≤ 客体级别 → 可写（"低写高"，向上写）
- 写规则的逻辑：低级主体不能把数据写入低级客体（防止信息从高密级流向低密级），只能写入高级别客体

**常见误区提醒：**
- MAC比DAC严格，但MAC**不是**所有DBMS的必备功能（题10判断为错）
- 角色 ≠ 用户，角色是权限的集合
- 触发器属于完整性，不属于安全性！

---

## 三、解题思路与一般方法

**安全性 vs 其他概念的快速判断：**
1. 题干中出现"非法用户"、"防止非法操作"、"保护"→ 安全性
2. 题干中出现"正确、有效、相容"→ 完整性
3. 题干中出现"并发"、"同时操作"→ 并发控制
4. 题干中出现"破坏"、"恢复"→ 恢复

**强制存取控制规则判断：**
- 读：主体级别 ≥ 客体级别 ✓
- 写：主体级别 ≤ 客体级别 ✓
- 记法："向下读、向上写"

---

## 四、见到这种题目的思考框架

```
看到"主体与客体" → 强制存取控制(MAC)
看到"用户标识与鉴别" / "自主存取控制" / "强制存取控制" / "审计" → 安全性手段
看到"触发器"被问是否属于安全性 → 不属于！触发器属于完整性
看到"角色" → CREATE ROLE → 属于安全性
看到"授予/回收权限" → GRANT / REVOKE → 数据控制语句(DCL)
看到"视图+授权" → 安全性手段
看到"保证正确、有效、相容" → 完整性，不是安全性
看到"防止非法用户" → 安全性
看到"审计可选" → 正确（耗时耗资源，非必需）
看到"强制存取控制必备" → 错误（不是所有DBMS必备）
```

---

## 五、本题详细解答

### 题1 — 主体与客体属于
**答案：D. 强制存取权限控制**

强制存取控制(MAC)的核心机制就是给主体（用户）和客体（数据）分配安全级别，根据级别规则控制访问。

### 题2 — 不属于安全性控制手段的是
**答案：D. 触发器**

触发器用于实现完整性约束（如级联更新），不属于安全性控制手段。A（用户标识与鉴别）、B（自主存取控制）、C（审计）都是安全性手段。

### 题3 — 关于安全性说法正确的是
**答案：B. 强制存取控制要求主体的许可证级别小于或等于客体的密级时，该主体才能写相应的客体**

- A错误：强制存取控制比自主存取控制更严格
- B正确：这是MAC的"向上写"规则——主体级别 ≤ 客体级别才能写
- C错误：角色不是数据库用户，是权限的集合
- D错误：审计是可选的，不是必选的

### 题4 — 定义角色的SQL语句
**答案：C. CREATE ROLE**

CREATE ROLE用于创建角色，属于安全性的权限管理范畴。

### 题5 — 属于数据控制语句的是
**答案：D. GRANT和REVOKE**

GRANT（授予）和REVOKE（回收）是数据控制语言(DCL)，用于权限管理。

### 题6 — 视图和授权可以实现
**答案：B. 安全性**

视图可以隐藏敏感列、授权可以限制用户操作，二者都是安全性控制手段。

### 题7 — 数据库安全性旨在
**答案：B. 保护数据库中的数据，防止非法用户和合法用户的非法操作**

- A描述的是完整性（正确、有效、相容）
- B正确：安全性的核心目标
- C、D描述的是具体约束，不是安全性的总体目标

### 题8 — DBMS提供的安全性手段不包括
**答案：D. 触发器**

触发器属于完整性机制，不属于安全性手段。

### 题9（判断）— 审计可作为可选安全性手段
**答案：对**

审计记录每次数据访问操作，能追踪可疑行为，但会消耗系统资源（时间和存储空间），因此常作为可选功能而非必选。

### 题10（判断）— 强制存取控制是所有DBMS必须提供的
**答案：错**

强制存取控制不是所有DBMS的必备功能，许多商用DBMS主要提供自主存取控制(DAC)，MAC常用于对安全性要求较高的场景（如军事、政府部门）。
