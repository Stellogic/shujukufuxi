# 第6章 数据库安全性 — 题目讲解

---

## 一、题目分析

本章10道题（8单选+2判断）围绕**数据库安全性**展开，涵盖安全性控制手段的分类、强制存取控制规则、数据控制语句(GRANT/REVOKE)、角色(ROLE)定义，以及安全性与其他概念的区分。

> **题目类型：** 概念辨析型 + 判断题
> **考点：**
> - 安全性控制手段：用户标识与鉴别、自主存取控制(DAC)、强制存取控制(MAC)、审计
> - 强制存取控制规则："向下读、向上写"（许可证级别 ≥ 密级才能读；许可证级别 ≤ 密级才能写）
> - 数据控制语句：GRANT（授予权限）、REVOKE（回收权限）
> - 角色(ROLE)的定义与作用
> - 安全性 vs 完整性 vs 并发控制的区分
> - 视图与授权的安全作用

---

## 二、相关知识点梳理（结构化讲解）

### 视图与安全性

可以实现数据屏蔽

在安全性这个考点下，视图强调的是限制数据可见范围。


### 📍 授权 (Authorization) 与安全性


---

### 【模块1】数据库安全性控制手段

> 核心关键词：**多层次纵深防御**，由外到内共四层 + 一个易混淆概念。

---

#### 1.1 第一层：用户标识与鉴别（Identification & Authentication）

| 项目 | 内容 |
|:---|:---|
| **是什么** | 验证用户身份的**第一道门**，未通过则无法进入数据库系统 |
| **本质** | 系统对用户身份的合法性进行检查，确认"你是谁" |
| **位置** | 安全体系的最外层，所有访问请求必须首先通过此关 |

**常见的鉴别方法对比：**

| 方法 | 原理 | 安全等级 | 优点 | 缺点 |
|:---|:---|:---:|:---|:---|
| 静态口令 | 用户名+固定密码 | ⭐⭐ | 简单易用，成本最低 | 易被猜测、窃听、撞库 |
| 动态口令 | 每次登录生成一次性密码 | ⭐⭐⭐⭐ | 一次有效，不怕窃取 | 需要额外设备或APP |
| 生物识别 | 指纹、虹膜、人脸等生理特征 | ⭐⭐⭐⭐⭐ | 唯一性强，难以伪造 | 成本高，设备要求高 |
| 智能卡认证 | 硬件卡+PIN码 | ⭐⭐⭐⭐ | 双重保护，防篡改 | 需要读卡器 |
| 多因素认证 | 组合以上两种或多种 | ⭐⭐⭐⭐⭐ | 安全性最高 | 用户体验略复杂 |

**📌 考试重点：**
- 用户标识与鉴别是**最外层**安全性手段
- 其作用仅仅是验证身份，不控制具体操作权限

---

#### 1.2 第二层：自主存取控制（DAC — Discretionary Access Control）

| 项目 | 内容 |
|:---|:---|
| **定义** | 数据拥有者可**自主**决定谁有权访问自己的数据 |
| **关键词** | "用户说了算" — 用户拥有对自己创建的数据对象的完全控制权 |
| **实现方式** | 通过GRANT（授予）和REVOKE（回收）两条SQL语句 |

**DAC的核心特征：**

```
数据拥有者 ─── 拥有完全控制权 ─── 可以决定：
                              ├── 给谁权限（User_A、User_B...）
                              ├── 给什么权限（SELECT/INSERT/UPDATE/DELETE）
                              ├── 给到什么级别（表级 / 列级 / 行级）
                              └── 是否允许转授（WITH GRANT OPTION）
```

**DAC与MAC的对比：**

| 对比维度 | DAC（自主存取控制） | MAC（强制存取控制） |
|:---|:---|:---|
| 控制主体 | 数据拥有者（用户） | 系统（数据库管理系统） |
| 灵活性 | 高，用户可以自主授权/回收 | 低，规则由系统预定义 |
| 安全性 | 较弱（权限可被恶意传递） | 更强（用户无法绕过规则） |
| 典型场景 | 大多数商用DBMS | 军用/政务等高安全场景 |
| 实现难度 | 简单 | 复杂 |

> **DAC的潜在风险：** 如果用户A将权限授予用户B（WITH GRANT OPTION），用户B又转授给用户C...一旦用户A的权限被回收，会触发**级联回收**问题。

**📌 考试重点：**
- DAC通过GRANT/REVOKE实现 ✅
- DAC灵活性高但安全性**弱于**MAC ✅
- DAC和MAC可以**共存**（先过DAC再过MAC）— 易漏考点！

---

#### 1.3 第三层：强制存取控制（MAC — Mandatory Access Control）

| 项目 | 内容 |
|:---|:---|
| **定义** | 系统**强制**执行安全规则，用户无权修改或绕过 |
| **关键词** | "系统说了算" — 安全规则是强制的，不是用户能决定的 |
| **设计目标** | 防止信息从高安全级流向低安全级 |

**两个核心概念（高频考点！）：**

```
┌─────────────────────────────────────────────────────┐
│     主体 (Subject)             客体 (Object)         │
│   ┌──────────────┐         ┌──────────────┐        │
│   │   用户/进程   │  ──访问──▶ │   数据/文件   │        │
│   │  许可证级别    │         │     密级     │        │
│   └──────────────┘         └──────────────┘        │
│   比如：机密级用户            比如：绝密级文件         │
└─────────────────────────────────────────────────────┘
```

**安全级别分级体系：**

| 级别名称 | 数值表示 | 含义 |
|:---|:---:|:---|
| 绝密（Top Secret） | 5 | 最高级，泄露会造成极其严重的损害 |
| 机密（Secret） | 4 | 次高级，泄露会造成严重损害 |
| 秘密（Confidential） | 3 | 中级，泄露会造成一定损害 |
| 公开（Unclassified） | 1 | 最低级，任何人均可访问 |

**MAC的两条核心规则（必考！必考！必考！）：**

**规则一：读规则 — "向下读"（主体级别 ≥ 客体级别）**

| 条件 | 结果 | 通俗理解 |
|:---|:---:|:---|
| 主体级别 ≥ 客体级别 | ✅ 可以读 | "高看低" — 高级别用户可以看低级别数据 |
| 主体级别 < 客体级别 | ❌ 不可以读 | 不能越级看更高级别的数据 |

> **示例：** 机密级用户（级别4）
> - ✅ 可以读：公开(1)、秘密(3)、机密(4) — 级别≤自己
> - ❌ 不可以读：绝密(5) — 级别>自己

**规则二：写规则 — "向上写"（主体级别 ≤ 客体级别）**

| 条件 | 结果 | 通俗理解 |
|:---|:---:|:---|
| 主体级别 ≤ 客体级别 | ✅ 可以写 | "低写高" — 只能写入不低于自己级别的对象 |
| 主体级别 > 客体级别 | ❌ 不可以写 | 不能写入比自己级别低的（防泄露） |

> **示例：** 机密级用户（级别4）
> - ✅ 可以写：机密(4)、绝密(5) — 级别≥自己
> - ❌ 不可以写：秘密(3)、公开(1) — 级别<自己

**为什么写规则必须是"向上写"？——核心原理：**

```
假设没有"向上写"规则 → 机密级用户可以写公开文件
  机密用户读到绝密数据 → 将绝密内容写入公开文件 → 所有低级别用户都能看到 → 绝密泄露 ❌

有了"向上写"规则后：
  机密用户只能写机密或更高级别文件 → 数据始终保持在高级别中 → 安全 ✅
  
一句话：写规则防止高密级信息通过"写入低密级客体"的途径泄露出去
```

**MAC规则的完整判断矩阵：**

| 主体级别 vs 客体级别 | 读操作 | 写操作 |
|:---:|:---:|:---:|
| 主体 > 客体（如4读/写2） | ✅ **可读**（向下读） | ❌ **不可写**（防泄露） |
| 主体 = 客体（如4读/写4） | ✅ 可读 | ✅ 可写 |
| 主体 < 客体（如3读/写5） | ❌ **不可读**（越级） | ✅ **可写**（向上写） |

**📌 考试重点：**
- "主体"和"客体"这个说法出现 → 立即想到**强制存取控制(MAC)** ✅
- 读规则：主体级别 **≥** 客体级别 → 可读 ✅
- 写规则：主体级别 **≤** 客体级别 → 可写 ✅
- **MAC不是所有DBMS的必备功能**（这是判断题高频陷阱）✅
- MAC比DAC更严格，但很多DBMS只实现了DAC ✅

---

#### 1.4 第四层：审计（Audit）

| 项目 | 内容 |
|:---|:---|
| **定义** | 将所有对数据库的操作**记录到日志**中，用于事后分析 |
| **作用** | **事后追踪**（不是事前阻止！） |
| **性质** | **可选**安全性手段（不是DBMS必备功能） |

**审计日志记录的内容（5W）：**

```
操作发生 ───▶ 审计日志记录：
              ├── Who — 谁操作的？（用户标识）
              ├── When — 什么时间操作的？（时间戳）
              ├── What — 做了什么操作？（SELECT/INSERT/UPDATE/DELETE）
              ├── Which — 操作了哪个对象？（表名、列名）
              └── Result — 操作结果如何？（成功/失败）
```

**审计的两种方式：**

| 方式 | 范围 | 用途 |
|:---|:---|:---|
| 用户级审计 | 针对特定用户 | 追踪可疑用户的所有行为 |
| 系统级审计 | 系统范围内所有活动 | 全局安全监控 |

**📌 考试重点：**
- 审计**不能阻止**非法操作，只能**事后发现** ✅
- 审计是**可选**的（耗时耗资源），非DBMS必备 ✅
- 题目说"审计可作为可选安全性手段" → **对**（题9判断依据）

---

#### 1.5 【易混淆】触发器（TRIGGER）—— 不属于安全性手段

| 项目 | 内容 |
|:---|:---|
| **所属范畴** | **完整性**机制，**不是**安全性机制 |
| **定义** | 当满足某个条件时**自动执行**一段SQL代码 |
| **常见用途** | 级联更新、自动校验数据、业务逻辑自动化 |

> **📌 考试重点：** 题目问"不属于安全性控制手段的是" → 选"触发器"（题2、题8已考）
> **快速判断：** 安全性手段 = 标识鉴别 / DAC / MAC / 审计 / 视图+授权 / 数据加密

---

### 【模块2】数据控制语句（DCL）

> DCL = Data Control Language，用于控制数据的访问权限。

---

#### 2.1 SQL语句三大分类（基础框架）

| 分类 | 全称 | 包含语句 | 作用 |
|:---|:---|:---|:---|
| **DDL** | 数据定义语言 | CREATE, ALTER, DROP, TRUNCATE | 定义数据库结构 |
| **DML** | 数据操纵语言 | SELECT, INSERT, UPDATE, DELETE | 操作数据本身 |
| **DCL** | 数据控制语言 | **GRANT, REVOKE** | 控制访问权限 |

> **📌 考试重点：** DCL只有两条语句：**GRANT** 和 **REVOKE**
> CREATE是DDL，INSERT是DML，它们**不是**数据控制语句！

---

#### 2.2 GRANT — 授予权限

**语法格式：**
```sql
GRANT <权限列表> ON <对象名> TO <用户列表> [WITH GRANT OPTION];
```

**参数说明：**

| 参数 | 说明 | 示例值 |
|:---|:---|:---|
| 权限列表 | 要授予的权限，多个用逗号分隔 | SELECT, INSERT, ALL PRIVILEGES |
| 对象名 | 权限作用的目标 | 表名、视图名、存储过程名 |
| 用户列表 | 接收权限的用户 | User_A, Role_Teacher |
| WITH GRANT OPTION | 可选，允许用户将权限转授他人 | — |

**示例详解：**

```sql
-- 示例1：给User_A查询Student表的权限
GRANT SELECT ON Student TO User_A;

-- 示例2：给User_B插入和修改SC表的权限
GRANT INSERT, UPDATE ON SC TO User_B;

-- 示例3：给User_C Student表的所有权限，且允许其转授
GRANT ALL PRIVILEGES ON Student TO User_C WITH GRANT OPTION;
-- 现在User_C可以：查询/插入/修改/删除Student表，还能把这些权限给其他人
```

**📌 关于WITH GRANT OPTION：**
- **有**该选项 → 用户可以将获得的权限**再授予**其他用户
- **没有**该选项 → 用户只能自己使用，**不能转授**给他人
- **连锁效应：** A给B（带转授），B给C（带转授），C给D → 回收A权限时需CASCADE

---

#### 2.3 REVOKE — 回收权限

**语法格式：**
```sql
REVOKE [GRANT OPTION FOR] <权限列表> ON <对象名> FROM <用户列表> [CASCADE|RESTRICT];
```

**示例详解：**

```sql
-- 示例1：收回User_A的Student表查询权限
REVOKE SELECT ON Student FROM User_A;

-- 示例2：收回User_B的SC表插入权限，级联收回其转授的权限
REVOKE INSERT ON SC FROM User_B CASCADE;
```

**CASCADE 与 RESTRICT 的对比：**

| 选项 | 含义 | 行为 |
|:---|:---|:---|
| **CASCADE**（级联） | 连坐制 | 收回用户权限的同时，将该用户**转授给他人**的权限也一并收回 |
| **RESTRICT**（限制） | 阻却制 | 若用户已将权限转授，则**拒绝执行回收**，防止权限链断裂 |

> **记忆法：** CASCADE是"连坐"——A犯错，A带出来的B、C全受罚；RESTRICT是"有牵连就不收"——你想收但发现有连带关系，干脆不收了。

**📌 考试重点：**
- GRANT = 授予权限 ✅
- REVOKE = 回收权限 ✅
- 两者都是**数据控制语句(DCL)** ✅
- CREATE/ALTER/DROP **不是**DCL（是DDL）⚠️
- INSERT/UPDATE/DELETE **不是**DCL（是DML）⚠️

---

### 【模块3】角色（ROLE）

> 角色是权限管理的"批处理"工具，极大简化权限分配工作。

---

#### 3.1 角色是什么

| 问题 | 答案 |
|:---|:---|
| **角色 = 用户？** | ❌ **不是！** 角色不是数据库用户，不能登录数据库 |
| **角色是什么？** | 角色是一组权限的**集合/容器** |
| **为什么需要角色？** | 避免重复授权，实现权限的**批量管理** |

#### 3.2 角色的操作流程（三步走）

```
第一步：创建角色容器
        CREATE ROLE Teacher_Role;
        
第二步：往容器里装权限
        GRANT SELECT, UPDATE ON Student TO Teacher_Role;
        
第三步：把容器分配给用户
        GRANT Teacher_Role TO Teacher_A;
        GRANT Teacher_Role TO Teacher_B;
        GRANT Teacher_Role TO Teacher_C;
                   ↓
   Teacher_A ──▶ ┌─────────────────────┐
   Teacher_B ──▶ │Teacher_Role的权限集合 │ ──▶ 自动获得SELECT, UPDATE权限
   Teacher_C ──▶ └─────────────────────┘
```

#### 3.3 有角色 vs 无角色的对比

| 场景 | 无角色 | 有角色 |
|:---|:---|:---|
| 10个教师都需要相同权限 | 写10次GRANT语句 | 创建1个角色，1次GRANT给角色，再分给10人 |
| 需要新增1个权限 | 需要修改10个人的权限 | 只需修改角色的权限，所有人**自动生效** |
| 某教师离职 | 单独回收其权限 | 回收其角色即可 |
| 教师变更为助教权限 | 逐一修改 | 换一个角色分配即可 |

**示例场景：**
```sql
-- 无角色：10个教师 → 10次授权
GRANT SELECT, UPDATE ON Student TO Teacher_A;
GRANT SELECT, UPDATE ON Student TO Teacher_B;
-- ... 还有8次 ...

-- 有角色：1个角色 → 1次授权 → 分配给10人
CREATE ROLE Teacher_Role;
GRANT SELECT, UPDATE ON Student TO Teacher_Role;
GRANT Teacher_Role TO Teacher_A, Teacher_B, Teacher_C, ...;  -- 一次搞定
```

**📌 考试重点：**
- 角色 ≠ 用户（判断题常见陷阱）✅
- 角色是权限的**集合** ✅
- 角色创建用 **CREATE ROLE** ✅
- 角色属于**安全性**范畴（不是完整性！）✅

---

### 【模块4】四大概念的系统性区分

> 考试中频繁出现"以下哪个属于安全性/完整性/并发控制/恢复"这类题型，以下是完整的辨析框架。

---

#### 4.1 一句话记忆法

| 概念 | 一句话 | 核心问题 | 好比 |
|:---:|:---|:---|:---:|
| **安全性** | **防坏人** | 数据被非法访问怎么办？ | 门锁+门禁+监控 |
| **完整性** | **防错误** | 数据不正确、不一致怎么办？ | 质检员+规则手册 |
| **并发控制** | **防冲突** | 多人同时操作打架怎么办？ | 交通规则+信号灯 |
| **恢复** | **防丢失** | 数据损坏了怎么办？ | 保险柜+备份钥匙 |

#### 4.2 各概念的详细辨析

**① 安全性（Security）— 防坏人**

| 维度 | 内容 |
|:---|:---|
| **要解决的问题** | 非法用户闯入、合法用户越权操作 |
| **手段（重点记忆）** | 用户标识与鉴别、DAC（GRANT/REVOKE）、MAC、审计、视图、数据加密 |
| **目标** | 只有合法用户做权限范围内的事 |
| **关键词** | 非法用户、越权、保护、权限、安全 |
| **不属于的** | 触发器（属于完整性） |

**② 完整性（Integrity）— 防错误**

| 维度 | 内容 |
|:---|:---|
| **要解决的问题** | 数据不符合业务规则、数据前后矛盾 |
| **手段（重点记忆）** | 实体完整性（主键）、参照完整性（外键）、用户定义完整性（CHECK/NOT NULL/UNIQUE）、断言(ASSERTION)、**触发器(TRIGGER)**、域约束(DOMAIN) |
| **目标** | 保证数据**正确、有效、相容**（标准定义词，选择题常考） |
| **关键词** | 正确、有效、相容、一致性、约束 |

**③ 并发控制（Concurrency）— 防冲突**

| 维度 | 内容 |
|:---|:---|
| **要解决的问题** | 多用户同时操作导致的数据不一致 |
| **具体问题** | 丢失修改、不可重复读、读脏数据、幻读 |
| **手段** | 封锁（X锁/S锁）、时间戳、MVCC |
| **目标** | 保证事务的隔离性 |
| **关键词** | 并发、同时操作、冲突 |

**④ 恢复（Recovery）— 防丢失**

| 维度 | 内容 |
|:---|:---|
| **要解决的问题** | 数据库发生故障后数据丢失或损坏 |
| **故障类型** | 事务故障、系统故障（断电/崩溃）、介质故障（磁盘损坏）、计算机病毒 |
| **手段** | 数据转储（备份）、日志文件（REDO/UNDO）、镜像、检查点 |
| **目标** | 故障后恢复到正确状态 |
| **关键词** | 故障、破坏、恢复、转储、日志 |

#### 4.3 速记表格（必背）

| 概念 | 记忆口诀 | 题干线索词 | 典型手段 | 不属于该范畴的 |
|:---:|:---:|:---:|:---|:---:|
| **安全性** | 防坏人 | 非法用户、越权、保护 | 标识鉴别、DAC、MAC、审计、视图 | 触发器 |
| **完整性** | 防错误 | **正确、有效、相容** | 主键、外键、CHECK、触发器 | — |
| **并发控制** | 防冲突 | 并发、同时操作 | 封锁、时间戳、MVCC | — |
| **恢复** | 防丢失 | 故障、破坏、崩溃 | 日志、转储、镜像 | — |

> **📌 解题技巧：**
> - 题干出现"正确、有效、相容" → 直接选**完整性**（这是完整性的标准定义用词）
> - 题干出现"非法用户"或"防止非法操作" → 直接选**安全性**
> - 题干出现"触发器"→ 属于**完整性**，不是安全性
> - 题干出现"主体与客体"→ **强制存取控制(MAC)**

---

### 【模块5】强制存取控制（MAC）规则专项突破

> 这是本章**必考内容**，且是学生最容易搞反规则的考点。下面用多种视角反复强化。

---

#### 5.1 规则速记卡（随身记）

```
┌──────────────┬────────────────────────────────┬──────────────────────┐
│   操作       │ 条件公式                       │ 三字口诀             │
├──────────────┼────────────────────────────────┼──────────────────────┤
│   📖 读      │ 主体级别 ≥ 客体级别 → 可读     │ "向下读"             │
│   ✏️ 写      │ 主体级别 ≤ 客体级别 → 可写     │ "向上写"             │
└──────────────┴────────────────────────────────┴──────────────────────┘
```

#### 5.2 分场景推演（安全级别：绝密5 > 机密4 > 秘密3 > 公开1）

**场景一：绝密级用户（级别5）**

| 操作 | 目标客体 | 比较 | 结果 | 原因 |
|:---|:---|:---:|:---:|:---|
| 读 | 绝密文件(5) | 5≥5 | ✅ | 同级可读 |
| 读 | 机密文件(4) | 5≥4 | ✅ | 向下读 |
| 写 | 公开文件(1) | 5≤1? ❌ | ❌ | 不能写低级（防泄露） |
| 写 | 绝密文件(5) | 5≤5 | ✅ | 同级可写 |

**场景二：公开级用户（级别1）**

| 操作 | 目标客体 | 比较 | 结果 | 原因 |
|:---|:---|:---:|:---:|:---|
| 读 | 公开文件(1) | 1≥1 | ✅ | 只能读公开的 |
| 读 | 秘密文件(3) | 1≥3? ❌ | ❌ | 级别不够，不能读 |
| 写 | 公开文件(1) | 1≤1 | ✅ | 可写自己的级别 |
| 写 | 绝密文件(5) | 1≤5 | ✅ | 向上写 |

#### 5.3 常见错误分析

```
❌ 常见错误：认为"高级别用户可以写低级别文件"
   错误原因：把读规则和写规则搞混了
   正解：读可以向下（高级别读低级别），但写不能向下（只能向上写）

❌ 常见错误：认为"MAC是所有DBMS必备"
   错误原因：把DAC和MAC的普及程度搞混了
   正解：大多数DBMS提供DAC，MAC是可选的增强功能

❌ 常见错误：混淆≥和≤的方向
   记住方法： 
     读(Read) → 字母R像向下的箭头 ↘ → 主体≥客体（向下读）
     写(Write) → 字母W像向上的箭头 ↗ → 主体≤客体（向上写）
```

#### 5.4 特殊情况总结

| 条件 | 读 | 写 | 说明 |
|:---:|:---:|:---:|:---|
| **主体 > 客体**（高级别读/写低级别） | ✅ 可读 | ❌ 不可写 | 读低写高，此处写被禁止 |
| **主体 = 客体**（同级别） | ✅ 可读 | ✅ 可写 | 同级操作不受限 |
| **主体 < 客体**（低级别读/写高级别） | ❌ 不可读 | ✅ 可写 | 读低写高，此处读被禁止 |

> **📌 终极记忆法：**
> - **"向下读"** = 级别高的读级别低的 ✅
> - **"向上写"** = 级别低的写级别高的 ✅
> - **站在主体（用户）的角度思考**：你能读比你低的，能写比你高的

---

### 【模块6】高频误区与易错点汇总

> 以下是在本章题目中反复出现的错误认知，全部来自历年考试真题的常见失分点。

| # | 错误认知 ❌ | 正确理解 ✅ | 对应题目 |
|:---:|:---|:---|:---:|
| 1 | MAC是所有DBMS的必备功能 | MAC**不是**必备功能，大多数商用DBMS主要提供DAC | 题10 |
| 2 | 审计可以阻止非法操作 | 审计只是**事后记录**，不能阻止操作 | 概念题 |
| 3 | 触发器属于安全性 | 触发器属于**完整性**，不是安全性 | 题2、题8 |
| 4 | 角色就是数据库用户 | 角色是**权限的集合**，≠ 用户 | 题3选项C |
| 5 | CREATE是DCL数据控制语句 | CREATE是**DDL**，DCL只有GRANT和REVOKE | 题5 |
| 6 | 向上写的条件是主体>客体 | 向上写的条件是主体级别**≤**客体级别 | 题3 |
| 7 | DAC比MAC更安全 | MAC比DAC更严格，安全性更高 | 题3选项A |
| 8 | 视图不能用于安全性 | 视图通过隐藏敏感列可以实现安全性控制 | 题6 |

> **本章考试命中率最高的3个考点：**
> 1. **MAC规则**："向下读、向上写"的方向判断
> 2. **DCL归属**：只有GRANT和REVOKE是数据控制语句
> 3. **概念辨析**：安全性 vs 完整性 vs 并发控制 vs 恢复

---

## 三、解题思路与一般方法

**安全性 vs 其他概念的快速判断：**
1. 题干中出现"非法用户"、"防止非法操作"、"保护"→ 安全性
2. 题干中出现"正确、有效、相容"→ 完整性
3. 题干中出现"并发"、"同时操作"→ 并发控制
4. 题干中出现"破坏"、"恢复"→ 恢复

**强制存取控制规则判断：**
- 读：主体级别 ≥ 客体级别 ✓
- 写：主体级别 ≤ 客体级别 ✓
- 记法："向下读、向上写"

---

## 四、见到这种题目的思考框架

```
看到"主体与客体" → 强制存取控制(MAC)
看到"用户标识与鉴别" / "自主存取控制" / "强制存取控制" / "审计" → 安全性手段
看到"触发器"被问是否属于安全性 → 不属于！触发器属于完整性
看到"角色" → CREATE ROLE → 属于安全性
看到"授予/回收权限" → GRANT / REVOKE → 数据控制语句(DCL)
看到"视图+授权" → 安全性手段
看到"保证正确、有效、相容" → 完整性，不是安全性
看到"防止非法用户" → 安全性
看到"审计可选" → 正确（耗时耗资源，非必需）
看到"强制存取控制必备" → 错误（不是所有DBMS必备）
```

---

## 五、本题详细解答

### 题1 — 主体与客体属于
**答案：D. 强制存取权限控制**

强制存取控制(MAC)的核心机制就是给主体（用户）和客体（数据）分配安全级别，根据级别规则控制访问。

### 题2 — 不属于安全性控制手段的是
**答案：D. 触发器**

触发器用于实现完整性约束（如级联更新），不属于安全性控制手段。A（用户标识与鉴别）、B（自主存取控制）、C（审计）都是安全性手段。

### 题3 — 关于安全性说法正确的是
**答案：B. 强制存取控制要求主体的许可证级别小于或等于客体的密级时，该主体才能写相应的客体**

- A错误：强制存取控制比自主存取控制更严格
- B正确：这是MAC的"向上写"规则——主体级别 ≤ 客体级别才能写
- C错误：角色不是数据库用户，是权限的集合
- D错误：审计是可选的，不是必选的

### 题4 — 定义角色的SQL语句
**答案：C. CREATE ROLE**

CREATE ROLE用于创建角色，属于安全性的权限管理范畴。

### 题5 — 属于数据控制语句的是
**答案：D. GRANT和REVOKE**

GRANT（授予）和REVOKE（回收）是数据控制语言(DCL)，用于权限管理。

### 题6 — 视图和授权可以实现
**答案：B. 安全性**

视图可以隐藏敏感列、授权可以限制用户操作，二者都是安全性控制手段。

### 题7 — 数据库安全性旨在
**答案：B. 保护数据库中的数据，防止非法用户和合法用户的非法操作**

- A描述的是完整性（正确、有效、相容）
- B正确：安全性的核心目标
- C、D描述的是具体约束，不是安全性的总体目标

### 题8 — DBMS提供的安全性手段不包括
**答案：D. 触发器**

触发器属于完整性机制，不属于安全性手段。

### 题9（判断）— 审计可作为可选安全性手段
**答案：对**

审计记录每次数据访问操作，能追踪可疑行为，但会消耗系统资源（时间和存储空间），因此常作为可选功能而非必选。

### 题10（判断）— 强制存取控制是所有DBMS必须提供的
**答案：错**

强制存取控制不是所有DBMS的必备功能，许多商用DBMS主要提供自主存取控制(DAC)，MAC常用于对安全性要求较高的场景（如军事、政府部门）。
