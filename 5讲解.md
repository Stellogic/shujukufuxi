# 第5章 数据库完整性 — 题目讲解

---

## 一、题目分析

本章11道单选题围绕**数据库完整性**展开，涵盖完整性约束的违反应对策略、高级完整性机制（断言、触发器、域）、完整性约束的分类层级（类型约束→属性约束→关系约束→数据库约束），以及与安全性概念的区分。

> **题目类型：** 概念辨析型 + 层次分类型选择题
> **考点：**
> - 违反实体完整性的处理方式（拒绝）
> - 违反参照完整性的处理选项（级联CASCADE、拒绝RESTRICT、置空值SET NULL、置默认值SET DEFAULT）
> - 高级完整性机制（断言ASSERTION、触发器TRIGGER、域DOMAIN）
> - 完整性约束的分类层级
> - 完整性 vs 安全性的区分
> - CONSTRAINT关键字

---

## 二、相关知识点梳理

```
📚 完整性约束违反应对策略
├── 📍 违反实体完整性 → 系统操作：拒绝(REJECT)
│   └── 实体完整性要求主码唯一且非空，任何违反的操作都会被拒绝
├── 📍 违反参照完整性的处理选项
│   ├── CASCADE（级联）— 连带更新/删除相关元组
│   ├── RESTRICT（拒绝）— 如果有引用则拒绝操作
│   ├── SET NULL（置空值）— 将引用该元组的外码设为NULL
│   └── SET DEFAULT（置默认值）— 将引用该元组的外码设为默认值
└── 📍 对照记忆：
    级联 = 一起改/删；
    拒绝 = 不允许；
    置空值 = 设为NULL；
    置默认值 = 设为默认值

📚 SQL高级完整性机制
├── 📍 断言(ASSERTION) — CREATE ASSERTION
│   ├── 定义：数据库级的完整性约束（跨表约束）
│   └── 属于完整性范畴
├── 📍 触发器(TRIGGER) — CREATE TRIGGER
│   ├── 定义：事件驱动的完整性规则
│   └── 不属于安全性控制手段
├── 📍 域(DOMAIN) — CREATE DOMAIN
│   └── 定义：带约束的数据类型（自定义数据类型）
├── 📍 角色(ROLE) — CREATE ROLE
│   └── 属于安全性范畴，不属于完整性
└── 📍 约束命名 — CONSTRAINT 约束名

📚 完整性 vs 安全性
├── 📍 完整性（Integrity）
│   ├── 目标：保证数据正确、有效、相容
│   ├── 手段：主码约束、外码约束、断言、触发器、域约束等
│   └── 关键词：数据正确性
└── 📍 安全性（Security）
    ├── 目标：保护数据防止非法访问
    ├── 手段：用户标识与密码、自主存取控制、强制存取控制、审计等
    └── 关键词：数据保护

📚 约束的分类（按作用范围从大到小）
├── 📍 数据库约束 — 跨多个关系的约束
│   └── 例：断言(ASSERTION)、外码约束属于此层级
├── 📍 关系约束 — 约束单个关系（表级）
│   └── 例：主码约束(PRIMARY KEY)、UNIQUE、CHECK
├── 📍 属性约束 — 约束单个属性（列级）
│   └── 例：NOT NULL
└── 📍 类型约束 — 约束取值类型
    └── 例：DOMAIN定义的类型约束
```

**常见误区提醒：**
- **外码约束属于什么层级？** 外码约束涉及两个表（参照关系+被参照关系），属于**数据库约束**，不是关系约束！
- **主码约束属于什么层级？** 主码约束只涉及一个表内部，属于**关系约束**。
- **完整性和安全性不要混淆：** 完整性 = 数据正确；安全性 = 数据不被非法访问。

---

## 三、解题思路与一般方法

**分类题（题7-10）的判断逻辑：**
1. 先明确题目要求的是"完整性"还是"安全性"
2. 完整性手段：约束、断言、触发器
3. 安全性手段：用户标识与密码、存取控制（自主/强制）、审计、角色
4. 约束层级判断：涉及几个表？0个→属性约束，1个→关系约束，多个→数据库约束

**参照完整性处理策略判断题：**
- CASCADE = 跟着改
- RESTRICT = 不让改
- SET NULL = 改空
- SET DEFAULT = 改默认

---

## 四、见到这种题目的思考框架

```
看到"违反实体完整性" → 拒绝
看到"级联操作" → CASCADE
看到"置默认值" → SET DEFAULT
看到"置空值" → SET NULL
看到"断言" → CREATE ASSERTION → 属于完整性
看到"触发器" → CREATE TRIGGER → 属于完整性，不属于安全性
看到"域" → CREATE DOMAIN
看到"角色" → CREATE ROLE → 属于安全性
看到"给约束起名" → CONSTRAINT
看到"主码约束属于什么" → 关系约束（只约束本表）
看到"外码约束属于什么" → 数据库约束（跨两个表）
看到"完整性范畴" → 断言、主码、外码、域、触发器
看到"安全性范畴" → 自主存取控制、强制存取控制、用户标识与密码
```

---

## 五、本题详细解答

### 题1 — 违反实体完整性规则时的操作
**答案：A. 拒绝**

实体完整性要求主码唯一且非空，任何违反此规则的操作（如插入重复主码、插入NULL主码）都会被系统直接拒绝。

### 题2 — 实现级联操作
**答案：A. CASCADE**

CASCADE（级联）指当被参照表的主码被修改/删除时，参照表中所有引用了该主码的外码值也跟着修改/删除。

### 题3 — 实现置默认值操作
**答案：D. SET DEFAULT**

SET DEFAULT策略：当被参照表的主码被删除时，参照表中对应的外码值自动设置为预先定义的默认值。

### 题4 — 定义断言的SQL语句
**答案：C. CREATE ASSERTION**

断言是数据库级的完整性约束，可以表达跨多个表的约束条件。

### 题5 — 定义触发器的SQL语句
**答案：B. CREATE TRIGGER**

触发器是一种特殊的存储过程，当特定的数据库事件（INSERT/UPDATE/DELETE）发生时自动执行。

### 题6 — 定义域的SQL语句
**答案：D. CREATE DOMAIN**

域是带有约束条件的数据类型。CREATE DOMAIN允许用户定义自己的数据类型并附加约束。

### 题7 — 属于完整性范畴的是
**答案：A. 断言**

断言用于定义数据库完整性约束。B、C、D（自主存取控制、用户标识与密码、强制存取控制）都属于安全性范畴。

### 题8 — 不属于完整性范畴的是
**答案：D. 强制存取权限控制**

强制存取权限控制（MAC）是安全性手段，不属于完整性范畴。主码约束、外码约束、断言都属于完整性。

### 题9 — 主码约束属于
**答案：C. 关系约束**

主码约束只在单个关系的范围内起作用（约束该表内主码的唯一性和非空性），属于关系约束层级。

### 题10 — 外码约束属于
**答案：D. 数据库约束**

外码约束涉及两个关系（参照关系引用被参照关系），是跨关系的约束，属于数据库约束层级。

### 题11 — 给约束起名字的短语
**答案：C. CONSTRAINT**

使用CONSTRAINT关键字可以给约束命名，如`CONSTRAINT PK_Student PRIMARY KEY(Sno)`，便于后续修改或删除约束。
