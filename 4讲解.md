# 第4章 SQL语言 — 题目讲解

---

## 一、题目分析

本章27道单选题覆盖SQL的核心语法知识，包括DDL（数据定义）、DML（数据操纵）、DQL（数据查询）、DCL（数据控制），以及视图和嵌入式SQL。题目全是概念辨析型，考察对各子句/关键字的用途记忆。

> **题目类型：** 概念辨析型选择题
> **考点：**
> - SQL语句分类：CREATE/ALTER/DROP、INSERT/UPDATE/DELETE、SELECT
> - 完整性约束关键字：PRIMARY KEY、FOREIGN KEY、UNIQUE、NOT NULL
> - SELECT查询子句：SELECT、FROM、WHERE、GROUP BY、HAVING、ORDER BY、LIKE
> - 聚集函数：COUNT、SUM、AVG、MAX、MIN
> - 集合操作：UNION、INTERSECT、EXCEPT
> - 视图的定义、性质与限制
> - 嵌入式SQL通信机制：SQLCODE、主语言变量、游标

---

## 二、相关知识点梳理

```
📚 SQL语句分类
├── 📍 DDL（数据定义语言）
│   ├── CREATE TABLE — 定义基本表
│   ├── CREATE VIEW — 定义视图
│   ├── CREATE INDEX — 定义索引
│   ├── ALTER TABLE — 修改基本表结构
│   ├── DROP TABLE/INDEX/VIEW — 删除数据库对象
│   └── 聚集索引关键字：CLUSTER
├── 📍 DML（数据操纵语言）
│   ├── INSERT — 插入数据
│   ├── UPDATE — 修改数据
│   └── DELETE — 删除数据
├── 📍 DQL（数据查询语言）
│   └── SELECT — 查询数据
└── 📍 DCL（数据控制语言）
    ├── GRANT — 授予权限
    └── REVOKE — 回收权限

📚 完整性约束定义关键字
├── 📍 PRIMARY KEY — 定义主码（非空且唯一）
├── 📍 FOREIGN KEY ... REFERENCES — 定义外码（参照完整性）
├── 📍 UNIQUE — 定义候选码（唯一，可以为空）
├── 📍 NOT NULL — 定义非空约束
└── 📍 CHECK — 定义检查约束（用户自定义完整性）

📚 SELECT查询语句子句结构
SELECT [列名/聚集函数]       ← 必须：指定查询结果列
FROM [表名]                   ← 必须：指定从哪些表查询
WHERE [条件]                  ← 可选：过滤行的条件
GROUP BY [分组列]             ← 可选：分组
HAVING [分组过滤条件]         ← 可选：过滤分组（必须与GROUP BY联用）
ORDER BY [排序列] [ASC|DESC]  ← 可选：排序
├── 📍 LIKE — 模糊查询，配合通配符%和_
├── 📍 聚集函数：COUNT()计数、SUM()求和、AVG()均值、MAX()最大值、MIN()最小值
└── 📍 分组规则：有GROUP BY时，SELECT子句中只能出现分组属性或聚集函数

📚 集合操作
├── 📍 UNION — 并（合并两个查询结果，自动去重）
├── 📍 INTERSECT — 交（取两个查询结果的公共部分）
└── 📍 EXCEPT — 差（第一个查询结果去掉第二个查询结果中的元组）

📚 视图（View）
├── 📍 定义：CREATE VIEW
├── 📍 作用：
│   ├── 简化复杂查询
│   ├── 提供逻辑独立性（注意：不提供物理独立性！）
│   ├── 安全性保护（隐藏部分数据）
│   └── 使用户以不同角度看待相同数据
├── 📍 更新限制：
│   ├── 行列子集视图可以更新
│   ├── 基于聚集函数的视图不能更新 ← 重要考点
│   └── 基于视图定义的视图可能不能更新
└── 关键：视图提供的是逻辑独立性，不是物理独立性！

📚 嵌入式SQL通信机制
├── 📍 SQLCODE — 返回SQL语句执行状态（成功/失败/异常）
├── 📍 主语言变量 — 主语言向SQL语句传递参数
├── 📍 游标 — 当查询结果为多个元组时，用游标逐条处理
└── 注意：普通变量不是通信机制，主语言变量才是！
```

**常见误区提醒：**
- **视图提供逻辑独立性还是物理独立性？** 视图提供的是逻辑独立性，物理独立性由模式-内模式映像提供！题23考这一点。
- **UNIQUE vs PRIMARY KEY：** UNIQUE定义的候选码可以为NULL，PRIMARY KEY不行。
- **没有GROUP BY时不能使用聚集函数？** 错！可以，聚集函数会把所有行作为一组。
- **GROUP BY和HAVING的区别：** WHERE过滤行 → GROUP BY分组 → HAVING过滤分组 → ORDER BY排序

---

## 三、解题思路与一般方法

SQL选择题的判断逻辑：
1. 看题目问的是"定义什么"→ 对应DDL关键字（CREATE TABLE/VIEW/INDEX）
2. 看题目问的是"修改什么"→ ALTER TABLE
3. 看题目问的是"控制什么"→ GRANT/REVOKE
4. 看题目问的是"查询的哪个子句"→ 对应SELECT语句各子句功能
5. 看题目问的是"视图能否更新"→ 聚集函数视图不能更新
6. 看题目问的是"嵌入式SQL怎么传递/接收"→ 游标=多行结果，主语言变量=传参

---

## 四、见到这种题目的思考框架

```
看到"定义表" → CREATE TABLE
看到"定义索引" → CREATE INDEX
看到"修改表结构" → ALTER TABLE
看到"删除对象" → DROP
看到"主码定义" → PRIMARY KEY
看到"外码定义" → FOREIGN KEY
看到"候选码定义" → UNIQUE
看到"聚集索引" → CLUSTER
看到"查询必不可少的子句" → SELECT和FROM（WHERE可选！）
看到"查询条件" → WHERE
看到"排序" → ORDER BY
看到"分组" → GROUP BY
看到"模糊查询" → LIKE
看到"分组后SELECT限制" → 只能选分组属性或聚集函数
看到"计数" → COUNT()
看到"并/交/差操作" → UNION / INTERSECT / EXCEPT
看到"插入/修改/删除" → INSERT / UPDATE / DELETE
看到"视图不能更新" → 基于聚集函数的视图
看到"视图不能提供什么独立性" → 物理独立性
看到"多行查询结果" → 游标
看到"向SQL传参" → 主语言变量
看到"给约束起名" → CONSTRAINT
```

---

## 五、本题详细解答

### 题1 — 定义表的SQL语句
**答案：B. CREATE TABLE**

CREATE TABLE用于创建基本表，是DDL中最常用的语句。

### 题2 — 定义索引的SQL语句
**答案：D. CREATE INDEX**

CREATE INDEX用于在表的列上创建索引，加速查询。

### 题3 — 修改基本表结构
**答案：B. ALTER TABLE**

ALTER TABLE用于修改表结构（添加列、删除列、修改列定义、添加/删除约束等）。

### 题4 — 定义主码
**答案：A. PRIMARY KEY**

PRIMARY KEY定义主码，隐含NOT NULL和UNIQUE。

### 题5 — 定义外码
**答案：B. FOREIGN KEY**

FOREIGN KEY ... REFERENCES ... 定义外码约束，建立参照关系。

### 题6 — 定义候选码
**答案：D. UNIQUE**

UNIQUE约束保证属性值唯一，允许NULL（候选码的特征）。PRIMARY KEY也是唯一的但不能为NULL。

### 题7 — 删除数据库对象
**答案：C. DROP**

DROP用于删除数据库对象（表、视图、索引等）。

### 题8 — 定义聚集索引关键字
**答案：C. CLUSTER**

SQL中定义聚集索引使用CLUSTER关键字（有些数据库使用CLUSTERED关键字）。

### 题9 — 查询必不可少的子句
**答案：A. SELECT和FROM**

最简单的查询只需要SELECT和FROM子句：`SELECT * FROM table;`。WHERE是可选的。

### 题10 — 表达查询条件的子句
**答案：C. WHERE子句**

WHERE子句指定行过滤条件，只有满足条件的元组才会出现在结果中。

### 题11 — 用于排序
**答案：D. ORDER BY子句**

ORDER BY用于对查询结果排序，ASC升序（默认），DESC降序。

### 题12 — 用于分组
**答案：C. GROUP BY子句**

GROUP BY将元组按指定属性值分组，常与聚集函数配合使用。

### 题13 — 用于模糊查询
**答案：B. LIKE短语**

LIKE配合通配符%（任意多个字符）和_（单个字符）进行模糊匹配。

### 题14 — 说法正确的是
**答案：C. 有分组子句时，SELECT子句中的结果列必须是分组属性或聚集函数**

- A错误：没有GROUP BY时也可以用聚集函数（会把所有行当一组）
- B错误：GROUP BY恰恰是细化聚集函数作用范围的
- C正确：这是SQL标准的规定
- D错误

### 题15 — 用来计数的聚集函数
**答案：D. COUNT()**

COUNT()用于统计行数，COUNT(*)统计所有行，COUNT(列名)统计非空值个数。

### 题16 — 实现并操作
**答案：A. UNION**

UNION合并两个查询结果，自动消除重复元组。UNION ALL保留重复。

### 题17 — 实现交操作
**答案：B. INTERSECT**

INTERSECT返回两个查询结果中共同存在的元组。

### 题18 — 实现差操作
**答案：C. EXCEPT**

EXCEPT返回在第一个结果中存在但在第二个结果中不存在的元组。

### 题19 — 实现插入操作
**答案：B. INSERT**

INSERT INTO 表名 VALUES(...) 或 INSERT INTO 表名 SELECT ... 插入新数据。

### 题20 — 实现修改操作
**答案：A. UPDATE**

UPDATE 表名 SET 列名=新值 WHERE 条件 修改已有数据。

### 题21 — 实现删除操作
**答案：C. DELETE**

DELETE FROM 表名 WHERE 条件 删除满足条件的元组，不带WHERE则删除全部。

### 题22 — 关于视图正确的是
**答案：D. 基于聚集函数定义的视图不能被更新**

- A错误：视图不一定能更新（取决于定义方式）
- B错误：行列子集视图是可以更新的
- C错误：基于视图定义的视图通常不可更新
- D正确：这是视图可更新的关键限制

### 题23 — 关于视图不正确的是
**答案：B. 视图提供了一定程度的物理独立性**

视图提供的是**逻辑独立性**，不是物理独立性！物理独立性由模式-内模式映像保证。A、C、D都是视图的正确功能。

### 题24 — 定义视图的SQL语句
**答案：C. CREATE VIEW**

CREATE VIEW 视图名 AS SELECT... 定义一个视图。

### 题25 — SQL与主语言通信不能通过
**答案：B. 变量**

普通变量不是SQL与主语言的通信机制。SQLCODE（状态反馈）、主语言变量（传参）、游标（处理多行结果）才是通信机制。选项"变量"不准确。

### 题26 — 查询结果为多个元组时必须使用
**答案：D. 游标**

当SELECT返回多个元组时，需要用游标逐行处理。单个元组可以直接用INTO子句放入主语言变量。

### 题27 — 主语言向SQL传递参数
**答案：C. 主语言变量**

主语言程序通过主语言变量（Host Variable）向SQL语句传递参数值。
