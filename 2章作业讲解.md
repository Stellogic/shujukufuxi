# 第2章作业 — ER图设计习题讲解

---

## 一、题目分析

本章3道大题要求设计ER图，分别对应三个真实场景：
1. 汽车保险公司系统（客户-车辆-事故）
2. 银行系统（支行-客户-存款-贷款）
3. 公司管理系统（部门-职工-项目-办公室）

> **题目类型：** ER图设计题
> **考点：**
> - 从需求描述中识别实体、属性、联系
> - 确定联系的基数约束（1:1、1:N、M:N）
> - 识别复合属性、多值属性、弱实体
> - 正确使用ER图符号（矩形、菱形、椭圆、双线）

---

## 二、相关知识点梳理

```
📚 ER图设计通用步骤
├── Step 1: 找出实体 — 从名词中识别
│   └── 识别标准：能否独立存在、有没有属性、有没有唯一标识
├── Step 2: 找出属性 — 描述实体特征的名称
│   ├── 每个属性只能属于一个实体
│   └── 注意复合属性和多值属性
├── Step 3: 找出联系 — 从动词/关联描述中识别
│   └── 例："拥有"、"属于"、"办理"、"发生在"
├── Step 4: 分析基数约束
│   ├── 读每个方向的"最多/至少"含义
│   └── 确定1:1、1:N、M:N
├── Step 5: 确定主键
│   └── 检查实体是否有唯一标识属性
└── Step 6: 绘制ER图并检查
```

**ER图符号速查：**
- 矩形框 → 实体集
- 菱形框 → 联系集
- 椭圆形框 → 属性
- 双矩形 → 弱实体集
- 双菱形 → 弱实体集与属主实体集之间的识别联系
- 双椭圆 → 多值属性
- 虚椭圆 → 派生属性
- 下划线标注 → 主码

---

## 三、解题思路与一般方法

**从文字描述到ER图的转换技巧：**

1. **找实体：** 扫描题目描述中"被登记信息和编号的对象"
   - 提示词："记录"、"登记"、"编号"、"ID"
2. **找联系：** 扫描题目中的动词或关系词
   - "拥有"、"属于"、"发生"、"办理"、"关联"
3. **定基数：** 注意数量词
   - "每个...一个/多个"、"0次或多次"、"最多一个"
4. **处理多值属性：** 画双椭圆（或单独建表）
5. **处理复合属性：** 列出子属性
6. **弱实体：** 自身无码，必须依赖其他实体

---

## 四、见到这种题目的思考框架

```
第一步：鸟瞰全文，圈出所有名词 → 候选实体
第二步：逐一检查每个候选实体 → 有独立标识(ID)？有属性？→ 确认实体
第三步：圈出动词和关联词 → 候选联系
第四步：每个联系读基数约束 → 找"每个/每次/最多/可以"等词确定1:1/1:N/M:N
第五步：检查"复合属性"(可拆分)、"多值属性"(多个取值)、"派生属性"(可计算)
第六步：画图 — 矩形(实体) → 菱形(联系) → 椭圆(属性)，确保连线不交叉
第七步：自检 — 题目给的每个信息遗漏了吗？基数约束标注了吗？
```

---

## 五、本题详细解答

### 题1 — 2.7 汽车保险公司ER图设计

**Step 1 — 识别实体：**
- **客户：** 有客户ID、姓名、住址、电话
- **汽车：** 有车辆编号、车型、出厂年份
- **事故：** 有事故编号、事故发生日期、发生地点、损坏估计

**Step 2 — 识别联系：**
- 客户 **拥有** 汽车（1:N：每个客户拥有多辆车，每辆车只属于一个客户）
- 汽车 **发生** 事故（1:N：每辆车可能发生多次事故，每次事故只涉及一辆车）

**Step 3 — ER图描述：**
```
                        ┌─────────┐
              姓名 ──── │         │──── 住址
                        │   客户   │
              电话 ──── │         │──── 客户ID（主码）
                        └────┬────┘
                             │1
                             │
                          ┌──┴──┐
                          │拥有 │
                          └──┬──┘
                             │N
                        ┌────┴────┐
             出厂年份 ── │         │──── 车辆编号（主码）
                        │   汽车   │
              车型  ─── │         │
                        └────┬────┘
                             │1
                             │
                          ┌──┴──┐
                          │发生 │
                          └──┬──┘
                             │N
                        ┌────┴────┐
             事故日期 ── │         │──── 事故编号（主码）
                        │   事故   │
             发生地点 ── │         │──── 损坏估计
                        └─────────┘
```

**基数约束说明：**
- 客户 → 汽车：1:N（一个客户拥有一辆或多辆汽车，一辆汽车只属于一个客户）
- 汽车 → 事故：1:N（一辆汽车可能发生0次或多次事故，一次事故涉及一辆汽车）

---

### 题2 — 2.8 工商银行ER图设计

**Step 1 — 识别实体：**
- **支行：** 名称（唯一）、资产、坐落城市、坐落街道
- **客户：** 客户标识、客户名、客户地址、联系电话
- **存款账户：** 账号、存款余额
- **贷款：** 贷款号、贷款日期、贷款金额
- **存取款记录**（弱实体或属性）

**Step 2 — 识别联系：**
- 支行 **位于** 某地（自身属性覆盖）
- 客户 **拥有** 存款账户（1:N：一个客户可有多个账户，一个账户属于一个客户）
- 存款账户上 **办理** 存取款（需要记录每次的日期和金额，这是多对多联系，但可以转换为弱实体）
- 客户 **申请** 贷款（1:N：一个客户可多次贷款，一笔贷款只贷给一个客户）
- 贷款 **关联** 支行（N:1：每笔贷款与一个支行关联，一个支行可发放多笔贷款）

**Step 3 — 简化处理的ER图（核心部分）：**

客户(客户标识, 客户名, 客户地址, 联系电话)

存款账户(账号, 存款余额) — 属于客户(1:N)

贷款(贷款号, 贷款日期, 贷款金额) — 贷款给客户(N:1)，关联支行(N:1)

支行(支行名称, 资产, 所在城市, 所在街道) — 关联贷款(1:N)

存取款记录(日期, 金额) — 依附于存款账户（弱实体/多值复合属性）

**关键处理点：**
- 存取款记录本身没有独立的码（需要通过账号+日期才能唯一标识），可以作为存款账户关联的弱实体，也可以作为存款账户的一个多值复合属性（每次存取款=日期+金额）。

---

### 题3 — 2.10 公司管理系统ER图设计

**Step 1 — 识别实体：**
- **部门：** 部门号（唯一）、部门名称、预算费、部门领导职工号
- **职工：** 职工号（唯一）、职工姓名、家庭住址、电话
- **项目：** 项目号（唯一）、项目名称、预算
- **办公室：** 办公室名称（唯一）、位置、电话

**Step 2 — 识别联系：**
- 部门 **包含** 职工（1:N：一个部门有若干职工，一个职工属于一个部门）
- 部门 **拥有** 项目（1:N：一个部门有若干项目，一个项目属于一个部门）
- 部门 **拥有** 办公室（1:N：一个部门有若干办公室，一个办公室属于一个部门）
- 职工 **当前参加** 项目（M:N：一个职工可参加多个项目，一个项目有多个职工参加）
- 职工 **曾在** 项目工作（工作经历：M:N，带有属性：起止年月、工资）
- 职工 **所在** 办公室（N:1：多个职工可在一个办公室）

**Step 3 — 关键处理：**
1. **工作经历：** 职工和项目之间的多对多联系，附有属性"起止年月"和"工资"。这是一个典型的"带属性的联系"——工作经历本身需要记录若干信息，在ER图中联系上附加属性即可，在转换为关系模式时需要单独建表。

2. **办公室电话：** 办公室有若干部电话 → 电话是多值属性，用双椭圆表示。

3. **部门领导：** "部门领导的职工号" → 这是职工和部门之间的"领导"联系（1:1，一个部门有一个领导职工，一个职工最多领导一个部门）。

**简化ER图（文字版）：**

职工(职工号←主码, 职工姓名, 家庭住址, 电话)
    ├── 参加项目(M:N) 联系属性: 起止年月、工资
    ├── 属于部门(N:1)
    ├── 领导部门(1:1)
    └── 所在办公室(N:1)

部门(部门号←主码, 部门名称, 预算费)
    ├── 属于职工(1:N)
    ├── 拥有项目(1:N)
    ├── 拥有办公室(1:N)
    └── 被职工领导(1:1)

项目(项目号←主码, 项目名称, 预算)
    ├── 被职工参加(M:N)

办公室(办公室名称←主码, 位置)
    ├── 电话(多值属性)
    └── 容纳职工(1:N)
```

---

## 六、从文字描述提取ER图的通用口诀

1. **找实体看"名词+编号"：** 有唯一标识的事物 = 实体
2. **找属性看"特征描述"：** 描述事物某方面特性的词 = 属性
3. **找联系看"动词"：** 两个事物之间的动作/关系 = 联系
4. **定基数看"几个"：** 每个/最多/可以多个 → 确定1:1, 1:N, M:N
5. **找弱实体看"没有自己的码"：** 无法独立标识的实体 = 弱实体
6. **找多值属性看"多个"：** 能有多个取值的 = 多值属性（双椭圆）
7. **找复合属性看"可以再分"：** 能被拆解的 = 复合属性
8. **找派生属性看"可以从...计算"：** 可推导的 = 派生属性（虚椭圆）
