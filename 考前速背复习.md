# 数据库系统原理考前速背复习

> 依据仓库中的单元测试题 `1.md` 到 `11.md`、作业、`22年真题.md` 和考前复习课件整理。讲解文件只作框架参考，重点按单元测试题、真题和课件来背。

## 0. 考试结构与复习优先级

课件给出的题型：

| 题型 | 分值 | 复习重点 |
|---|---:|---|
| 单选题 | 20 | 概念速判、关键字对应 |
| 简答题 | 20 | 安全性、完整性、设计阶段、冗余、并发、派生属性 |
| 分析设计题 | 25 | E-R 图、E-R 转关系、规范化/BCNF 分解 |
| 关系代数和 SQL | 35 | 查询表达、聚集分组、连接、全部/没有、建表和更新 |

复习优先级：

1. 关系代数 + SQL：分值最高，必须会写。
2. E-R 设计 + E-R 转关系 + 数据库设计阶段：分析设计题高频。
3. 规范化理论：候选码、范式、无损连接、保持依赖、BCNF 分解。
4. 完整性、安全性、事务并发、恢复：选择和简答都容易考。
5. 数据库系统概述、查询优化：多为概念和关键词速判。

## 1. 必背知识点

### 1.1 数据库系统概述

数据模型三要素：

- 数据结构：描述数据对象以及对象之间的联系，是静态特性。
- 数据操作：定义对数据允许执行的操作及规则，是动态特性。
- 完整性约束：保证数据正确、有效、相容。

常见数据模型：

- 层次模型：最早出现。
- 网状模型。
- 关系模型：目前最流行，用二维表表示数据。
- 面向对象模型。

三级模式结构：

| 层次 | 含义 | 个数 |
|---|---|---|
| 外模式 | 用户看到的数据逻辑结构 | 可多个 |
| 模式 | 全体数据的逻辑结构 | 一个 |
| 内模式 | 数据物理结构和存储方式 | 一个 |

两级映像：

- 外模式-模式映像：保证逻辑独立性。
- 模式-内模式映像：保证物理独立性。

数据库语言：

- DDL：数据定义语言，如 `CREATE TABLE`、`ALTER TABLE`、`DROP`。
- DML：数据操纵语言，如 `SELECT`、`INSERT`、`UPDATE`、`DELETE`。
- DCL：数据控制语言，如 `GRANT`、`REVOKE`。

### 1.2 E-R 模型

基本概念：

- 实体：客观存在且可区分的事物。
- 属性：实体具有的特性。
- 联系：实体之间的关联。
- 超码：能唯一标识实体，但可能含多余属性。
- 候选码：最小超码，不含多余属性。
- 主码：选定的候选码。
- 弱实体集：自身属性不足以唯一标识，依赖属主实体集。

属性类型：

- 复合属性：可继续分解，如地址可分为省、市、街道。
- 多值属性：一个实体可有多个取值，如多个电话。
- 派生属性：可由其他属性计算得到，如年龄由出生日期得到。

E-R 图符号：

| 符号 | 含义 |
|---|---|
| 矩形 | 实体集 |
| 菱形 | 联系集 |
| 椭圆 | 属性 |
| 双矩形 | 弱实体集 |
| 双菱形 | 标识性联系 |
| 双椭圆 | 多值属性 |
| 虚线椭圆 | 派生属性 |
| 属性下划线 | 主码 |

联系类型：

- 1:1：两边每个实体最多对应对方一个实体。
- 1:N：一端一个实体可对应多端多个实体，多端每个实体最多对应一端一个实体。
- M:N：两边都可对应多个实体。

### 1.3 关系模型与 E-R 转关系

关系模型基本点：

- 关系可用二维表表示。
- 关系中不能有两个完全相同的元组。
- 行、列的次序不重要。
- 主码不能取空值。
- 外码可以取空值，除非另有约束。
- 主码受实体完整性约束；外码受参照完整性约束。

E-R 转关系规则：

| E-R 成分 | 转换规则 |
|---|---|
| 强实体集 | 转成一个关系模式，属性照抄，主码为实体主码 |
| 弱实体集 | 与属主实体的主码一起组成关系模式，通常主码为“属主主码 + 弱实体部分码” |
| 1:1 联系 | 可把任一端主码放入另一端作外码，也可单独建联系表 |
| 1:N 联系 | 把 1 端主码放入 N 端作为外码；联系属性也放在 N 端 |
| M:N 联系 | 单独建联系关系，主码通常为两端主码联合；联系属性放入该关系 |
| 多值属性 | 单独建关系，主码通常为“所属实体主码 + 多值属性” |
| 复合属性 | 展平为各个组成属性，忽略复合属性本身 |
| 派生属性 | 在总体 E-R 图/模式设计中通常去掉；外模式可按用户需要加入 |

关系代数五种基本运算：

- 并 `∪`
- 差 `-`
- 投影 `π`
- 选择 `σ`
- 笛卡儿积 `×`

常用扩展运算：

- 交、连接、自然连接、除法。
- 广义投影、聚集函数、分组聚集、外连接。

速记：

- 选择 `σ`：筛行。
- 投影 `π`：取列，可能去重。
- 自然连接 `⋈`：按同名属性相等连接，并去掉重复列。
- 除法 `÷`：处理“所有、全部、至少包含全部”。
- 差 `-`：处理“没有、不属于、不满足”。

### 1.4 SQL

SQL 特点：

- 集数据定义、查询、操纵、控制于一体。
- 高度非过程化。
- 面向集合操作。
- 同一种语法可交互使用，也可嵌入主语言。

常用关键字对应：

| 题目问法 | 答案 |
|---|---|
| 定义表 | `CREATE TABLE` |
| 定义视图 | `CREATE VIEW` |
| 定义索引 | `CREATE INDEX` |
| 修改基本表结构 | `ALTER TABLE` |
| 删除数据库对象 | `DROP` |
| 主码 | `PRIMARY KEY` |
| 外码 | `FOREIGN KEY ... REFERENCES` |
| 候选码/唯一 | `UNIQUE` |
| 非空 | `NOT NULL` |
| 检查约束 | `CHECK` |
| 给约束命名 | `CONSTRAINT` |
| 聚集索引 | `CLUSTER` |
| 并 | `UNION` |
| 交 | `INTERSECT` |
| 差 | `EXCEPT` |
| 插入 | `INSERT` |
| 修改 | `UPDATE` |
| 删除 | `DELETE` |

查询语句顺序：

```sql
SELECT 目标列或聚集函数
FROM 表
WHERE 行条件
GROUP BY 分组列
HAVING 分组条件
ORDER BY 排序列 ASC/DESC;
```

必背规则：

- `SELECT` 和 `FROM` 是查询必不可少的子句。
- `WHERE` 写普通筛选条件。
- `GROUP BY` 分组。
- `HAVING` 对分组结果筛选。
- `ORDER BY` 排序。
- `LIKE` 用于模糊查询，`%` 表示任意长度字符串，`_` 表示任意单个字符。
- 有 `GROUP BY` 时，`SELECT` 子句只能出现分组属性或聚集函数。
- 没有 `GROUP BY` 也可以使用聚集函数，此时把所有行看作一组。

聚集函数：

- `COUNT()`：计数。
- `SUM()`：求和。
- `AVG()`：平均。
- `MAX()`：最大。
- `MIN()`：最小。

视图：

- `CREATE VIEW` 定义视图。
- 视图可简化查询、增强逻辑独立性、提供安全保护、让用户从不同角度看数据。
- 视图提供逻辑独立性，不提供物理独立性。
- 基于聚集函数定义的视图不能更新。

嵌入式 SQL：

- `SQLCODE`：返回 SQL 执行状态。
- 主语言变量：主语言向 SQL 传递参数。
- 游标：查询结果为多个元组时逐条处理。

### 1.5 完整性

三类完整性：

- 实体完整性：主码唯一且非空。
- 参照完整性：外码要么为空，要么等于被参照表某个主码值。
- 用户自定义完整性：业务规则，如成绩 0 到 100。

违约处理：

- 违反实体完整性：拒绝。
- 违反参照完整性：可拒绝、级联、置空值、置默认值。

参照完整性短语：

- `CASCADE`：级联。
- `RESTRICT`：拒绝。
- `SET NULL`：置空值。
- `SET DEFAULT`：置默认值。

高级完整性机制：

- `CREATE ASSERTION`：断言，常用于数据库级/跨表约束。
- `CREATE TRIGGER`：触发器，事件发生后自动执行。
- `CREATE DOMAIN`：域，定义带约束的数据类型。
- `CONSTRAINT`：给约束起名字。

完整性和安全性区别：

- 完整性：保证数据正确、有效、相容。
- 安全性：保护数据，防止非法用户和合法用户的非法操作。

约束层级：

| 层级 | 例子 |
|---|---|
| 类型约束 | `DOMAIN` |
| 属性约束 | `NOT NULL`、列级 `CHECK` |
| 关系约束 | 主码约束、表级 `CHECK`、`UNIQUE` |
| 数据库约束 | 外码约束、断言、跨表约束 |

易错：主码约束属于关系约束；外码约束涉及两个关系，属于数据库约束。

### 1.6 安全性

安全性目标：

- 保护数据库中的数据，防止非法用户和合法用户的非法操作。

安全性控制手段：

- 用户标识与鉴别。
- 自主存取控制。
- 强制存取控制。
- 视图。
- 审计。
- 数据加密。

不属于安全性控制手段：

- 触发器。触发器主要属于完整性机制。

自主存取控制：

- 通过 `GRANT` 授权。
- 通过 `REVOKE` 回收权限。
- `WITH GRANT OPTION` 表示被授权者还能继续转授权。
- `CASCADE` 表示级联回收。
- `RESTRICT` 表示存在转授权依赖时拒绝回收。

授权模板：

```sql
GRANT 权限
ON 对象
TO 用户或角色
[WITH GRANT OPTION];
```

回收模板：

```sql
REVOKE 权限
ON 对象
FROM 用户或角色
[CASCADE | RESTRICT];
```

角色：

```sql
CREATE ROLE 角色名;
GRANT 权限 ON 对象 TO 角色名;
GRANT 角色名 TO 用户;
```

强制存取控制：

- 主体：用户、进程等主动访问者，有许可证级别。
- 客体：数据、文件等被访问对象，有密级。
- 读规则：主体许可证级别大于等于客体密级才可读，即“向下读”。
- 写规则：主体许可证级别小于等于客体密级才可写，即“向上写”。

判断题常考：

- 审计耗时耗资源，通常作为可选安全手段。
- 强制存取控制不是所有 DBMS 必须提供的安全手段。

### 1.7 规范化理论

函数依赖：

- `X -> Y`：若两个元组在 X 上相等，则在 Y 上也相等。
- 平凡函数依赖：`Y` 是 `X` 的子集。
- 非平凡函数依赖：`Y` 不是 `X` 的子集。
- 完全函数依赖：`X -> Y` 成立，且 X 的任意真子集都不能推出 Y。
- 部分函数依赖：`X -> Y` 成立，但 X 的某个真子集也能推出 Y。
- 传递函数依赖：`X -> Y`、`Y -> Z`，且 `Y` 不能推出 `X`，则 Z 传递依赖于 X。

Armstrong 公理：

- 自反律：若 `Y ⊆ X`，则 `X -> Y`。
- 增广律：若 `X -> Y`，则 `XZ -> YZ`。
- 传递律：若 `X -> Y`、`Y -> Z`，则 `X -> Z`。

常用推理规则：

- 合并规则：`X -> Y` 且 `X -> Z`，则 `X -> YZ`。
- 分解规则：`X -> YZ`，则 `X -> Y` 且 `X -> Z`。
- 伪传递规则：`X -> Y` 且 `WY -> Z`，则 `WX -> Z`。

候选码求法：

1. 把属性分成四类：
   - L 类：只出现在函数依赖左边，必在候选码中。
   - R 类：只出现在右边，通常不在候选码中。
   - N 类：左右都不出现，必在候选码中。
   - LR 类：左右都出现，待定。
2. 先算 `(L ∪ N)+`。
3. 如果闭包包含全部属性，则 `L ∪ N` 是候选码。
4. 否则依次加入 LR 类属性求闭包，找到能推出全部属性的最小属性集。

主属性和非主属性：

- 主属性：出现在任意候选码中的属性。
- 非主属性：不出现在任何候选码中的属性。

范式：

| 范式 | 要求 | 违反时表现 |
|---|---|---|
| 1NF | 属性不可再分 | 有嵌套表、复合/多值属性未处理 |
| 2NF | 非主属性完全依赖于候选码 | 非主属性对码有部分依赖 |
| 3NF | 非主属性不传递依赖于候选码 | 非主属性对码有传递依赖 |
| BCNF | 每个非平凡函数依赖的决定因素都是超码 | 存在非码决定因素 |
| 4NF | BCNF 基础上消除非平凡多值依赖 | 非平凡多值依赖左部不是超码 |

重要结论：

- 只有两个属性的关系模式，最高可达 BCNF。
- 极小函数依赖集可能有 1 个或多个，不一定唯一。
- 所有属性都是主属性时，最高范式必定至少是 3NF，但不一定是 BCNF。
- 只要求无损连接分解，一定可分解到 BCNF。
- 要求无损连接且保持函数依赖，一定可分解到 3NF，不一定能到 BCNF。
- 2NF 不彻底：没有消除非主属性对码的传递依赖。
- 3NF 不彻底：没有消除主属性对码的部分依赖和传递依赖。
- BCNF 是针对主属性问题进一步提出的。

二元分解无损连接判断：

设 `R` 分解为 `R1`、`R2`，若满足下面任一条件，则无损连接：

- `(R1 ∩ R2) -> (R1 - R2)`
- `(R1 ∩ R2) -> (R2 - R1)`

保持函数依赖判断：

- 看原来的每个函数依赖，是否能在分解后的某个关系或投影依赖集合中推出。
- 若某个依赖左右属性被拆到不同关系中，且无法推回，则不保持。

BCNF 分解模板：

1. 求候选码。
2. 找违反 BCNF 的依赖 `X -> Y`，即 X 不是超码。
3. 分解为：
   - `R1 = X ∪ Y`
   - `R2 = R - (Y - X)`
4. 对分解后的关系继续检查，直到每个关系都满足 BCNF。

真题模板：

`STC(Sno, Tname, Cname)`，依赖为：

- `Sno, Cname -> Tname`
- `Tname -> Cname`

候选码：

- `(Sno, Cname)`
- `(Sno, Tname)`

所以所有属性都是主属性，属于 3NF；但 `Tname -> Cname` 中 `Tname` 不是超码，所以不属于 BCNF。BCNF 分解为：

- `R1(Tname, Cname)`
- `R2(Sno, Tname)`

### 1.8 数据库设计阶段

六个阶段：

| 阶段 | 核心任务 | 标志性成果 |
|---|---|---|
| 需求分析 | 分析数据和处理需求 | 数据字典、数据流图 |
| 概念结构设计 | 抽象信息世界模型 | 分 E-R 图、全局 E-R 图 |
| 逻辑结构设计 | E-R 转关系、规范化、优化、外模式设计 | 关系模式 |
| 物理结构设计 | 存储结构、存取路径、索引 | 物理存储结构、索引 |
| 数据库实施 | 建库、装入数据、调试试运行 | 可运行数据库系统 |
| 运行与维护 | 日常管理、维护、调整 | 运行中的系统 |

速判：

- 数据字典、数据流图：需求分析。
- 分 E-R 图、全局 E-R 图：概念结构设计。
- 关系模式优化、规范化、外模式设计：逻辑结构设计。
- 索引设计、存储路径设计：物理结构设计。

### 1.9 事务与并发控制

ACID：

| 特性 | 含义 |
|---|---|
| 原子性 | 事务中的操作要么全做，要么全不做 |
| 一致性 | 事务使数据库从一个一致状态变到另一个一致状态 |
| 隔离性 | 一个事务的操作对并发事务隔离 |
| 持久性/永久性 | 事务一旦提交，对数据库的改变永久有效 |

事务语句：

- `COMMIT`：提交事务。
- `ROLLBACK`：回滚事务。

锁：

- X 锁/排他锁/写锁：可读也可写；与任何锁互斥。
- S 锁/共享锁/读锁：只可读；多个 S 锁可相容，但与 X 锁互斥。

并发不一致问题：

- 丢失修改。
- 读脏数据。
- 不可重复读。
- “可重复读”不是问题本身。

三级封锁协议：

| 协议 | 规则 | 能解决 |
|---|---|---|
| 一级封锁协议 | 修改前加 X 锁，事务结束释放 | 丢失修改 |
| 二级封锁协议 | 一级 + 读前加 S 锁，读完释放 | 丢失修改、读脏数据 |
| 三级封锁协议 | 一级 + 读前加 S 锁，事务结束释放 | 丢失修改、读脏数据、不可重复读 |

死锁与活锁：

- 活锁：事务一直等不到服务。解决：先来先服务。
- 死锁：事务互相等待对方资源。预防：一次封锁法、顺序封锁法；解决：检测和解除策略。

调度：

- 串行调度一定正确。
- 并发调度正确的标准：结果与某个串行调度结果相同，即可串行化。
- 冲突可串行化一定是可串行化调度，是正确调度。

### 1.10 数据库恢复

故障类型：

| 故障 | 判断关键词 | 恢复思路 |
|---|---|---|
| 事务故障 | 某个事务未正常结束、逻辑错误、运算异常、死锁回滚 | UNDO 该事务 |
| 系统故障 | 系统突然停止、断电、所有事务非正常终止 | UNDO 未完成事务，REDO 已提交事务 |
| 介质故障 | 磁盘损坏、硬件损坏、数据库被破坏 | 装入后备副本，再用日志 REDO |

恢复核心思想：

- 建立冗余数据，而不是杜绝故障。
- 冗余数据主要有日志文件、数据库后备副本、镜像盘。
- 缓冲区不是冗余数据，也不是恢复技术。

日志：

- 记录所有更新操作，不记录查询操作。
- 必须严格按并发事务执行的时间次序登记。
- 必须先记日志，后写数据库。

REDO/UNDO：

- REDO：重做，以新值代替旧值。
- UNDO：撤销，以旧值代替新值。

检查点：

- 目的：改善恢复效率。
- 不是为了解决某类故障，也不是增加安全性。

转储分类：

- 按是否允许事务运行：静态转储、动态转储。
- 按转储数据范围：海量转储、增量转储。

恢复技术：

- 基于日志和后备副本的恢复。
- 影子分页技术。
- 数据库镜像。
- 不包括缓冲区技术。

### 1.11 查询处理与优化

课件单独列了查询优化模块，仓库没有单独测试题，但考前要背关键词。

查询处理过程：

- 语法分析与翻译。
- 查询优化。
- 查询执行。

选择运算实现：

- 全表扫描。
- 使用索引。

连接运算实现：

- 嵌套循环连接。
- 块嵌套循环连接。
- 索引嵌套循环连接。
- 排序-归并连接。
- 散列连接。

查询优化：

- 代数优化：基于等价变换规则改写查询表达式。
- 物理优化：选择具体执行算法和存取路径。

启发式规则：

- 选择运算尽量先做。
- 投影运算尽量先做，只保留后续需要的列。
- 把笛卡儿积和选择合并成连接。
- 先做能显著缩小中间结果的操作。

## 2. 常见题型做题方法

### 2.1 单选题速判

看到题干关键词，直接对照：

| 关键词 | 选什么 |
|---|---|
| 静态特性、对象联系 | 数据结构 |
| 动态特性、允许操作 | 数据操作 |
| 正确、有效、相容 | 完整性约束 |
| 全体数据逻辑结构 | 模式 |
| 用户数据逻辑结构 | 外模式 |
| 数据物理结构 | 内模式 |
| 逻辑独立性 | 外模式-模式映像 |
| 物理独立性 | 模式-内模式映像 |
| 可以有多个 | 外模式 |
| 实体图形 | 矩形 |
| 联系图形 | 菱形 |
| 属性图形 | 椭圆 |
| 多值属性 | 双椭圆 |
| 派生属性 | 虚椭圆 |
| 主码取值限制 | 实体完整性 |
| 外码取值限制 | 参照完整性 |
| 主码能否空 | 不能 |
| 外码能否空 | 可以，除非另有约束 |
| 行运算 | 选择 |
| 列运算且去重 | 投影 |
| 同名属性才能做 | 自然连接 |
| 笛卡儿积逆运算 | 除法 |
| 事务全做或全不做 | 原子性 |
| 提交后永久 | 持久性 |
| 并发事务互不干扰 | 隔离性 |
| 一致状态到一致状态 | 一致性 |
| 记录每次更新 | 日志文件 |
| 改善恢复效率 | 检查点 |
| 新值代替旧值 | REDO |
| 旧值代替新值 | UNDO |

### 2.2 简答题模板

#### 简述保护数据库安全性的措施

可答：

数据库安全性措施包括用户标识与鉴别、存取控制、视图、审计、数据加密等。用户标识与鉴别用于确认用户身份；存取控制限制用户能访问哪些数据、执行哪些操作；视图可隐藏敏感数据；审计记录用户操作；加密保护存储和传输中的数据。

#### 什么是派生属性？不同设计阶段如何处理？

可答：

派生属性是可以由其他属性或实体计算得到的属性，也称计算属性，如年龄可由出生日期计算得到。在总体 E-R 图设计或模式设计时，一般去掉派生属性，避免冗余；在外模式设计时，可根据用户查询和显示需要再加入。

#### 简述关系的完整性规则

可答：

关系完整性规则包括实体完整性、参照完整性和用户自定义完整性。实体完整性要求主码唯一且非空；参照完整性要求外码要么为空，要么引用被参照关系中已有主码值；用户自定义完整性由具体应用语义决定。

#### 简述数据库设计阶段及成果

可答：

数据库设计通常包括需求分析、概念结构设计、逻辑结构设计、物理结构设计、数据库实施、运行与维护。需求分析成果是数据字典和数据流图；概念结构设计成果是全局 E-R 图；逻辑结构设计成果是关系模式和外模式；物理结构设计成果是存储结构和索引；实施阶段建立数据库、装入数据并调试；运行维护阶段进行日常管理和调整。

#### 如何看待数据库中的冗余现象

可答：

冗余有消极作用，也有积极作用。消极方面，冗余会导致修改异常、插入异常和删除异常，需要通过规范化和模式分解减少不良冗余。积极方面，数据库恢复需要日志、后备副本、镜像等冗余数据，以便故障后恢复。因此冗余既可能破坏数据一致性，也能提高恢复能力，关键是控制合理冗余。

#### 什么是并发调度？并发执行有什么好处？

可答：

并发调度是指一组事务中至少有两个事务已经开始执行且尚未结束，它们的操作交叉进行。并发执行可以提高系统吞吐量和资源利用率，也可以减少事务等待时间，但必须通过并发控制避免数据不一致。

### 2.3 E-R 设计题方法

步骤：

1. 读题圈名词：找实体，如客户、汽车、事故、支行、账户、贷款、学生、班级、学会。
2. 找“登记/记录/编号/ID”：这些通常提示实体和属性。
3. 圈动词：找联系，如拥有、发生、办理、属于、参加、工作于。
4. 看数量词：确定 1:1、1:N、M:N，如“每个”“多个”“最多一个”“0 次或多次”。
5. 标主码：题目说“编号、ID、唯一名称”通常是码。
6. 处理特殊属性：多值属性双椭圆，派生属性虚椭圆，复合属性拆开。
7. 检查题目每一句是否都在图里体现。

常见场景：

- “学生参加多个学会，每个学会有多个学生，参加有入会年份”：学生-学会是 M:N 联系，联系属性是入会年份。
- “客户有多个账户，一个账户属于一个客户”：客户-账户是 1:N。
- “每笔贷款只能贷给一个客户，一位客户可多次贷款”：客户-贷款是 1:N。
- “每个部门有若干职工”：部门-职工是 1:N。
- “职工做过项目，有起止年月和工资”：职工-项目是 M:N 联系，联系上带属性。

### 2.4 E-R 转关系题方法

写关系模式时按下面顺序：

1. 每个强实体先写一个表。
2. 1:N 联系把 1 端主码加到 N 端。
3. M:N 联系单独建表，把两端主码作为联合主码。
4. 联系有属性就放进联系表；如果 1:N 合并到 N 端，就放进 N 端表。
5. 多值属性单独建表。
6. 弱实体表要加属主实体主码。
7. 最后标明主码和外码。

模板：

```text
实体A(Aid, A属性...)
实体B(Bid, B属性..., Aid)          -- 1:N 时 Aid 放入 N 端
联系AB(Aid, Bid, 联系属性...)      -- M:N 时单独建表
```

### 2.5 关系代数题方法

通用五步：

1. 看最后要输出什么属性：最后一定用投影 `π`。
2. 看条件在哪张表：先选择 `σ`。
3. 涉及多个表：用连接 `⋈`。
4. 出现“没有、不、未”：用差 `-`。
5. 出现“所有、全部、至少包含全部”：用除法 `÷`。

常用模板：

筛选：

```text
σ 条件(关系)
```

投影：

```text
π 属性列表(关系)
```

连接后查询：

```text
π 输出属性(σ 条件(R ⋈ S ⋈ T))
```

“没有/不提供”：

```text
全集 - 满足条件的集合
```

例如“不提供 P2 的供应商名称”：

```text
πSname((πSno(Suppliers) - πSno(σPno='P2'(SPJ))) ⋈ Suppliers)
```

“所有/全部”：

```text
πA,B(关系) ÷ πB(要求全集)
```

例如“提供所有零件的供应商名称”：

```text
πSname(Suppliers ⋈ (πSno,Pno(SPJ) ÷ πPno(Parts)))
```

“本地”：

```text
σ 供应商城市 = 工程城市(Suppliers ⋈ SPJ ⋈ Projects)
```

聚集扩展关系代数：

```text
γ 聚集函数(属性)->结果名(关系)
γ 分组属性; 聚集函数(属性)->结果名(关系)
```

例如“S1 提供每种零件的总数量”：

```text
γ Pno; SUM(Quantity)->TotalQuantity(σSno='S1'(SPJ))
```

### 2.6 SQL 语句题方法

SQL 查询通用思路：

1. `SELECT` 写最终要显示的列。
2. `FROM` 写涉及的表。
3. `WHERE` 写连接条件和筛选条件。
4. 有“每个/按/分别统计”就 `GROUP BY`。
5. 对分组后的结果筛选用 `HAVING`。
6. “所有/全部”优先想双重 `NOT EXISTS`。
7. “没有/不”优先想 `NOT EXISTS` 或 `EXCEPT`。

建表模板：

```sql
CREATE TABLE 表名 (
    列名 数据类型 约束,
    列名 数据类型 约束,
    PRIMARY KEY (主码列),
    FOREIGN KEY (外码列) REFERENCES 被参照表(主码列),
    CHECK (条件)
);
```

连接查询模板：

```sql
SELECT 输出列
FROM A, B, C
WHERE A.主码 = B.外码
  AND C.主码 = B.外码
  AND 普通条件;
```

也可以写成：

```sql
SELECT 输出列
FROM A
JOIN B ON A.主码 = B.外码
JOIN C ON C.主码 = B.外码
WHERE 普通条件;
```

聚集分组模板：

```sql
SELECT 分组列, 聚集函数(列) AS 结果名
FROM 表
WHERE 分组前条件
GROUP BY 分组列
HAVING 分组后条件;
```

“所有/全部”双重 `NOT EXISTS` 模板：

```sql
SELECT ...
FROM 外层对象 x
WHERE NOT EXISTS (
    SELECT *
    FROM 要求全集 y
    WHERE NOT EXISTS (
        SELECT *
        FROM 对应关系 r
        WHERE r.x_id = x.x_id
          AND r.y_id = y.y_id
    )
);
```

含外键删除模板：

```sql
DELETE FROM 子表
WHERE 外码 IN (
    SELECT 主码
    FROM 主表
    WHERE 条件
);

DELETE FROM 主表
WHERE 条件;
```

原因：先删引用表/子表，再删被引用表/主表，避免违反参照完整性。

授权模板：

```sql
GRANT SELECT, INSERT, UPDATE, DELETE
ON 表名
TO 用户
WITH GRANT OPTION;
```

断言模板：

```sql
CREATE ASSERTION 断言名
CHECK (
    NOT EXISTS (
        SELECT *
        FROM 表
        WHERE 违反约束的条件
    )
);
```

触发器模板：

```sql
CREATE TRIGGER 触发器名
AFTER INSERT OR UPDATE OR DELETE ON 表名
FOR EACH ROW
BEGIN
    -- 自动执行的维护语句
END;
```

### 2.7 规范化分析题方法

题目给关系模式和函数依赖时，按下面步骤写：

1. 写出函数依赖集 F。
2. 用闭包法求候选码。
3. 写主属性和非主属性。
4. 判断 2NF：看非主属性是否部分依赖于候选码。
5. 判断 3NF：看非主属性是否传递依赖于候选码。
6. 判断 BCNF：看每个非平凡函数依赖左部是否都是超码。
7. 若要分解到 BCNF，找左部不是超码的依赖，按 `X -> Y` 分解。

闭包写法：

```text
X+ 初始为 X
若 F 中有 A -> B 且 A ⊆ X+，则把 B 加入 X+
重复直到不能增加
若 X+ = U，则 X 是超码；若 X 最小，则 X 是候选码
```

范式判断短语：

- “非主属性对码有部分依赖，所以只满足 1NF。”
- “不存在部分依赖，但存在传递依赖，所以最高满足 2NF。”
- “无非主属性，或非主属性无部分/传递依赖，满足 3NF。”
- “存在决定因素不是超码的依赖，所以不满足 BCNF。”

## 3. 通用思路与易错点

### 3.1 大题先判断题型

- 要画图：先实体、再联系、再属性、再基数。
- 要关系模式：先实体表，再联系表，最后主外码。
- 要 SQL：先输出列，再表，再连接，再条件，再分组。
- 要关系代数：先投影目标，再选择条件，再连接；“没有”用差，“所有”用除。
- 要规范化：先求候选码，不求码不要急着判范式。

### 3.2 关系代数与 SQL 的互译

| 关系代数 | SQL |
|---|---|
| `σ 条件` | `WHERE 条件` |
| `π 属性` | `SELECT 属性` |
| `⋈` | `JOIN ... ON ...` 或 `WHERE A.id=B.id` |
| `-` | `EXCEPT` 或 `NOT EXISTS` |
| `÷` | 双重 `NOT EXISTS` |
| `γ` | `GROUP BY` + 聚集函数 |

### 3.3 最高频易错点

- 外模式可以有多个；模式和内模式通常各一个。
- 逻辑独立性靠外模式-模式映像；物理独立性靠模式-内模式映像。
- `SELECT` 和 `FROM` 必不可少，`WHERE` 可选。
- `UNIQUE` 对应候选码；`PRIMARY KEY` 对应主码。
- 基于聚集函数的视图不能更新。
- 视图提供逻辑独立性，不是物理独立性。
- 主码不能空；外码可以空。
- 自然连接要求同名属性。
- 除法看“所有/全部/至少全部”。
- 外码约束属于数据库约束，不是关系约束。
- 触发器属于完整性机制，不是安全性控制手段。
- 安全性是防非法访问，完整性是保证数据正确。
- 一级封锁只能防丢失修改。
- 二级封锁不能保证可重复读。
- 三级封锁可以解决丢失修改、读脏数据、不可重复读。
- 活锁用先来先服务；死锁用一次封锁法、顺序封锁法、检测解除。
- 恢复的思想是建立冗余数据，不是杜绝故障。
- 日志记录更新，不记录查询。
- 缓冲区不是冗余数据，也不是恢复技术。
- 检查点目的是改善恢复效率。
- 全属性都是主属性时通常能到 3NF，但不一定到 BCNF。
- 无损连接和保持函数依赖是两个不同性质，互不保证。

## 4. 考前 30 分钟速背清单

1. 数据模型三要素：数据结构、数据操作、完整性约束。
2. 三级模式：外模式、模式、内模式；两个映像对应逻辑/物理独立性。
3. E-R 符号：矩形实体、菱形联系、椭圆属性、双椭圆多值、虚椭圆派生、双矩形弱实体。
4. E-R 转关系：1:N 外码放 N 端；M:N 单独建表；多值属性单独建表；复合属性展平；派生属性通常去掉。
5. 关系代数：选择筛行、投影取列、自然连接按同名属性、差表“没有”、除法表“所有”。
6. SQL：`SELECT-FROM-WHERE-GROUP BY-HAVING-ORDER BY`。
7. SQL 关键字：`CREATE TABLE/VIEW/INDEX`、`ALTER TABLE`、`DROP`、`PRIMARY KEY`、`FOREIGN KEY`、`UNIQUE`、`GRANT`、`REVOKE`。
8. 完整性：实体完整性拒绝；参照完整性可拒绝、级联、置空、置默认。
9. 安全性：用户标识、存取控制、视图、审计、加密；`GRANT/REVOKE`。
10. 候选码：L/N 必在码，R 通常不在码，LR 待定，算闭包。
11. 范式：2NF 消部分依赖，3NF 消非主属性传递依赖，BCNF 要每个决定因素都是超码。
12. 设计阶段：需求分析出数据字典和数据流图；概念设计出全局 E-R 图；逻辑设计出关系模式；物理设计做索引和存储路径。
13. ACID：原子性全做全不做；一致性状态正确转换；隔离性互不干扰；持久性提交后永久。
14. 锁：X 锁可读可写；S 锁只读。
15. 恢复：事务故障 UNDO；系统故障 UNDO 未完成 + REDO 已提交；介质故障后备副本 + 日志。
16. 查询优化：选择/投影尽早下推，连接代替笛卡儿积，索引和连接算法属于物理优化。

