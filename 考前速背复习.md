# 数据库系统原理考前速背复习

> 依据仓库中的单元测试题 `1.md` 到 `11.md`、作业、`22年真题.md` 和考前复习课件整理。讲解文件只作框架参考，重点按单元测试题、真题和课件来背。

## 0. 考试结构与复习优先级

课件给出的题型：

| 题型 | 分值 | 复习重点 |
|---|---:|---|
| 单选题 | 20 | 概念速判、关键字对应 |
| 简答题 | 20 | 安全性、完整性、设计阶段、冗余、并发、派生属性 |
| 分析设计题 | 25 | E-R 图、E-R 转关系、规范化/BCNF 分解 |
| 关系代数和 SQL | 35 | 查询表达、聚集分组、连接、全部/没有、建表和更新 |

复习优先级：

1. 关系代数 + SQL：分值最高，必须会写。
2. E-R 设计 + E-R 转关系 + 数据库设计阶段：分析设计题高频。
3. 规范化理论：候选码、范式、无损连接、保持依赖、BCNF 分解。
4. 完整性、安全性、事务并发、恢复：选择和简答都容易考。
5. 数据库系统概述、查询优化：多为概念和关键词速判。

## 1. 必背知识点

### 1.1 数据库系统概述

基本概念：

- 数据：描述事物的符号记录，既有“型”也有“值”。
- 数据库：长期存储在计算机内、有组织、可共享的大量数据集合。
- 数据库管理系统 DBMS：位于用户和操作系统之间，用来管理数据库的软件。
- 数据库系统 DBS：由数据库、DBMS、应用程序、数据库管理员和用户等组成。

DBMS 基本功能：

- 数据定义功能：定义数据库结构。
- 数据组织、存储和管理功能。
- 数据操纵功能：查询、插入、修改、删除。
- 事务管理和运行管理功能：并发控制、恢复、安全性、完整性。
- 数据库建立和维护功能：装入、转储、恢复、重组、性能监控。

数据模型三要素：

- 数据结构：描述数据对象以及对象之间的联系，是静态特性。
- 数据操作：定义对数据允许执行的操作及规则，是动态特性。
- 完整性约束：保证数据正确、有效、相容。

常见数据模型：

- 层次模型：最早出现。
- 网状模型。
- 关系模型：目前最流行，用二维表表示数据。
- 面向对象模型。

三级模式结构：

| 层次 | 含义 | 个数 |
|---|---|---|
| 外模式 | 用户看到的数据逻辑结构 | 可多个 |
| 模式 | 全体数据的逻辑结构 | 一个 |
| 内模式 | 数据物理结构和存储方式 | 一个 |

两级映像：

- 外模式-模式映像：保证逻辑独立性。
- 模式-内模式映像：保证物理独立性。

数据库语言：

- DDL：数据定义语言，如 `CREATE TABLE`、`ALTER TABLE`、`DROP`。
- DML：数据操纵语言，如 `SELECT`、`INSERT`、`UPDATE`、`DELETE`。
- DCL：数据控制语言，如 `GRANT`、`REVOKE`。

### 1.2 E-R 模型

基本概念：

- 实体：客观存在且可区分的事物。
- 属性：实体具有的特性。
- 联系：实体之间的关联。
- 超码：能唯一标识实体，但可能含多余属性。
- 候选码：最小超码，不含多余属性。
- 主码：选定的候选码。
- 弱实体集：自身属性不足以唯一标识，依赖属主实体集。

属性类型：

- 复合属性：可继续分解，如地址可分为省、市、街道。
- 多值属性：一个实体可有多个取值，如多个电话。
- 派生属性：可由其他属性计算得到，如年龄由出生日期得到。

E-R 图符号：

| 符号 | 含义 |
|---|---|
| 矩形 | 实体集 |
| 菱形 | 联系集 |
| 椭圆 | 属性 |
| 双矩形 | 弱实体集 |
| 双菱形 | 标识性联系 |
| 双椭圆 | 多值属性 |
| 虚线椭圆 | 派生属性 |
| 属性下划线 | 主码 |

联系类型：

- 1:1：两边每个实体最多对应对方一个实体。
- 1:N：一端一个实体可对应多端多个实体，多端每个实体最多对应一端一个实体。
- M:N：两边都可对应多个实体。

### 1.3 关系模型与 E-R 转关系

关系模型基本点：

- 关系可用二维表表示。
- 关系中不能有两个完全相同的元组。
- 行、列的次序不重要。
- 主码不能取空值。
- 外码可以取空值，除非另有约束。
- 主码受实体完整性约束；外码受参照完整性约束。

E-R 转关系规则：

| E-R 成分 | 转换规则 |
|---|---|
| 强实体集 | 转成一个关系模式，属性照抄，主码为实体主码 |
| 弱实体集 | 与属主实体的主码一起组成关系模式，通常主码为“属主主码 + 弱实体部分码” |
| 1:1 联系 | 可把任一端主码放入另一端作外码，也可单独建联系表 |
| 1:N 联系 | 把 1 端主码放入 N 端作为外码（**一放n做外码**）；联系属性也放在 N 端 |
| M:N 联系 | 单独建联系关系，主码通常为两端主码联合；联系属性放入该关系 |
| 多值属性 | 单独建关系，主码通常为“所属实体主码 + 多值属性” |
| 复合属性 | 展平为各个组成属性，忽略复合属性本身 |
| 派生属性 | 在总体 E-R 图/模式设计中通常去掉；外模式可按用户需要加入 |

关系代数五种基本运算：

先记两个表：

`Student(学号, 姓名, 系别)`：

| 学号 | 姓名 | 系别 |
|---|---|---|
| 1 | 张三 | 计算机 |
| 2 | 李四 | 数学 |
| 3 | 王五 | 计算机 |

`SC(学号, 课程号, 成绩)`：

| 学号 | 课程号 | 成绩 |
|---|---|---:|
| 1 | C1 | 90 |
| 1 | C2 | 80 |
| 2 | C1 | 85 |

- 选择 `σ`：筛行。  
  例：`σ 系别='计算机'(Student)`，意思是从学生表中筛出计算机系学生，结果是张三、王五。  
  题感：看到“满足某条件的记录/元组”，想到选择。
- 投影 `π`：取列，结果会去重。  
  例：`π 姓名,系别(Student)`，意思是只要姓名、系别两列。  
  例：`π 系别(Student)` 的结果只有“计算机、数学”，重复的计算机只保留一次。  
  题感：看到“查询哪些属性/只显示哪些列”，想到投影。
- 并 `∪`：两个同结构结果合并，并去重。  
  例：`计算机系学生 ∪ 数学系学生`，意思是把两个学生集合合到一起。  
  注意：并运算两边列数、列含义要一致。
- 差 `-`：前面的结果扣掉后面的结果。  
  例：`π 学号(Student) - π 学号(SC)`，意思是“所有学生学号 - 已选课学生学号”，结果是没选课的学生学号 `3`。  
  题感：看到“没有、不属于、未选、未参加、不满足”，想到差。
- 笛卡儿积 `×`：两表所有行两两组合。  
  例：`Student × SC`，Student 有 3 行，SC 有 3 行，结果有 `3 × 3 = 9` 行。  
  完整展开就是 Student 的每一行分别配 SC 的每一行：

  | Student.学号 | 姓名 | 系别 | SC.学号 | 课程号 | 成绩 |
  |---|---|---|---|---|---:|
  | 1 | 张三 | 计算机 | 1 | C1 | 90 |
  | 1 | 张三 | 计算机 | 1 | C2 | 80 |
  | 1 | 张三 | 计算机 | 2 | C1 | 85 |
  | 2 | 李四 | 数学 | 1 | C1 | 90 |
  | 2 | 李四 | 数学 | 1 | C2 | 80 |
  | 2 | 李四 | 数学 | 2 | C1 | 85 |
  | 3 | 王五 | 计算机 | 1 | C1 | 90 |
  | 3 | 王五 | 计算机 | 1 | C2 | 80 |
  | 3 | 王五 | 计算机 | 2 | C1 | 85 |

  其中只有 `Student.学号 = SC.学号` 的行才是真正有意义的学生选课记录，所以再用选择筛一下：

  | Student.学号 | 姓名 | 系别 | SC.学号 | 课程号 | 成绩 |
  |---|---|---|---|---|---:|
  | 1 | 张三 | 计算机 | 1 | C1 | 90 |
  | 1 | 张三 | 计算机 | 1 | C2 | 80 |
  | 2 | 李四 | 数学 | 2 | C1 | 85 |

  作用：它本身一般不直接用，通常配合选择组成连接：`σ Student.学号=SC.学号(Student × SC)`。

常用扩展运算：

- 交 `∩`：两边都有的共同部分。  
  例：`选了C1的学生 ∩ 选了C2的学生`，意思是既选 C1 又选 C2 的学生。  
  题感：看到“既……又……”，想到交。
- 连接 `⋈`：按条件把两张表横向拼起来。  
  例：`Student ⋈ Student.学号=SC.学号 SC`，意思是按学号把学生信息和选课信息拼起来，得到“张三选 C1、C2，李四选 C1”。  
  具体结果：

  | Student.学号 | 姓名 | 系别 | SC.学号 | 课程号 | 成绩 |
  |---|---|---|---|---|---:|
  | 1 | 张三 | 计算机 | 1 | C1 | 90 |
  | 1 | 张三 | 计算机 | 1 | C2 | 80 |
  | 2 | 李四 | 数学 | 2 | C1 | 85 |

  王五没有选课，所以连接结果里没有王五。
  本质：连接 = 笛卡儿积 + 选择。
- 自然连接 `⋈`：自动按同名属性相等连接，并去掉重复列。  
  例：`Student ⋈ SC`，两表都有 `学号`，所以自动按 `学号` 相等连接，结果只保留一个 `学号` 列。  
  题感：看到“同名属性相等连接”，想到自然连接。
- 除法 `÷`：处理“所有、全部、每一个”。  
  例：若 `Need(课程号) = {C1, C2}`，`SC ÷ Need` 表示找出选了 Need 中全部课程的学生。  
  在上表中，只有学号 `1` 同时选了 C1 和 C2，所以结果是 `1`。  
  题感：看到“选修了全部课程、购买了所有商品、参加了每一个项目”，优先想到除法。
- 广义投影：取列时可以顺便计算。  
  例：`π 学号,成绩+5(SC)`，表示查询学号和加 5 分后的成绩。
- 聚集函数：对一组值统计。  
  常见：`COUNT` 计数、`SUM` 求和、`AVG` 平均、`MAX` 最大、`MIN` 最小。  
  例：`AVG(成绩)(SC)`，表示求所有成绩平均分。
- 分组聚集：先分组，再分别统计。  
  例：按 `系别` 分组统计人数，结果类似“计算机 2 人，数学 1 人”。  
  写成关系代数可记作：`系别 γ COUNT(学号)->人数 (Student)`。  
  ```
  系别        γ        COUNT(学号)->人数        (Student)
  分组属性   分组聚集符号      聚集计算             被统计的表
  ```
  计算过程是先按 `系别` 把 Student 分组：

  | 分组 | 包含的学生 |
  |---|---|
  | 计算机 | 张三、王五 |
  | 数学 | 李四 |

  再对每组分别 `COUNT(学号)`：

  | 系别 | 人数 |
  |---|---:|
  | 计算机 | 2 |
  | 数学 | 1 |

  题感：看到“每个系、每门课、每个学生分别统计”，想到分组聚集。
- 外连接：连接时保留没有匹配上的元组，用 `NULL` 补空。  
  例：`Student ⟕ SC` 是左外连接，所有学生都保留；王五没有选课，也会出现，课程号和成绩为 `NULL`。

关系演算：

- 元组关系演算：以元组变量为对象描述查询条件。  
  例：`{t | Student(t) 且 t.系别='计算机'}`，意思是找出满足“是 Student 中元组且系别为计算机”的元组。
- 域关系演算：以属性取值域变量为对象描述查询条件。  
  例：`{<x,y> | 存在 z，使 Student(x,y,z) 且 z='计算机'}`，意思是找出计算机系学生的学号和姓名。
- 关系代数偏“过程化地组合运算”：先筛、再投影、再连接，强调怎么做。
- 关系演算偏“说明满足什么条件”：只描述想要什么结果，不强调具体步骤。

速记：

- 选择 `σ`：筛行。
- 投影 `π`：取列，可能去重。
- 并 `∪`：合并，去重。
- 差 `-`：扣掉，常考“没有、未、不属于”。
- 笛卡儿积 `×`：两表所有组合，行数相乘。
- 交 `∩`：共同部分，常考“既……又……”。
- 连接 `⋈`：按条件横向拼表。
- 自然连接 `⋈`：按同名属性相等连接，并去掉重复列。
- 除法 `÷`：处理“所有、全部、每一个、至少包含全部”。
- 聚集/分组聚集：统计总数、平均、最大最小；“每个……”通常要分组。
- 外连接：没匹配上也保留，用 `NULL` 补。

### 1.4 SQL

SQL 特点：

- 集数据定义、查询、操纵、控制于一体。
- 高度非过程化。
- 面向集合操作。
- 同一种语法可交互使用，也可嵌入主语言。
- 功能强大，语言简洁。

常用关键字对应：

| 题目问法 | 答案 |
|---|---|
| 定义表 | `CREATE TABLE` |
| 定义视图 | `CREATE VIEW` |
| 定义索引 | `CREATE INDEX` |
| 修改基本表结构 | `ALTER TABLE` |
| 删除数据库对象 | `DROP` |
| 主码 | `PRIMARY KEY` |
| 外码 | `FOREIGN KEY ... REFERENCES` |
| 候选码/唯一 | `UNIQUE` |
| 非空 | `NOT NULL` |
| 检查约束 | `CHECK` |
| 给约束命名 | `CONSTRAINT` |
| 聚集索引 | `CLUSTER` |
| 并 | `UNION` |
| 交 | `INTERSECT` |
| 差 | `EXCEPT` |
| 插入 | `INSERT` |
| 修改 | `UPDATE` |
| 删除 | `DELETE` |

查询语句顺序：

```sql
SELECT 目标列或聚集函数
FROM 表
WHERE 行条件
GROUP BY 分组列
HAVING 分组条件
ORDER BY 排序列 ASC/DESC;
```

必背规则：

- `SELECT` 和 `FROM` 是查询必不可少的子句。
- `WHERE` 写普通筛选条件。
- `GROUP BY` 分组。
- `HAVING` 对分组结果筛选。
- `ORDER BY` 排序。
- `LIKE` 用于模糊查询，`%` 表示任意长度字符串，`_` 表示任意单个字符。
- 有 `GROUP BY` 时，`SELECT` 子句只能出现分组属性或聚集函数。
- 没有 `GROUP BY` 也可以使用聚集函数，此时把所有行看作一组。

聚集函数：

- `COUNT()`：计数。
- `SUM()`：求和。
- `AVG()`：平均。
- `MAX()`：最大。
- `MIN()`：最小。

视图：

- `CREATE VIEW` 定义视图。
- 视图可简化查询、增强逻辑独立性、提供安全保护、让用户从不同角度看数据。
- 视图提供逻辑独立性，不提供物理独立性。
- 基于聚集函数定义的视图不能更新。如果一个视图里用了 COUNT、SUM、AVG、MAX、MIN 这类聚集函数，那么这个视图通常不能直接执行 INSERT / UPDATE / DELETE。

物化视图：

- 普通视图只保存查询定义，不真实保存查询结果。
- 物化视图把查询结果物理存储下来。
- 基本表更新后，物化视图需要同步维护；若 DBMS 不支持物化视图，可用触发器在 `INSERT`、`UPDATE`、`DELETE` 时维护一张普通表。

嵌入式 SQL：

- `SQLCODE`：返回 SQL 执行状态。
- 主语言变量：主语言向 SQL 传递参数。
- 游标：查询结果为多个元组时逐条处理。

嵌入式 SQL 方便背诵版：

- 一句话：把 SQL 语句嵌到 C、Java 等主语言程序里，让程序能访问数据库。
- `SQLCODE` 看结果：每执行一条 SQL 后，用它判断“成功、没查到、出错”。常记：`0` 表示成功，`100` 常表示没有数据或游标取完，负数表示出错。
- 主语言变量传数据：主程序变量和 SQL 之间交换数据，通常写成 `:变量名`。例如 `WHERE Sno = :sno` 是主程序把学号传给 SQL，`INTO :name` 是 SQL 把结果放回主程序变量。
- 游标管多行：查询结果有多条记录时，用游标一行一行取。步骤记：`DECLARE` 声明，`OPEN` 打开，`FETCH` 取一行，`CLOSE` 关闭。
- 速记口诀：`SQLCODE` 看状态，主语言变量传数据，游标处理多行结果。

小例子：

```c
EXEC SQL SELECT Sname
INTO :name
FROM Student
WHERE Sno = :sno;
```

- `:sno`：主程序传给 SQL 的查询条件。
- `:name`：SQL 查询后传回主程序的结果。
- 执行后看 `SQLCODE`：`0` 是查到了，`100` 是没查到，负数是出错。

### 1.5 完整性

三类完整性：

- 实体完整性：主码唯一且非空。
- 参照完整性：外码要么为空，要么等于被参照表某个主码值。
- 用户自定义完整性：业务规则，如成绩 0 到 100。

违约处理：

- 违反实体完整性：拒绝。
- 违反参照完整性：可拒绝、级联、置空值、置默认值。

参照完整性短语：

- `CASCADE`：级联。父表记录被删除/主码被修改时，子表中引用它的记录也自动删除/自动更新。
- `RESTRICT`：拒绝。只要子表中还有记录引用父表这条记录，就不允许删除/修改父表记录。
- `SET NULL`：置空值。父表记录被删除/主码被修改时，把子表对应外码改成 `NULL`。
- `SET DEFAULT`：置默认值。父表记录被删除/主码被修改时，把子表对应外码改成预先设置的默认值。

统一例子：

`Dept(Deptno, Dname)` 是部门表，`Student(Sno, Sname, Deptno)` 是学生表，`Student.Deptno` 是外码，引用 `Dept.Deptno`。

| Deptno | Dname |
|---|---|
| 10 | 计算机系 |
| 20 | 数学系 |

| Sno | Sname | Deptno |
|---|---|---|
| 1 | 张三 | 10 |
| 2 | 李四 | 10 |

如果现在要删除 `Deptno = 10` 的计算机系：

| 处理方式 | 结果 |
|---|---|
| `ON DELETE CASCADE` | 张三、李四两条学生记录也被自动删除 |
| `ON DELETE RESTRICT` | 因为还有学生属于计算机系，所以拒绝删除计算机系 |
| `ON DELETE SET NULL` | 张三、李四还在，但他们的 `Deptno` 变成 `NULL` |
| `ON DELETE SET DEFAULT` | 张三、李四还在，但他们的 `Deptno` 变成默认部门号 |

速记：`CASCADE` 是子表跟着变，`RESTRICT` 是不让父表变，`SET NULL` 是外码清空，`SET DEFAULT` 是外码改默认值。

高级完整性机制：

- `CREATE ASSERTION`：断言，常用于数据库级/跨表约束。
- `CREATE TRIGGER`：触发器，事件发生后自动执行。
- `CREATE DOMAIN`：域，定义带约束的数据类型。
- `CONSTRAINT`：给约束起名字。

高级完整性机制例子：

1. `CREATE ASSERTION`：定义跨表/全局约束。

```sql
CREATE ASSERTION salary_limit
CHECK (
    NOT EXISTS (
        SELECT *
        FROM Employee E, Department D
        WHERE E.Deptno = D.Deptno
          AND E.Salary > D.ManagerSalary
    )
);
```

含义：不允许员工工资超过本部门经理工资。  
题感：约束涉及多个表、整个数据库范围时，想到断言。

2. `CREATE TRIGGER`：某事件发生后自动执行动作。

```sql
CREATE TRIGGER update_total
AFTER INSERT ON SC
FOR EACH ROW
UPDATE Student
SET TotalCredit = TotalCredit + 1
WHERE Sno = NEW.Sno;
```

含义：向选课表 `SC` 插入一条选课记录后，自动把该学生总学分/总选课数加 1。  
题感：看到“当……时自动……”想到触发器。

3. `CREATE DOMAIN`：定义带约束的数据类型。

```sql
CREATE DOMAIN score_type AS INT
CHECK (VALUE >= 0 AND VALUE <= 100);
```

使用时：

```sql
CREATE TABLE SC (
    Sno CHAR(10),
    Cno CHAR(10),
    Grade score_type
);
```

含义：`Grade` 使用 `score_type` 域，所以成绩必须在 0 到 100 之间。  
题感：多个属性都要共用同一类取值范围时，想到域。

4. `CONSTRAINT`：给约束起名字，方便报错识别或以后删除。

```sql
CREATE TABLE Student (
    Sno CHAR(10),
    Sname CHAR(20),
    Age INT,
    CONSTRAINT pk_student PRIMARY KEY (Sno),
    CONSTRAINT ck_age CHECK (Age >= 0 AND Age <= 120)
);
```

含义：`pk_student` 是主码约束名，`ck_age` 是年龄检查约束名。  
题感：`CONSTRAINT 约束名 约束内容`，本质是“给约束贴标签”。

完整性和安全性区别：

- 完整性：保证数据正确、有效、相容。
- 安全性：保护数据，防止非法用户和合法用户的非法操作。

约束层级：

| 层级 | 例子 |
|---|---|
| 类型约束 | `DOMAIN` |
| 属性约束 | `NOT NULL`、列级 `CHECK` |
| 关系约束 | 主码约束、表级 `CHECK`、`UNIQUE` |
| 数据库约束 | 外码约束、断言、跨表约束 |

理解方法：看这个约束“管到多大范围”。

- 类型约束：管某一种数据类型/域本身，所有使用这个域的属性都要遵守。  
  例：定义 `score_type` 为 0 到 100，则凡是用 `score_type` 的列都不能超过这个范围。
- 属性约束：只管某一列的取值。  
  例：`Sname NOT NULL` 只要求学生姓名这一列不能为空；列级 `CHECK (Age >= 0)` 只检查 `Age` 这一列。
- 关系约束：管一张表内部的元组和属性之间的关系。  
  例：主码约束要求同一张表里主码不能重复且不能空；`UNIQUE` 要求同一张表某列/某几列不能重复；表级 `CHECK` 可以比较同一行的多个列，如 `CHECK (StartDate <= EndDate)`。
- 数据库约束：管多张表之间或整个数据库范围内的数据相容性。  
  例：外码约束要求子表中的值必须能在父表中找到；断言、跨表约束可以表达“一个学生选课总学分不能超过上限”这类涉及多表或全局的规则。

速判：只管“值的类型范围”是类型约束；只管“一列”是属性约束；只管“一张表内部”是关系约束；涉及“两张及以上表/全局规则”是数据库约束。

易错：主码约束属于关系约束；外码约束涉及两个关系，属于数据库约束。

### 1.6 安全性

安全性目标：

- 保护数据库中的数据，防止非法用户和合法用户的非法操作。

安全性控制手段：

- 用户标识与鉴别：先确认“你是谁”。
- 自主存取控制：再决定“你能干什么”。
- 强制存取控制：按密级硬性规定“高低权限怎么读写”。
- 视图：只让用户看到允许看的部分。
- 审计：把重要操作记日志，事后追查。
- 数据加密：即使数据被拿到，也尽量看不懂。

一句话总背法：安全性 = 身份确认 + 权限控制 + 隐藏数据 + 记录行为 + 加密保护。

不属于安全性控制手段：

- 触发器。触发器主要属于完整性机制。

用户标识与鉴别：

- 用户标识：系统给用户一个身份标记，如用户名、用户 ID、账号。
- 用户鉴别：验证这个用户是不是真的，如密码、口令、指纹、动态验证码、证书。
- 作用：把非法用户挡在数据库外面，是安全控制的第一关。
- 易懂例子：用户名相当于“报名字”，密码相当于“验身份”。
- 考试表达：用户标识与鉴别用于确认访问数据库的用户身份，防止冒名访问。

自主存取控制：

- 英文常写 DAC，核心是“权限由数据拥有者或授权者自主决定”。
- 控制对象：表、视图、列等数据库对象。
- 控制内容：用户对对象能不能执行 `SELECT`、`INSERT`、`UPDATE`、`DELETE` 等操作。
- 通过 `GRANT` 授权。
- 通过 `REVOKE` 回收权限。
- `WITH GRANT OPTION` 表示被授权者还能继续转授权。
- `CASCADE` 表示级联回收。
- `RESTRICT` 表示存在转授权依赖时拒绝回收。
- 好背：DAC = “谁有权，谁授权；给权限用 GRANT，收权限用 REVOKE”。

授权模板：

```sql
GRANT 权限
ON 对象
TO 用户或角色
[WITH GRANT OPTION];
```

回收模板：

```sql
REVOKE 权限
ON 对象
FROM 用户或角色
[CASCADE | RESTRICT];
```

角色：

```sql
CREATE ROLE 角色名;
GRANT 权限 ON 对象 TO 角色名;
GRANT 角色名 TO 用户;
```

强制存取控制：

- 英文常写 MAC，核心是“按安全级别强制控制”，用户自己不能随便改。
- 主体：用户、进程等主动访问者，有许可证级别。
- 客体：数据、文件等被访问对象，有密级。
- 常见密级从高到低可理解为：绝密 > 机密 > 秘密 > 公开。
- 读规则：主体许可证级别大于等于客体密级才可读，即“向下读”。  
  例：机密级用户可以读秘密级数据；秘密级用户不能读机密级数据。
- 写规则：主体许可证级别小于等于客体密级才可写，即“向上写”。  
  例：秘密级用户可以向机密级对象写入，但不能向公开级对象写入，防止高密级信息向低密级泄露。
- 好背：MAC = “密级说了算”；读要够高，写不能往低处写。

DAC 和 MAC 对比：

| 项目 | 自主存取控制 DAC | 强制存取控制 MAC |
|---|---|---|
| 控制依据 | 用户/角色拥有的权限 | 主体许可证级别和客体密级 |
| 谁决定 | 授权者可自主授权、回收 | 系统安全策略强制决定 |
| 典型语句 | `GRANT`、`REVOKE` | 按密级规则判断 |
| 好背 | 给不给权限 | 高低密级能不能读写 |

视图：

- 视图是从基本表或其他视图导出的“虚表”，通常只保存定义，不保存实际数据。
- 安全作用：通过视图隐藏敏感列、敏感行，只把允许看的数据开放给用户。
- 例子：学生表有 `Student(学号, 姓名, 性别, 身份证号, 电话)`，普通老师只需要看学号、姓名、性别，就可以建一个不含身份证号和电话的视图再授权。
- 考试表达：视图可使用户只能访问被授权的局部数据，从而提供一定的安全保护。
- 易错：视图能增强逻辑独立性和安全性，但不是物理独立性；视图本身不是加密。

审计：

- 审计就是把用户对数据库的重要操作记录下来，形成审计日志。
- 常记录：谁在什么时间，对什么对象，执行了什么操作，操作是否成功。
- 作用：事后追踪、发现违规、追究责任，也能帮助分析安全事件。
- 例子：如果成绩表被改了，审计日志可以查出哪个用户在什么时间执行了 `UPDATE`。
- 易错：审计主要是“记录和追查”，不是直接阻止访问；审计会耗费时间和空间资源，所以通常作为可选安全手段。

数据加密：

- 加密是把明文数据变成密文，没有密钥就难以还原。
- 保护对象：数据库中存储的数据、备份数据、网络传输中的数据。
- 存储加密：防止磁盘、备份文件泄露后被直接读懂。
- 传输加密：防止数据在网络传输过程中被窃听。
- 易懂例子：原文“成绩=95”加密后变成看不懂的一串字符，只有有密钥的人才能解开。
- 考试表达：数据加密通过密码技术保护数据的机密性，是数据库安全性的重要补充手段。

六种安全手段速记：

| 手段 | 解决的问题 | 关键词 |
|---|---|---|
| 用户标识与鉴别 | 确认是谁 | 用户名、密码、认证 |
| 自主存取控制 | 能不能访问/操作 | `GRANT`、`REVOKE` |
| 强制存取控制 | 高低密级读写 | 主体、客体、密级 |
| 视图 | 隐藏不该看的数据 | 虚表、隐藏行列 |
| 审计 | 事后追查 | 日志、谁何时做了什么 |
| 数据加密 | 泄露也看不懂 | 明文、密文、密钥 |

判断题常考：

- 审计耗时耗资源，通常作为可选安全手段。
- 强制存取控制不是所有 DBMS 必须提供的安全手段。

### 1.7 规范化理论

函数依赖：

- `X -> Y`：x函数决定y。若两个元组在 X 上相等，则在 Y 上也相等。
- 平凡函数依赖：`Y` 是 `X` 的子集。
- 非平凡函数依赖：`Y` 不是 `X` 的子集。
- 完全函数依赖：`X -> Y` 成立，且 X 的任意真子集都不能推出 Y。
- 部分函数依赖：`X -> Y` 成立，但 X 的某个真子集也能推出 Y。
- 传递函数依赖：`X -> Y`、`Y -> Z`，且 `Y` 不能推出 `X`，则 Z 传递依赖于 X。

Armstrong 公理：

- 自反律：若 `Y ⊆ X`，则 `X -> Y`。
- 增广律：若 `X -> Y`，则 `XZ -> YZ`。例如`学号 -> 姓名`则`(学号, 课程号) -> (姓名, 课程号)`
- 传递律：若 `X -> Y`、`Y -> Z`，则 `X -> Z`。

常用推理规则：

- 合并规则：`X -> Y` 且 `X -> Z`，则 `X -> YZ`。
- 分解规则：`X -> YZ`，则 `X -> Y` 且 `X -> Z`。
- 伪传递规则：`X -> Y` 且 `WY -> Z`，则 `WX -> Z`。

属性闭包：

- 属性闭包记作 `X+`，意思是从属性集 `X` 出发，根据函数依赖 F 能推出的所有属性。
- 直观理解：先手里拿着 `X`，凡是遇到左边已经在手里的依赖 `A -> B`，就把右边 `B` 也拿进来，直到不能再增加。
- 若 `X+` 包含关系模式的全部属性 U，则 `X` 是超码。
- 若 `X+ = U`，且去掉 `X` 中任意一个属性后闭包都不能推出 U，则 `X` 是候选码。

闭包计算步骤：

1. 令 `X+ = X`。
2. 扫描函数依赖集 F：如果有 `A -> B`，且 `A ⊆ X+`，则把 `B` 加入 `X+`。
3. 反复扫描，直到 `X+` 不再变化。
4. 最后看 `X+` 是否包含全部属性，判断 `X` 能不能当码。

小例子：关系 `R(A,B,C,D)`，函数依赖 `A -> B`，`B -> C`，`AC -> D`。

- 求 `A+`：初始 `{A}`。
- 由 `A -> B` 得 `{A,B}`。
- 由 `B -> C` 得 `{A,B,C}`。
- 此时有 `A` 和 `C`，由 `AC -> D` 得 `{A,B,C,D}`。
- 所以 `A+ = ABCD`，`A` 能推出全部属性，是超码；且 `A` 不能再删属性，所以 `A` 是候选码。

候选码求法：

1. 把属性分成四类：LN必定
   - L 类：只出现在函数依赖左边，必在候选码中。
   - R 类：只出现在右边，通常不在候选码中。
   - N 类：左右都不出现，必在候选码中。
   - LR 类：左右都出现，待定。
2. 先算 `(L ∪ N)+`。
3. 如果闭包包含全部属性，则 `L ∪ N` 是候选码。
4. 否则依次加入 LR 类属性求闭包，找到能推出全部属性的最小属性集。

主属性和非主属性：

- 主属性：出现在任意候选码中的属性。
- 非主属性：不出现在任何候选码中的属性。

范式：

| 范式 | 理解版要求 | 做题严格判定 | 违反时表现 |
|---|---|---|---|
| 1NF | 属性不可再分 | 每个属性值都是原子值 | 有嵌套表、复合属性、多值属性未处理 |
| 2NF | 非主属性完全依赖于候选码 | 在 1NF 基础上，非主属性不能部分依赖于任何候选码 | 非主属性只依赖组合码的一部分 |
| 3NF | 非主属性不传递依赖于候选码 | 对每个非平凡函数依赖 `X -> A`，`X` 是超码，或 `A` 是主属性 | 非主属性对码有传递依赖；口诀：左边是码，或右边是主属性 |
| BCNF | 每个非平凡函数依赖的决定因素都是超码 | 对每个非平凡函数依赖 `X -> A`，`X` 必须是超码 | 存在非码决定因素；口诀：左边必须是码 |
| 4NF | BCNF 基础上消除非平凡多值依赖 | 对每个非平凡多值依赖 `X ->-> Y`，`X` 必须是超码 | 非平凡多值依赖左部不是超码；即一个 `X` 对应一组独立的 `Y` 值，且这些 `Y` 值能和其他属性自由组合 |

重要结论：

- 只有两个属性的关系模式，最高可达 BCNF。
- 极小函数依赖集可能有 1 个或多个，不一定唯一。
  - 也叫极小覆盖、最小依赖集，意思是用一组“最精简”的函数依赖，和原来的 F 能推出完全相同的依赖，即 `F+ = G+`。
  - “极小”不是指依赖条数一定全局最少，而是指已经不能再删：右部都是单属性，左部没有多余属性，依赖集中没有冗余依赖。
  - 不唯一的原因：化简时删哪个多余属性、删哪个冗余依赖，顺序不同，可能得到不同但等价的结果。
  - 小例子：`F={A->BC, B->AC, C->AB}`。可得到 `G1={A->B, B->C, C->A}`，也可得到 `G2={A->C, C->B, B->A}`；两者形式不同，但都能推出 A、B、C 之间相互决定，所以都是极小函数依赖集。
  - 做题时如果答案和标准答案长得不一样，先别急：只要满足“等价 + 极小”两个条件，就可能是正确答案。
- 所有属性都是主属性时，通常至少满足 3NF，但不一定满足 BCNF。（默认已满足 1NF；2NF、3NF主要检查非主属性问题，而 BCNF 要求每个非平凡依赖的决定因素都是超码。）
- 只要求无损连接分解，一定可分解到 BCNF。
- 要求无损连接且保持函数依赖，一定可分解到 3NF，不一定能到 BCNF。
- 2NF 不彻底：没有消除非主属性对码的传递依赖。
- 3NF 不彻底：没有消除主属性对码的部分依赖和传递依赖。
- BCNF 是针对主属性问题进一步提出的。

无损连接和保持函数依赖：

- 无损连接：分解后的关系再自然连接回来，结果必须正好是原关系，不能多出假元组，也不能丢信息。
  - 直观记法：无损看“数据能不能正确还原”。
  - 例：`R(A,B,C)` 分解为 `R1(A,B)`、`R2(B,C)`。如果同一个 `B` 能对应多个 `A` 和多个 `C`，连接回来可能产生原来没有的组合，这就是有损分解。
- 保持函数依赖：原关系上的约束，分解后仍能只检查各个小关系就保证，不必先把小关系连接回大关系再检查。
  - 直观记法：保依赖看“约束能不能局部检查”。
  - 例：`F={A->B, B->C}`，若分解为 `R1(A,B)`、`R2(B,C)`，则 `A->B` 可在 `R1` 检查，`B->C` 可在 `R2` 检查，所以保持依赖。
  - 若分解为 `R1(A,B)`、`R2(A,C)`，则 `B->C` 没有一个小关系同时包含 `B` 和 `C`，一般不能直接检查，所以可能不保持依赖。

为什么“BCNF 保无损，3NF 保无损且保依赖”：

- 只要求无损连接，一定可分解到 BCNF。
  - 若关系不满足 BCNF，一定存在非平凡依赖 `X -> Y`，且 `X` 不是超码。
  - 按 BCNF 模板分解：`R1 = X ∪ Y`，`R2 = R - (Y - X)`。
  - 两个子关系的公共属性是 `X`，而 `X -> Y`，所以公共属性能决定 `R1` 中除 `X` 外的属性，满足无损连接条件。
  - 不断这样分解，最终能到 BCNF；但某些原依赖可能被拆散，所以不保证保持函数依赖。
- 要求无损连接且保持函数依赖，一定可分解到 3NF，不一定能到 BCNF。
  - 3NF 分解通常从极小覆盖出发：对每个依赖 `X -> A` 建关系 `XA`，所以原依赖能在小关系中直接检查，天然利于保持依赖。
  - 如果这些小关系里没有任何一个包含候选码，就额外加一个候选码关系，用来保证无损连接。
  - 3NF 比 BCNF 宽：若 `X -> A` 中 `X` 不是超码，但 `A` 是主属性，3NF 仍允许；BCNF 不允许。
  - 因此有些模式若强行分解到 BCNF，就会丢失依赖保持，只能保证无损，不能同时保证保依赖。

经典反例：

- `R(A,B,C)`，`F={AB->C, C->B}`。
- 候选码是 `AB` 和 `AC`，所以 `A、B、C` 都是主属性。
- `C->B` 中 `C` 不是超码，但 `B` 是主属性，所以满足 3NF；又因为 `C` 不是超码，所以不满足 BCNF。
- 若按 `C->B` 分解到 BCNF：`R1(C,B)`、`R2(A,C)`，该分解无损。
- 但原依赖 `AB->C` 无法只在 `R1` 或 `R2` 中检查，需要连接后才能验证，所以不保持函数依赖。

二元分解无损连接判断：

设 `R` 分解为 `R1`、`R2`，若满足下面任一条件，则无损连接：

- `(R1 ∩ R2) -> (R1 - R2)`
- `(R1 ∩ R2) -> (R2 - R1)`

保持函数依赖判断：

- 看原来的每个函数依赖，是否能在分解后的某个关系或投影依赖集合中推出。
- 若某个依赖左右属性被拆到不同关系中，且无法推回，则不保持。

BCNF 分解模板：**谁不够格还想决定别人，就把它和它能决定的扔出去**

1. 求候选码。
2. 找违反 BCNF 的依赖 `X -> Y`，即 X 不是超码。
3. 分解为：一个表收留依赖，另一张表把被决定的多余的右边拿掉（就是右边新出现的属性）
   - `R1 = X ∪ Y`
   - `R2 = R - (Y - X)`
4. 对分解后的关系继续检查，直到每个关系都满足 BCNF。

真题模板：

`STC(Sno, Tname, Cname)`，依赖为：

- `Sno, Cname -> Tname`
- `Tname -> Cname`

L ： sno

LR ： cname，tname


候选码：

- `(Sno, Cname)`
- `(Sno, Tname)`

所以所有属性都是主属性，属于 3NF；但 `Tname -> Cname` 中 `Tname` 不是超码，所以不属于 BCNF。BCNF 分解为：

- `R1(Tname, Cname)`
- `R2(Sno, Tname)`

### 1.8 数据库设计阶段

六个阶段：

| 阶段 | 核心任务 | 标志性成果 |
|---|---|---|
| 需求分析 | 分析数据和处理需求 | 数据字典、数据流图 |
| 概念结构设计 | 抽象信息世界模型 | 分 E-R 图、全局 E-R 图 |
| 逻辑结构设计 | E-R 转关系、规范化、优化、外模式设计 | 关系模式 |
| 物理结构设计 | 存储结构、存取路径、索引 | 物理存储结构、索引 |
| 数据库实施 | 建库、装入数据、调试试运行 | 可运行数据库系统 |
| 运行与维护 | 日常管理、维护、调整 | 运行中的系统 |

速判：

- 数据字典、数据流图：需求分析。
- 分 E-R 图、全局 E-R 图：概念结构设计。
- 关系模式优化、规范化、外模式设计：逻辑结构设计。
- 索引设计、存储路径设计：物理结构设计。

### 1.9 事务与并发控制

事务：

- 事务是用户定义的一个数据库操作序列，是数据库系统中并发控制和恢复的基本单位。
- 事务一般经历活动、部分提交、提交、失败、中止等状态。

ACID：

| 特性 | 含义 |
|---|---|
| 原子性 | 事务中的操作要么全做，要么全不做 |
| 一致性 | 事务使数据库从一个一致状态变到另一个一致状态 |
| 隔离性 | 一个事务的操作对并发事务隔离 |
| 持久性/永久性 | 事务一旦提交，对数据库的改变永久有效 |

事务语句：

- `COMMIT`：提交事务。
- `ROLLBACK`：回滚事务。

锁：

- X 锁/排他锁/写锁：可读也可写；与任何锁互斥。
- S 锁/共享锁/读锁：只可读；多个 S 锁可相容，但与 X 锁互斥。

并发不一致问题：

- 丢失修改。
- 读脏数据。
- 不可重复读。
- “可重复读”不是问题本身。

三级封锁协议：

| 协议 | 规则 | 能解决 |
|---|---|---|
| 一级封锁协议 | 修改前加 X 锁，事务结束释放 | 丢失修改 |
| 二级封锁协议 | 一级 + 读前加 S 锁，读完释放 | 丢失修改、读脏数据 |
| 三级封锁协议 | 一级 + 读前加 S 锁，事务结束释放 | 丢失修改、读脏数据、不可重复读 |

死锁与活锁：

- 活锁：事务一直等不到服务。解决：先来先服务。
- 死锁：事务互相等待对方资源，谁也继续不了。
- 一次封锁法：事务开始时一次性申请所需的全部锁，拿不到就不执行。优点是不会边占有边等待，可预防死锁；缺点是降低并发度，且事务要预先知道全部要访问的数据。
- 顺序封锁法：给数据对象规定一个统一加锁顺序，所有事务都按这个顺序申请锁。这样不会形成循环等待，可预防死锁；缺点是维护统一顺序麻烦，也会影响并发。
- 检测和解除策略：允许死锁发生，系统定期检查等待图；若发现环路，说明有死锁，再选择代价较小的事务撤销/回滚，释放它占有的锁，让其他事务继续。

调度：

- 串行调度一定正确。
- 并发调度正确的标准：结果与某个串行调度结果相同，即可串行化。  
  “某个串行调度”不是只和 `T1 -> T2 -> T3` 比，而是和这些事务所有可能的串行顺序比。比如只有 `T1、T2` 时，要分别想 `T1 -> T2` 和 `T2 -> T1`；如果并发执行后的最终数据库状态和其中任意一个串行顺序一样，就叫可串行化。
- 考场常用判断法一：看最终结果。  
  适合题目给了具体读写计算过程。先按并发调度一步步算出最终数据值，再分别按所有可能的串行顺序算最终值；只要能对上一个，就是可串行化。  
  例：初值 `A=100`，`T1: A=A+10`，`T2: A=A*2`。  
  串行 `T1 -> T2` 得 `220`，串行 `T2 -> T1` 得 `210`。如果某个并发调度最终也得到 `220` 或 `210`，就和某个串行结果相同；如果得到别的值，通常就不可串行化。
- 考场常用判断法二：画冲突图/优先图。  
  适合题目只给 `r1(A)、w2(A)` 这种操作序列。找不同事务对同一数据项的冲突操作：`读-写`、`写-读`、`写-写` 冲突，`读-读` 不冲突。若冲突操作中 `Ti` 的操作在 `Tj` 前面，就画边 `Ti -> Tj`。最后看图：无环就是冲突可串行化；有环就不是冲突可串行化。
- 冲突可串行化一定是可串行化调度，是正确调度。  
  记法：冲突图无环 => 冲突可串行化 => 一定可串行化；但“可串行化”不一定都能用冲突图判出来，因为还存在视图可串行化但不是冲突可串行化的情况。考试一般问“判断冲突可串行化”就画图看有没有环。

  **冲突可串行化 ⊂ 视图可串行化 ⊂ 可串行化**

并发执行的好处：

- 提高系统吞吐量和资源利用率。
- 减少事务等待时间。

并发控制常见方法：

- 封锁法：事务访问数据前先加锁，用 S 锁控制读、X 锁控制写。锁不相容时后来的事务等待，靠“先锁住再操作”保证并发执行不互相破坏。常见考点是三级封锁协议、两段锁协议、死锁和活锁。
- 时间戳法：给每个事务一个时间戳，系统按时间戳规定事务读写顺序。若某个读写操作违反时间先后关系，就拒绝该操作或撤销事务重做。核心思想是“不等锁，按时间戳判断谁先谁后”。
- 乐观控制法：假设冲突少，事务执行时先不加锁，先自由读写到临时区域；提交前再检查是否和其他事务冲突。若无冲突就提交，若有冲突就回滚重做。适合读多写少、冲突较少的场景。

### 1.10 数据库恢复

故障类型：

| 故障 | 判断关键词 | 恢复思路 |
|---|---|---|
| 事务故障 | 某个事务未正常结束、逻辑错误、运算异常、死锁回滚 | UNDO 该事务 |
| 系统故障 | 系统突然停止、断电、所有事务非正常终止 | UNDO 未完成事务，REDO 已提交事务（可能还没写入磁盘） |
| 介质故障 | 磁盘损坏、硬件损坏、数据库被破坏 | 装入后备副本，再用日志 REDO |

恢复核心思想：

- 建立冗余数据，而不是杜绝故障。
- 冗余数据主要有日志文件、数据库后备副本、镜像盘。
- 缓冲区不是冗余数据，也不是恢复技术。

日志：

- 记录所有更新操作，不记录查询操作。
- 必须严格按并发事务执行的时间次序登记。
- 必须先记日志，后写数据库。

REDO/UNDO：

- REDO：重做，以新值代替旧值。
- UNDO：撤销，以旧值代替新值。

检查点：

- 目的：改善恢复效率。
- 不是为了解决某类故障，也不是增加安全性。

基于日志的更新恢复方式：

- 延迟更新技术：事务提交前不把更新写入数据库，故障后主要对已提交事务 REDO。
- 即时更新技术：事务提交前允许把更新写入数据库，故障后可能需要 UNDO 未提交事务，也需要 REDO 已提交事务。

转储分类：

- 按是否允许事务运行：
  - 静态转储：转储期间不允许其他事务运行，数据库处于一致状态，备份简单可靠，但会影响系统使用。
  - 动态转储：转储期间允许事务继续运行，不会长时间中断业务；但备份过程中数据可能被修改，必须配合日志文件才能保证恢复到一致状态。
- 按转储数据范围：
  - 海量转储：每次转储整个数据库，也叫完全转储；恢复时较方便，但备份时间长、占用空间大。
  - 增量转储：只转储上次转储以后发生变化的数据；备份快、占用空间少，但恢复时通常要结合最近一次海量转储和之后的增量转储。

恢复技术：

- 基于日志和后备副本的恢复：最常见的恢复方法。后备副本用于把数据库恢复到某个备份时刻，日志用于继续 REDO 已提交事务、UNDO 未提交事务，使数据库恢复到一致状态。
- 影子分页技术：为数据库页保留当前页表和影子页表，事务修改时写到新页，不直接覆盖旧页；提交时切换页表，故障时可利用影子页表快速恢复。
- 数据库镜像：在另一磁盘或另一地点维护数据库的副本，主数据库发生故障时可切换到镜像数据库，提高可靠性和可用性，但开销较大。
- 不包括缓冲区技术：缓冲区只是内存中的临时数据区，用来提高读写效率，不属于冗余数据，也不是数据库恢复技术。

### 1.11 查询处理与优化

课件单独列了查询优化模块，仓库没有单独测试题，但考前要背关键词。

查询处理过程：

- 语法分析与翻译。
- 查询优化。
- 查询执行。

选择运算实现：

- 全表扫描。
- 使用索引。

连接运算实现：

- 嵌套循环连接。
- 块嵌套循环连接。
- 索引嵌套循环连接。
- 排序-归并连接。
- 散列连接。

查询优化：

- 代数优化：基于等价变换规则改写查询表达式。
- 物理优化：选择具体执行算法和存取路径。
- 语法树优化：把查询表示成语法树，再按启发式规则下推选择、投影并调整连接顺序。

启发式规则：

- 选择运算尽量先做。
- 投影运算尽量先做，只保留后续需要的列。
- 把笛卡儿积和选择合并成连接。
- 先做能显著缩小中间结果的操作。

## 2. 常见题型做题方法

### 2.1 单选题速判

看到题干关键词，直接对照：

| 关键词 | 选什么 |
|---|---|
| 静态特性、对象联系 | 数据结构 |
| 动态特性、允许操作 | 数据操作 |
| 正确、有效、相容 | 完整性约束 |
| 全体数据逻辑结构 | 模式 |
| 用户数据逻辑结构 | 外模式 |
| 数据物理结构 | 内模式 |
| 逻辑独立性 | 外模式-模式映像 |
| 物理独立性 | 模式-内模式映像 |
| 可以有多个 | 外模式 |
| 实体图形 | 矩形 |
| 联系图形 | 菱形 |
| 属性图形 | 椭圆 |
| 多值属性 | 双椭圆 |
| 派生属性 | 虚椭圆 |
| 主码取值限制 | 实体完整性 |
| 外码取值限制 | 参照完整性 |
| 主码能否空 | 不能 |
| 外码能否空 | 可以，除非另有约束 |
| 行运算 | 选择 |
| 列运算且去重 | 投影 |
| 同名属性才能做 | 自然连接 |
| 笛卡儿积逆运算 | 除法：若 `R ÷ S = T`，意思是 `T × S` 能被包含在 `R` 中；所以它是从乘积结果 `R` 和其中一边 `S`，反推出另一边 `T` |
| 事务全做或全不做 | 原子性 |
| 提交后永久 | 持久性 |
| 并发事务互不干扰 | 隔离性 |
| 一致状态到一致状态 | 一致性 |
| 记录每次更新 | 日志文件 |
| 改善恢复效率 | 检查点 |
| 新值代替旧值 | REDO |
| 旧值代替新值 | UNDO |

### 2.2 简答题模板

#### 简述保护数据库安全性的措施

可答：

数据库安全性措施包括用户标识与鉴别、存取控制、视图、审计、数据加密等。用户标识与鉴别用于确认用户身份；存取控制限制用户能访问哪些数据、执行哪些操作；视图可隐藏敏感数据；审计记录用户操作；加密保护存储和传输中的数据。

#### 什么是派生属性？不同设计阶段如何处理？

可答：

派生属性是可以由其他属性或实体计算得到的属性，也称计算属性，如年龄可由出生日期计算得到。在总体 E-R 图设计或模式设计时，一般去掉派生属性，避免冗余；在外模式设计时，可根据用户查询和显示需要再加入。

#### 简述关系的完整性规则

可答：

关系完整性规则包括实体完整性、参照完整性和用户自定义完整性。实体完整性要求主码唯一且非空；参照完整性要求外码要么为空，要么引用被参照关系中已有主码值；用户自定义完整性由具体应用语义决定。

#### 简述数据库设计阶段及成果

可答：

数据库设计通常包括需求分析、概念结构设计、逻辑结构设计、物理结构设计、数据库实施、运行与维护。需求分析成果是数据字典和数据流图；概念结构设计成果是全局 E-R 图；逻辑结构设计成果是关系模式和外模式；物理结构设计成果是存储结构和索引；实施阶段建立数据库、装入数据并调试；运行维护阶段进行日常管理和调整。

#### 如何看待数据库中的冗余现象

可答：

冗余有消极作用，也有积极作用。消极方面，冗余会导致修改异常、插入异常和删除异常，需要通过规范化和模式分解减少不良冗余。积极方面，数据库恢复需要日志、后备副本、镜像等冗余数据，以便故障后恢复。因此冗余既可能破坏数据一致性，也能提高恢复能力，关键是控制合理冗余。

#### 什么是并发调度？并发执行有什么好处？

可答：

并发调度是指一组事务中至少有两个事务已经开始执行且尚未结束，它们的操作交叉进行。并发执行可以提高系统吞吐量和资源利用率，也可以减少事务等待时间，但必须通过并发控制避免数据不一致。

### 2.3 E-R 设计题方法

步骤：

1. 读题圈名词：找实体，如客户、汽车、事故、支行、账户、贷款、学生、班级、学会。
2. 找“登记/记录/编号/ID”：这些通常提示实体和属性。
3. 圈动词：找联系，如拥有、发生、办理、属于、参加、工作于。
4. 看数量词：确定 1:1、1:N、M:N，如“每个”“多个”“最多一个”“0 次或多次”。
5. 标主码：题目说“编号、ID、唯一名称”通常是码。
6. 处理特殊属性：多值属性双椭圆，派生属性虚椭圆，复合属性拆开。
7. 检查题目每一句是否都在图里体现。

常见场景：

- “学生参加多个学会，每个学会有多个学生，参加有入会年份”：学生-学会是 M:N 联系，联系属性是入会年份。
- “客户有多个账户，一个账户属于一个客户”：客户-账户是 1:N。
- “每笔贷款只能贷给一个客户，一位客户可多次贷款”：客户-贷款是 1:N。
- “每个部门有若干职工”：部门-职工是 1:N。
- “职工做过项目，有起止年月和工资”：职工-项目是 M:N 联系，联系上带属性。

### 2.4 E-R 转关系题方法

写关系模式时按下面顺序：

1. 每个强实体先写一个表。
2. 1:N 联系把 1 端主码加到 N 端。
3. M:N 联系单独建表，把两端主码作为联合主码。
4. 联系有属性就放进联系表；如果 1:N 合并到 N 端，就放进 N 端表。
5. 多值属性单独建表。
6. 弱实体表要加属主实体主码。
7. 如果几个关系模式主码相同且语义上是一一对应，可以合并；若题目要求“说明理由”，写清楚合并是为了减少连接和冗余，且不丢失语义。
8. 最后标明主码和外码。

模板：

```text
实体A(Aid, A属性...)
实体B(Bid, B属性..., Aid)          -- 1:N 时 Aid 放入 N 端
联系AB(Aid, Bid, 联系属性...)      -- M:N 时单独建表
```

批发商店作业的答题点：

- 弱实体 `Dependents` 要带上职工主码，主码通常为 `(Eno, Dname)`。
- `Works_in` 这种职工-部门的 N:1 联系可合并到 `Employees`，在 `Employees` 中加入 `Dno` 外码。
- `Manages` 若是部门和职工的一对一联系，可在 `Departments` 中加入 `ManagerEno` 外码。
- `Carries`、`SuppliesPrice`、`Includes` 这类 M:N 或带属性联系单独建表。

汽车保险题答题点：

- 实体通常有客户/车主、汽车、事故。
- 客户-汽车一般是 1:N，汽车表可加入车主编号外码。
- 若事故可能涉及多辆车，汽车-事故应建“参与/发生”联系表，主码可为 `(车辆编号, 事故编号)`，损坏估计放在联系表中。
- SQL 题里的 `Participated(Driver-id, Car-no, Report-no, Damage-amount)` 就是典型联系表。

真题“院-专业-班级-学生-学会”关系模式模板：

```text
宿舍区(宿舍区编号, 宿舍区名)
院(院号, 院名, 联系电话, 宿舍区编号)
专业(专业号, 专业名, 院号)
班级(班级编号, 班级号, 专业号)
学生(学号, 姓名, 出生年月, 入学年份, 班级编号)
学会(学会编号, 学会名称, 成立年份)
参加(学号, 学会编号, 入会年份)
```

其中“学生参加学会”是 M:N，`入会年份` 是联系属性。

### 2.5 关系代数题方法

通用五步：

1. 看最后要输出什么属性：最后一定用投影 `π`。
2. 看条件在哪张表：先选择 `σ`。
3. 涉及多个表：用连接 `⋈`。
4. 出现“没有、不、未”：用差 `-`。
5. 出现“所有、全部、至少包含全部”：用除法 `÷`。
6. 如果有启发策略和代数优化后则先选择、先投影、后连接

常用模板：

筛选：

```text
σ 条件(关系)
```

投影：

```text
π 属性列表(关系)
```

连接后查询：

```text
π 输出属性(σ 条件(R ⋈ S ⋈ T))
```

“没有/不提供”：

```text
全集 - 满足条件的集合
```

例如“不提供 P2 的供应商名称”：

```text
πSname((πSno(Suppliers) - πSno(σPno='P2'(SPJ))) ⋈ Suppliers)
```

“所有/全部”：

```text
πA,B(关系) ÷ πB(要求全集)
```

例如“提供所有零件的供应商名称”：

```text
πSname(Suppliers ⋈ (πSno,Pno(SPJ) ÷ πPno(Parts)))
```

“本地”：

```text
σ 供应商城市 = 工程城市(Suppliers ⋈ SPJ ⋈ Projects)
```

聚集扩展关系代数：

```text
γ 聚集函数(属性)->结果名(关系)
γ 分组属性; 聚集函数(属性)->结果名(关系)
```

例如“S1 提供每种零件的总数量”：

```text
γ Pno; SUM(Quantity)->TotalQuantity(σSno='S1'(SPJ))
```

### 2.6 SQL 语句题方法

SQL 查询通用思路：

1. `SELECT` 写最终要显示的列。
2. `FROM` 写涉及的表。
3. `WHERE` 写连接条件和筛选条件。
4. 有“每个/按/分别统计”就 `GROUP BY`。
5. 对分组后的结果筛选用 `HAVING`。
6. “所有/全部”优先想双重 `NOT EXISTS`。
7. “没有/不”优先想 `NOT EXISTS` 或 `EXCEPT`。

建表模板：

```sql
CREATE TABLE 表名 (
    列名 数据类型 约束,
    列名 数据类型 约束,
    PRIMARY KEY (主码列),
    FOREIGN KEY (外码列) REFERENCES 被参照表(主码列),
    CHECK (条件)
);
```

建表题检查清单：

- 编号类属性常用 `CHAR(n)` 或 `VARCHAR(n)`。
- 名称、城市、地址等常用 `VARCHAR(n)`。
- 数量、状态、工资等用 `INT`、`SMALLINT` 或 `DECIMAL`。
- 主码写 `PRIMARY KEY`，联合主码写 `PRIMARY KEY (A, B, C)`。
- 外码写 `FOREIGN KEY (...) REFERENCES ...(...)`。
- 题目要求合理假设时，可加 `NOT NULL`、`DEFAULT`、`CHECK`。
- 供应商-工程-零件 `SPJ` 表常用联合主码 `(Sno, Pno, Jno)`，三个属性同时也是外码。

连接查询模板：

```sql
SELECT 输出列
FROM A, B, C
WHERE A.主码 = B.外码
  AND C.主码 = B.外码
  AND 普通条件;
```

也可以写成：

```sql
SELECT 输出列
FROM A
JOIN B ON A.主码 = B.外码
JOIN C ON C.主码 = B.外码
WHERE 普通条件;
```

聚集分组模板：

```sql
SELECT 分组列, 聚集函数(列) AS 结果名
FROM 表
WHERE 分组前条件
GROUP BY 分组列
HAVING 分组后条件;
```

“所有/全部”双重 `NOT EXISTS` 模板：

```sql
SELECT ...
FROM 外层对象 x
WHERE NOT EXISTS (
    SELECT *
    FROM 要求全集 y
    WHERE NOT EXISTS (
        SELECT *
        FROM 对应关系 r
        WHERE r.x_id = x.x_id
          AND r.y_id = y.y_id
    )
);
```

这类题的本质是：

> 找出这样的 `x`：对要求全集里的每一个 `y`，`x` 都和它有对应关系。

也就是：

```text
所有 y 都满足
= 不存在一个 y 不满足
= NOT EXISTS (y WHERE NOT EXISTS (...))
```

读法一定要从里往外读：

1. 内层 `NOT EXISTS`：判断“当前这个 `x` 没有对应当前这个 `y`”。
2. 中间层 `FROM 要求全集 y`：把所有必须满足的 `y` 都检查一遍。
3. 外层 `NOT EXISTS`：表示“不存在一个没满足的 `y`”。
4. 合起来就是：`x` 满足了全部 `y`。

口诀：

> “所有”不好直接写，就写“没有一个不满足”。  
> 内层找“缺的”，外层否定“有缺的”，所以结果就是“全都有”。

例：查询选修了所有课程的学生。

```sql
SELECT s.sno, s.sname
FROM Student s
WHERE NOT EXISTS (
    SELECT *
    FROM Course c
    WHERE NOT EXISTS (
        SELECT *
        FROM SC sc
        WHERE sc.sno = s.sno
          AND sc.cno = c.cno
    )
);
```

对应关系：

| 模板成分 | 本题中对应 |
|---|---|
| 外层对象 `x` | 学生 `Student s` |
| 要求全集 `y` | 所有课程 `Course c` |
| 对应关系 `r` | 选课表 `SC sc` |
| `r.x_id = x.x_id` | `sc.sno = s.sno` |
| `r.y_id = y.y_id` | `sc.cno = c.cno` |

逐层翻译：

- 内层：有没有 `sc` 记录，证明学生 `s` 选了课程 `c`。
- 内层 `NOT EXISTS`：没有这样的选课记录，即学生 `s` 没选课程 `c`。
- 中间层：寻找“学生 `s` 没选的课程”。
- 外层 `NOT EXISTS`：不存在没选的课程，所以学生 `s` 选了所有课程。

再例：查询购买了所有商品的顾客。

```sql
SELECT cu.*
FROM Customer cu
WHERE NOT EXISTS (
    SELECT *
    FROM Product p
    WHERE NOT EXISTS (
        SELECT *
        FROM Buy b
        WHERE b.customer_id = cu.customer_id
          AND b.product_id = p.product_id
    )
);
```

考试套模板步骤：

1. 先找要输出谁，就是外层对象 `x`。
2. 再找“所有/全部/每一个”的对象，就是要求全集 `y`。
3. 最后找连接二者的表，就是对应关系 `r`。
4. 内层只写两类条件：`r` 连 `x`，`r` 连 `y`。有额外限制时再补普通条件。

含外键删除模板：

```sql
DELETE FROM 子表
WHERE 外码 IN (
    SELECT 主码
    FROM 主表
    WHERE 条件
);

DELETE FROM 主表
WHERE 条件;
```

原因：先删引用表/子表，再删被引用表/主表，避免违反参照完整性。

汽车保险 SQL 高频模板：

统计某年出过事故的总人数：

```sql
SELECT COUNT(DISTINCT O.Driver_id) AS total_people
FROM Owns O
JOIN Participated P ON O.Car_no = P.Car_no
JOIN Accidents A ON P.Report_no = A.Report_no
WHERE A.Date >= '2016-01-01'
  AND A.Date < '2017-01-01';
```

这里 `DISTINCT` 表示去重，`COUNT(DISTINCT O.Driver_id)` 就是先把重复司机编号去掉，再统计不同司机人数；避免一个司机有多辆车或多次事故时被重复计算。

查询某人相关事故数量：

```sql
SELECT COUNT(DISTINCT P.Report_no) AS accident_count
FROM Clients C
JOIN Owns O ON C.Driver_id = O.Driver_id
JOIN Participated P ON O.Car_no = P.Car_no
WHERE C.Cname = '王明';
```

删除某人的某品牌车时，`T` 不是原数据库里的表名，而是临时记号，表示“目标车号集合”。考试写完整 SQL 时，要先把 `T` 定义清楚，再按 `T` 先删联系表、再删拥有关系、最后删车：

```sql
-- 先定义 T：李莉拥有的 Mazda 车号集合
CREATE TEMPORARY TABLE T AS
SELECT O.Car_no
FROM Clients C
JOIN Owns O ON C.Driver_id = O.Driver_id
JOIN Cars Ca ON O.Car_no = Ca.Car_no
WHERE C.Cname = '李莉'
  AND Ca.Model = 'Mazda';

DELETE FROM Participated
WHERE Car_no IN (SELECT Car_no FROM T);

DELETE FROM Owns
WHERE Car_no IN (SELECT Car_no FROM T);

DELETE FROM Cars
WHERE Car_no IN (SELECT Car_no FROM T);
```

注意：不能先删 `Owns` 后再用 `Owns` 去查目标车号，所以要先把 `T` 保存下来。

新事故要插入哪些表：

- `Accidents`：插入事故编号、日期、地点。
- `Participated`：每辆参与事故的车插入一条参与记录，包括车主、车号、事故编号、损坏估计。
- 通常不需要插入 `Clients`、`Cars`、`Owns`，除非题目说有新客户、新车或新拥有关系。

授权模板：

```sql
GRANT SELECT, INSERT, UPDATE, DELETE
ON 表名
TO 用户
WITH GRANT OPTION;
```

列级授权：

```sql
GRANT UPDATE(Salary)
ON Employee
TO LiYong;
```

对所有用户授权：

```sql
GRANT SELECT
ON Department
TO PUBLIC;
```

用视图隐藏敏感列再授权：

```sql
CREATE VIEW Emp_public AS
SELECT Ename, Title, Dno
FROM Employee;

GRANT SELECT
ON Emp_public
TO PUBLIC;
```

授权聚集统计视图：

```sql
CREATE VIEW Dept_salary_stat AS
SELECT Dno,
       MIN(Salary) AS MinSalary,
       MAX(Salary) AS MaxSalary,
       AVG(Salary) AS AvgSalary
FROM Employee
GROUP BY Dno;

GRANT SELECT
ON Dept_salary_stat
TO ShangHua;
```

断言模板：

```sql
CREATE ASSERTION 断言名
CHECK (
    NOT EXISTS (
        SELECT *
        FROM 表
        WHERE 违反约束的条件
    )
);
```

触发器模板：

```sql
CREATE TRIGGER 触发器名
AFTER INSERT OR UPDATE OR DELETE ON 表名
FOR EACH ROW
BEGIN
    -- 自动执行的维护语句
END;
```

物化视图同步触发器答题口径：

- `INSERT` 触发器：用 `NEW.属性` 把新增行对应的数据插入物化表。
- `UPDATE` 触发器：用 `NEW.属性` 更新物化表中的对应行。
- `DELETE` 触发器：用 `OLD.属性` 删除物化表中的对应行。

```sql
CREATE TRIGGER insert_IEGrades
AFTER INSERT ON SC
FOR EACH ROW
BEGIN
    INSERT INTO IEGrades(Sno, Sname, Cname, Grade)
    SELECT S.Sno, S.Sname, C.Cname, NEW.Grade
    FROM Student S, Course C
    WHERE S.Sno = NEW.Sno
      AND C.Cno = NEW.Cno;
END;
```

### 2.7 规范化分析题方法

题目给关系模式和函数依赖时，按下面步骤写：

1. 写出函数依赖集 F。
2. 用闭包法求候选码。
3. 写主属性和非主属性。
4. 判断 2NF：看非主属性是否部分依赖于候选码。
5. 判断 3NF：看非主属性是否传递依赖于候选码。
6. 判断 BCNF：看每个非平凡函数依赖左部是否都是超码。
7. 若要分解到 BCNF，找左部不是超码的依赖，按 `X -> Y` 分解。

闭包写法：

```text
X+ 初始为 X
若 F 中有 A -> B 且 A ⊆ X+，则把 B 加入 X+
重复直到不能增加
若 X+ = U，则 X 是超码；若 X 最小，则 X 是候选码
```

判断逻辑蕴涵：

- 逻辑蕴涵的意思：在依赖集 `F` 已经成立的前提下，某个函数依赖 `X -> Y` 是否一定也成立。
- 简单说：`F ⊨ X -> Y` 就是“只要满足 `F` 的关系，一定也满足 `X -> Y`”。不是看 `X -> Y` 有没有直接写在 `F` 里，而是看它能不能被 `F` 推出来。
- 例：`F={A->B, B->C}`，虽然没有直接写 `A->C`，但 `A` 能推出 `B`，`B` 又能推出 `C`，所以 `F` 逻辑蕴涵 `A->C`。
- 要判断 `F` 是否蕴涵 `X -> Y`，只需要求 `X+`。
- 如果 `Y ⊆ X+`，则 `F ⊨ X -> Y`。
- 如果 `Y` 中有任一属性不在 `X+` 中，则不能蕴涵。

作业样例：

- `F={AB->C, C->A, BC->D, ACD->B, D->EG, BE->C, CG->BD, CE->AG}`，求 `BD+`：
  `BD -> BDEG -> BCDEG -> ABCDEG`，所以 `BD+={A,B,C,D,E,G}`。
- `F={A->B, BC->DE, AEH->G}`，求 `AC+`：
  `AC -> ABC -> ABCDE`，没有 `H` 推不出 `G`，所以 `AC+={A,B,C,D,E}`；`F` 蕴涵 `AC->D`，但不蕴涵 `AC->G`，因此不蕴涵 `AC->DG`。

极小覆盖/最小依赖集求法：

1. 右部单属性化：把 `X -> YZ` 拆成 `X -> Y`、`X -> Z`。
2. 去掉左部多余属性：对 `XA -> B`，若在当前依赖集下 `X+` 能推出 `B`，则 `A` 多余。
3. 去掉冗余依赖：临时删除某个依赖，若剩余依赖仍能推出它，则该依赖冗余。
4. 合并同左部依赖：如 `X -> Y`、`X -> Z` 可写成 `X -> YZ`，但考试中写单属性右部更稳。

极小覆盖易错：

- 极小覆盖不唯一。
- 每一步删属性/删依赖后都要重新检查。
- `F={A->BC, B->AC, C->AB}` 这类互相决定的依赖，极小覆盖可以有多个，例如 `A->B, B->C, C->A` 和 `A->C, C->B, B->A` 都能推出全部属性间的相互依赖。

一般分解的无损连接判定表法：

1. 列出属性 `A1...An`，每个子模式 `Ri` 建一行。
2. 若 `Aj` 属于 `Ri`，表格填 `aj`；否则填 `bij`。
3. 对每个函数依赖 `X -> Y` 反复应用：若两行在 `X` 上符号相同，则令它们在 `Y` 上也相同；有 `aj` 优先统一成 `aj`。
4. 若某一行最后全是 `a1...an`，则分解具有无损连接性；否则不具有。

一般分解判定表法例题：

设 `R(A,B,C,D)`，函数依赖集 `F={A -> B, B -> C, C -> D}`，分解为：

- `R1(A,B)`
- `R2(B,C)`
- `R3(C,D)`

第一步，列属性 `A,B,C,D`，每个子模式一行。属于该子模式的属性填 `a`，不属于的填 `b`：

| 子模式 | A | B | C | D |
|---|---|---|---|---|
| R1(A,B) | a1 | a2 | b13 | b14 |
| R2(B,C) | b21 | a2 | a3 | b24 |
| R3(C,D) | b31 | b32 | a3 | a4 |

第二步，反复用函数依赖统一符号：

- 用 `A -> B`：要找两行在 `A` 列符号相同的行。现在 `A` 列分别是 `a1,b21,b31`，没有两行相同，所以暂时不能改。
- 用 `B -> C`：`R1` 和 `R2` 在 `B` 列都是 `a2`，所以它们在 `C` 列必须相同。`R2` 的 `C` 是 `a3`，有 `a3` 优先，因此把 `R1` 的 `C` 从 `b13` 改成 `a3`。

得到：

| 子模式 | A | B | C | D |
|---|---|---|---|---|
| R1(A,B) | a1 | a2 | a3 | b14 |
| R2(B,C) | b21 | a2 | a3 | b24 |
| R3(C,D) | b31 | b32 | a3 | a4 |

- 用 `C -> D`：现在三行在 `C` 列都是 `a3`，所以三行在 `D` 列必须相同。`R3` 的 `D` 是 `a4`，有 `a4` 优先，因此把 `R1`、`R2` 的 `D` 都改成 `a4`。

得到：

| 子模式 | A | B | C | D |
|---|---|---|---|---|
| R1(A,B) | a1 | a2 | a3 | a4 |
| R2(B,C) | b21 | a2 | a3 | a4 |
| R3(C,D) | b31 | b32 | a3 | a4 |

第三步，看是否出现某一行全是 `a1,a2,a3,a4`：

- `R1` 这一行已经是 `a1,a2,a3,a4`。
- 所以该分解 `{R1(A,B), R2(B,C), R3(C,D)}` 是无损连接分解。

这个例子说明：一般分解里，信息可能是一步步传过去的。这里先由 `B -> C` 把 `R1` 的 `C` 改成 `a3`，再由 `C -> D` 把 `R1` 的 `D` 改成 `a4`，最后才出现全 `a` 行。所以一般分解要用表法反复推，不能只看某一次交集。

二元分解可以用“交集推出差集”的快捷判定：

设 `R` 只分解成两个子模式 `R1`、`R2`，则无损连接当且仅当满足下面任一条件：

- `(R1 ∩ R2) -> (R1 - R2)`
- `(R1 ∩ R2) -> (R2 - R1)`

意思是：两个表的公共属性如果能决定其中一边的非公共属性，那么自然连接时就不会乱配出假元组。

例：`R(A,B,C)` 分解为 `R1(A,B)`、`R2(B,C)`。

- 公共属性：`R1 ∩ R2 = B`
- 左边差集：`R1 - R2 = A`
- 右边差集：`R2 - R1 = C`
- 若有 `B -> A` 或 `B -> C`，则该二元分解无损。
- 若既没有 `B -> A`，也没有 `B -> C`，一般不能判为无损。

但是一般分解不能直接套这个快捷法：

- “交集推出差集”只适用于一次分成两个关系的情况。
- 若分解为 `R1, R2, R3, ...`，多个关系自然连接时，属性可能经过多轮依赖传播，不能只看某两个表的交集。
- 所以多于两个子模式的一般分解，标准做法是用上面的判定表法。

可以这样记：

- 二元分解：优先用 `(R1 ∩ R2) -> (R1 - R2)` 或 `(R1 ∩ R2) -> (R2 - R1)`。
- 一般分解：用判定表法。
- 逐步二元分解：如果题目明确是先分一次、再对子关系继续分，每一步都可以按二元分解判断；每一步无损，则整体无损。

小心区分：

- `R` 一次分成 `{R1, R2, R3}`：这是一般分解，不能只凭某个交集推出某个差集下结论。
- `R` 先分成 `{R1, R'}`，再把 `R'` 分成 `{R2, R3}`：这是逐步二元分解，可以每一步用交集推出差集。

范式判断短语：

- “非主属性对码有部分依赖，所以只满足 1NF。”
- “不存在部分依赖，但存在传递依赖，所以最高满足 2NF。”
- “无非主属性，或非主属性无部分/传递依赖，满足 3NF。”
- “存在决定因素不是超码的依赖，所以不满足 BCNF。”

## 3. 通用思路与易错点

### 3.1 大题先判断题型

- 要画图：先实体、再联系、再属性、再基数。
- 要关系模式：先实体表，再联系表，最后主外码。
- 要 SQL：先输出列，再表，再连接，再条件，再分组。
- 要关系代数：先投影目标，再选择条件，再连接；“没有”用差，“所有”用除。
- 要规范化：先求候选码，不求码不要急着判范式。

### 3.2 关系代数与 SQL 的互译

| 关系代数 | SQL |
|---|---|
| `σ 条件` | `WHERE 条件` |
| `π 属性` | `SELECT 属性` |
| `⋈` | `JOIN ... ON ...` 或 `WHERE A.id=B.id` |
| `-` | `EXCEPT` 或 `NOT EXISTS` |
| `÷` | 双重 `NOT EXISTS` |
| `γ` | `GROUP BY` + 聚集函数 |

### 3.3 最高频易错点

- 外模式可以有多个；模式和内模式通常各一个。
- 逻辑独立性靠外模式-模式映像；物理独立性靠模式-内模式映像。
- `SELECT` 和 `FROM` 必不可少，`WHERE` 可选。
- `UNIQUE` 对应候选码；`PRIMARY KEY` 对应主码。
- 基于聚集函数的视图不能更新。
- 视图提供逻辑独立性，不是物理独立性。
- 主码不能空；外码可以空。
- 自然连接要求同名属性。
- 除法看“所有/全部/至少全部”。
- 外码约束属于数据库约束，不是关系约束。
- 触发器属于完整性机制，不是安全性控制手段。
- 安全性是防非法访问，完整性是保证数据正确。
- 一级封锁只能防丢失修改。
- 二级封锁不能保证可重复读。
- 三级封锁可以解决丢失修改、读脏数据、不可重复读。
- 活锁用先来先服务；死锁预防用一次封锁法、顺序封锁法，发生后用等待图检测，撤销/回滚事务解除。
- 恢复的思想是建立冗余数据，不是杜绝故障。
- 日志记录更新，不记录查询。
- 缓冲区不是冗余数据，也不是恢复技术。
- 检查点目的是改善恢复效率。
- 全属性都是主属性时通常能到 3NF，但不一定到 BCNF。
- 无损连接和保持函数依赖是两个不同性质，互不保证。

## 4. 考前 30 分钟速背清单

1. 数据模型三要素：数据结构、数据操作、完整性约束。
2. 三级模式：外模式、模式、内模式；两个映像对应逻辑/物理独立性。
3. E-R 符号：矩形实体、菱形联系、椭圆属性、双椭圆多值、虚椭圆派生、双矩形弱实体。
4. E-R 转关系：1:N 外码放 N 端；M:N 单独建表；多值属性单独建表；复合属性展平；派生属性通常去掉。
5. 关系代数：选择筛行、投影取列、自然连接按同名属性、差表“没有”、除法表“所有”。
6. SQL：`SELECT-FROM-WHERE-GROUP BY-HAVING-ORDER BY`。
7. SQL 关键字：`CREATE TABLE/VIEW/INDEX`、`ALTER TABLE`、`DROP`、`PRIMARY KEY`、`FOREIGN KEY`、`UNIQUE`、`GRANT`、`REVOKE`。
8. 完整性：实体完整性拒绝；参照完整性可拒绝、级联、置空、置默认。
9. 安全性：用户标识、存取控制、视图、审计、加密；`GRANT/REVOKE`。
10. 候选码：L/N 必在码，R 通常不在码，LR 待定，算闭包。
11. 范式：2NF 消部分依赖，3NF 消非主属性传递依赖，BCNF 要每个决定因素都是超码。
12. 设计阶段：需求分析出数据字典和数据流图；概念设计出全局 E-R 图；逻辑设计出关系模式；物理设计做索引和存储路径。
13. ACID：原子性全做全不做；一致性状态正确转换；隔离性互不干扰；持久性提交后永久。
14. 锁：X 锁可读可写；S 锁只读。
15. 恢复：事务故障 UNDO；系统故障 UNDO 未完成 + REDO 已提交；介质故障后备副本 + 日志。
16. 查询优化：选择/投影尽早下推，连接代替笛卡儿积，索引和连接算法属于物理优化。
17. 极小覆盖：右部单属性化、删左部多余属性、删冗余依赖；极小覆盖不唯一。
18. 物化视图：真实保存查询结果；基本表更新后用触发器同步维护。
19. 日志恢复：延迟更新偏 REDO；即时更新可能既要 UNDO 又要 REDO。
