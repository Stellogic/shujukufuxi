# 模式分解

## 定义

讲一个关系模式R(U,F)分解成若干较小的子关系模式R1(U1,F1)...

满足

* U₁ ∪ U₂ ∪ ... ∪ Uₖ = U（所有属性的并集等于原属性集——不丢属性）
* 每个 F_i 是 F 在 U_i 上的投影（子模式上的函数依赖集）

## 性质

### 无损连接性 ：单独满足这个性质，最多达到BCNF

分解后子关系做自然连接，能精确还原原始关系，不丢失元组

自然连接：等值连接的特里，在两个关系找同名属性的值相等做连接然后去重的列

连接：先做笛卡尔积，再根据各种条件筛选

二元分解的无损判定定理：

将 R 分解为 R₁ 和 R₂，当且仅当以下两个函数依赖至少有一个成立时，该分解是无损的：


(R₁ ∩ R₂) → (R₁ - R₂)    -- 交集中属性函数决定R₁的独有属性
  或
(R₁ ∩ R₂) → (R₂ - R₁)    -- 交集中属性函数决定R₂的独有属性

**公共属性必须是其中一个子模式的超键**

### 函数依赖保持性 ： 单独满足这个，最多3NF

定义： 各子模式上函数依赖的并集，逻辑上能推出原模式上的所有函数依赖。

(F₁ ∪ F₂ ∪ ... ∪ Fₖ)⁺ = F⁺

## 分解算法

