# 第2章 实体-联系模型 — 题目讲解

---

## 一、题目分析

本章16道单选题围绕**ER模型的基本概念**展开，包括实体、属性、联系三大核心概念及其分类。题目都是概念辨析类，考察对术语定义的精确理解。

> **题目类型：** 概念辨析型选择题
> **考点：**
> - ER模型三要素：实体、属性、联系
> - 属性的分类：复合属性、多值属性、派生属性
> - 联系的分类（按基数约束）：一对一、一对多、多对一、多对多
> - ER图的图形表示（矩形=实体、菱形=联系、椭圆=属性）
> - 码的分类：候选码、主码、超码
> - 弱实体集与强实体集

---

## 二、相关知识点梳理

```
📚 ER模型三要素
├── 📍 实体 — 客观存在并可相互区分的任何事物
│   ├── 例：一个学生、一门课程、一家公司
│   └── ER图中用【矩形框】表示
├── 📍 属性 — 实体所具有的某种特性
│   ├── 例：学生的学号、姓名、年龄
│   └── ER图中用【椭圆形框】表示
└── 📍 联系 — 事物之间的关联
    ├── 例：学生"选修"课程
    └── ER图中用【菱形框】表示

📚 属性的分类
├── 📍 复合属性 — 可以划分为更小部分的属性
│   ├── 例：地址 = 省 + 市 + 街道；姓名 = 姓 + 名
│   └── ER图向关系转换时：使用展平技术，直接用成分属性代替
├── 📍 多值属性 — 可以取多个值的属性
│   ├── 例：一个人的电话号码（可能有多个）、学位
│   └── ER图向关系转换时：为其创建新的关系模式
├── 📍 派生属性 — 可从其他相关属性计算得出的属性
│   ├── 例：年龄（可从出生日期计算）、总价（=单价×数量）
│   └── ER图向关系转换时：忽略
├── 📍 简单属性 — 不可再分的属性
└── 📍 单值属性 — 只能取一个值的属性

📚 联系的基数约束（实体集E1和E2之间）
├── 📍 一对一联系（1:1）
│   ├── E1中每个实体最多与E2中一个实体关联
│   └── E2中每个实体最多与E1中一个实体关联
├── 📍 一对多联系（1:N）
│   ├── E1中每个实体可与E2中任意多个实体关联
│   └── E2中每个实体最多与E1中一个实体关联
├── 📍 多对一联系（N:1）
│   ├── E1中每个实体最多与E2中一个实体关联
│   └── E2中每个实体可与E1中任意多个实体关联
└── 📍 多对多联系（M:N）
    ├── E1中每个实体可与E2中任意多个实体关联
    └── E2中每个实体可与E1中任意多个实体关联

📚 码的类型
├── 📍 超码 — 能唯一标识实体集中每个实体的属性集（可能包含无关紧要的属性）
├── 📍 候选码 — 最小的超码（去掉任何属性后就不再是超码）
├── 📍 主码 — 从候选码中选出的一个，作为实体集的主标识
└── 关系：候选码 ⊆ 超码，主码 ∈ 候选码

📚 弱实体集
├── 📍 定义 — 自身的任何属性都不足以形成码的实体集
├── 📍 必须依赖强实体集（标识性实体集/属主实体集）存在
├── 📍 处理方式 — 作为多值复合属性处理，将其属性纳入强实体集
└── 📍 ER图中用双矩形表示弱实体集

📚 ER图符号速记
├── 实体集 → 矩形框
├── 联系集 → 菱形框
└── 属性   → 椭圆形框
```

**常见误区提醒：**
- **复合属性 vs 多值属性：** "可以再分"= 复合（如地址拆分）；"可以取多个值"= 多值（如多个电话）
- **一对一 vs 一对多 vs 多对一：** 看清楚"谁对谁"——实体集E1对E2的方向很重要
- **超码 vs 候选码：** 超码可能包含多余属性，候选码是最精简的

---

## 三、解题思路与一般方法

**基数约束判断题的通用解法：**

1. 画一个小表格，左侧列E1，右侧列E2
2. 从选项描述中提取两个条件：E1→E2的限制 和 E2→E1的限制
3. 匹配四种类型

| E1→E2 | E2→E1 | 联系类型 |
|-------|-------|---------|
| 最多1个 | 最多1个 | 1:1 |
| 任意多个 | 最多1个 | 1:N（E1对E2） |
| 最多1个 | 任意多个 | N:1（E1对E2） |
| 任意多个 | 任意多个 | M:N |

**常见变形：**
- 从"E1对E2是一对多"换一种说法描述
- 给一个具体场景（如"一个部门有多个职工"），判断联系类型
- 结合ER图符号考察

**易错点提醒：**
- "E1对E2是一对多"等价于"E2对E1是多对一"——方向要搞清楚！
- 超码"包含无关紧要属性"是它区别于候选码的关键特征

---

## 四、见到这种题目的思考框架

```
看到"客观存在并且可相互区分" → 实体的定义 → 答案：实体
看到"事物的某种特性" → 属性的定义 → 答案：属性
看到"事物之间的关联" → 联系的定义 → 答案：联系
看到"可划分为更小部分" → 复合属性 → 答案：复合属性
看到"可取多个值" → 多值属性 → 答案：多值属性
看到"从其他属性计算得到" → 派生属性 → 答案：派生属性
看到两边都"最多一个" → 一对一
看到一边"任意多个"一边"最多一个" → 一对多/多对一（分清方向）
看到两边都"任意多个" → 多对多
看到"包含无关紧要属性" → 超码
看到"矩形框" → 实体集
看到"菱形框" → 联系集
看到"椭圆形框" → 属性
看到"任何属性都不足以形成码" → 弱实体集
```

---

## 五、本题详细解答

### 题1 — 客观存在并可相互区分的任何事物
**答案：A. 实体**

实体的定义：客观存在并可相互区分的任何事物。一个人、一本书、一家公司都是实体。

### 题2 — 表示事物所具有的某种特性
**答案：B. 属性**

属性就是实体具有的某种特性，如学生的姓名、年龄、学号都是学生实体的属性。

### 题3 — 描述事物之间关联的是
**答案：C. 联系**

联系描述实体之间的关联关系，如学生和课程之间的"选修"关系。

### 题4 — 可被划分为更小部分的属性
**答案：A. 复合属性**

复合属性的特征是能被划分为更小的子部分，如姓名可分为姓和名，地址可分为省、市、街道。

### 题5 — 可以取多个值的属性
**答案：B. 多值属性**

多值属性指一个属性在一个实体上可以有多个取值，如一个人可以有多个电话号码。

### 题6 — 可从其他相关属性计算得到
**答案：C. 派生属性**

派生属性的值可以通过其他属性推导/计算得出，如年龄可从出生日期计算得到。

### 题7 — E1每个最多与E2一个关联，且反之亦然 → 一对一
**答案：A. 一对一联系**

两边都是"最多一个"，这是典型的一对一联系，如"一个丈夫最多有一个妻子"且"一个妻子最多有一个丈夫"（在某一法律制度下）。

### 题8 — E1可关联E2任意多个，E2最多关联E1一个 → 一对多（E1对E2）
**答案：B. 一对多联系**

E1一侧可以关联多个E2实体，E2一侧只能关联一个E1实体。如"一个部门有多个职工，一个职工只属于一个部门"。

### 题9 — E1最多关联E2一个，E2可关联E1任意多个 → 多对一（E1对E2）
**答案：C. 多对一联系**

与题8方向相反。如"多个职工属于一个部门"（从职工角度看）。

### 题10 — 两边都可以关联任意多个 → 多对多
**答案：D. 多对多联系**

两边都没有数量限制，如"一个学生可选多门课程，一门课程可被多个学生选修"。

### 题11 — 包含无关紧要属性的码
**答案：B. 超码**

超码的定义是能唯一标识实体集中每个实体的属性集，但它可能包含不必要的（无关紧要的）属性。候选码则是最小超码，不包含任何多余属性。

### 题12 — ER图中实体集用
**答案：A. 矩形框**

ER图基本符号：实体集=矩形，联系集=菱形，属性=椭圆形。

### 题13 — ER图中联系集用
**答案：B. 菱形框**

同上。联系用菱形框表示，内部标注联系名。

### 题14 — ER图中属性用
**答案：C. 椭圆形框**

属性用椭圆形框表示，内部标注属性名，用连线连接到对应的实体或联系上。

### 题15 — 任何属性都不足以形成码的实体集
**答案：B. 弱实体集**

弱实体集的关键特征：自身属性不足以形成码，必须依赖强实体集（标识性实体集/属主实体集）来标识。

### 题16 — 处理弱实体集正确的方法是
**答案：C. 把它作为多值复合属性处理**

弱实体集在ER模型中的正确处理方式是把它作为强实体集的多值复合属性来处理，将其描述的属性纳入属主实体集中。
