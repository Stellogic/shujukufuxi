# 第1章 数据模型与数据库系统结构 — 题目讲解

---

## 一、题目分析

本章15道单选题全部围绕**数据模型的三要素**和**数据库系统的三级模式结构**展开。这是数据库课程最基础的概念章节。

> **题目类型：** 概念辨析型选择题
> **考点：**
> - 数据模型三要素（数据结构、数据操作、数据完整性约束）及其职责划分
> - 三级模式结构（模式、外模式、内模式）及其职责
> - 两级映像（外模式-模式映像、模式-内模式映像）与数据独立性

**关键词解析：**
- **静态特性 vs 动态特性：** 静态 = 系统不运行时也存在的东西，即"结构"；动态 = 系统运行时发生的动作，即"操作"
- **模式 / 外模式 / 内模式：** 模式 = 全体数据的逻辑结构；外模式 = 用户看到的数据逻辑结构；内模式 = 数据的物理存储结构
- **数据独立性：** 逻辑独立性 = 模式变了应用不变（靠外模式-模式映像）；物理独立性 = 存储变了模式不变（靠模式-内模式映像）

---

## 二、相关知识点梳理

```
📚 数据模型三要素
├── 📍 数据结构 — 描述数据库的静态特性（对象类型、对象间联系）
│   ├── 定义：描述数据库中数据对象的组成以及对象之间的联系
│   └── 关键词：静态、对象与联系、结构的框架
├── 📍 数据操作 — 描述数据库的动态特性（增删改查等）
│   ├── 定义：定义各种数据对象实例上允许执行的操作及操作规则
│   └── 关键词：动态、操作方法、操作规则
└── 📍 数据完整性约束 — 保证数据正确、有效、相容的规则
    ├── 定义：一组数据完整性规则的集合，保证数据库中的数据是正确的
    └── 关键词：约束条件、正确性、相容性

📚 数据模型发展历程（按时间顺序）
├── 📍 层次模型 — 最早出现（1960s IBM的IMS系统），树形结构
├── 📍 网状模型 — 第二个出现（1960s末 CODASYL），图形结构
├── 📍 关系模型 — 目前最流行（1970 E.F.Codd提出），二维表结构
└── 📍 面向对象模型 — 较新，融合面向对象思想

📚 数据库系统三级模式结构（ANSI/SPARC架构）
├── 📍 模式（Schema/概念模式）— 全体数据的逻辑结构描述
│   └── 一个数据库只有一个模式
├── 📍 外模式（External Schema/子模式）— 用户视角的数据逻辑结构
│   └── 一个数据库可以有多个外模式（不同用户看到不同视图）
├── 📍 内模式（Internal Schema/存储模式）— 数据的物理存储结构
│   └── 一个数据库只有一个内模式
├── 📍 外模式-模式映像 — 定义外模式与模式之间的对应关系
│   └── 作用：保证数据的逻辑独立性（模式改变时只需修改此映像）
└── 📍 模式-内模式映像 — 定义模式与内模式之间的对应关系
    └── 作用：保证数据的物理独立性（存储结构改变时只需修改此映像）

📚 数据独立性
├── 📍 逻辑数据独立性 — 模式变化时，通过修改外模式-模式映像，使外模式不变
└── 📍 物理数据独立性 — 内模式变化时，通过修改模式-内模式映像，使模式不变
```

**常见误区提醒：**
- **易混点1：** 数据结构描述"对象与对象之间的联系"（题5），不是数据操作！很多人以为联系是动态的，但它属于结构层面。
- **易混点2：** 外模式和内模式的数量：模式1个、内模式1个、外模式可以有**多个**。
- **易混点3：** 两级映像的方向和职责不要搞反：外-模映像 → 逻辑独立性；模-内映像 → 物理独立性。

---

## 三、解题思路与一般方法

**这类概念辨析题的通用解法：**

1. 先明确关键词属性：题目问的是"静态/动态/约束"还是"逻辑/物理/用户视图"还是"映像/独立性"
2. 回到三要素或三级模式的定义，用排除法逐一排除
3. 读选项时特别注意"以上都不对"——如果前三个都不匹配，这个可能是正确答案

**常见变形：**
- 给一个具体描述，问属于三要素中的哪一个
- 给一个具体描述，问属于三级结构中的哪一个
- 问某个映像保证哪种独立性
- 问哪个模式可以有多个

**易错点提醒：**
- "描述对象与对象之间联系" = 数据结构，不是数据操作！
- 保证独立性的是**映像**，不是三个模式本身
- 外模式可以有多个，模式和内模式只有一个

---

## 四、见到这种题目的思考框架

```
看到"静态特性" → 想到数据结构 → 确认：描述对象类型和对象间联系
看到"动态特性" → 想到数据操作 → 确认：增删改查等操作的定义
看到"正确、有效、相容" → 想到数据完整性约束
看到"对象与对象的联系" → 想到数据结构（结构层面描述联系）
看到"操作和操作规则" → 想到数据操作
看到"全体数据的逻辑结构" → 想到模式（全局逻辑结构）
看到"物理结构" → 想到内模式（存储描述）
看到"用户数据逻辑结构" → 想到外模式（用户视角）
看到"逻辑独立性" → 想到外模式-模式映像
看到"物理独立性" → 想到模式-内模式映像
看到"有多个" → 想到外模式（模式只有1个，内模式只有1个）
```

---

## 五、本题详细解答

### 题1 — 数据模型中描述静态特性的是
**答案：A. 数据结构**

数据模型三要素中，数据结构描述系统的静态特性（有哪些数据对象、对象之间如何关联——这些在系统不运行时也存在）；数据操作描述动态特性；数据完整性是约束条件。

### 题2 — 数据模型中描述动态特性的是
**答案：B. 数据操作**

数据操作描述了允许对数据对象执行哪些操作（查询、插入、删除、修改等），这属于系统运行时发生的行为，是动态特性。

### 题3 — 目前最流行的数据模型
**答案：C. 关系模型**

关系模型自1970年E.F.Codd提出后，因其理论基础坚实（关系代数）、使用简单（二维表），已成为主流商用数据库（Oracle、MySQL、SQL Server等）的基础。

### 题4 — 最早出现的数据模型
**答案：A. 层次模型**

层次模型最早出现于1960s，代表系统是IBM的IMS。网状模型随后出现（CODASYL）。关系模型在1970年才被提出。

### 题5 — 描述数据库中对象与对象之间联系的是
**答案：A. 数据结构**

数据结构不仅描述有哪些数据对象，还描述对象之间的**联系**（如实体间的联系、记录型间的联系）。这是数据结构定义的一部分，属于静态特性。

### 题6 — 定义各种数据对象实例上的操作和操作规则的是
**答案：B. 数据操作**

"操作和操作规则"直接对应数据操作要素的定义。这是数据操作要素的核心职责。

### 题7 — 保证数据正确、有效、相容的是
**答案：C. 数据完整性**

关键词"正确"、"有效"、"相容"是数据完整性的目标。数据完整性通过各类约束（实体完整性、参照完整性、用户定义完整性）来保证。

### 题8 — 涉及全体数据逻辑结构描述的是
**答案：A. 模式**

模式（概念模式）描述数据库中全体数据的逻辑结构和特征，是所有用户的公共数据视图。只有一个模式。

### 题9 — 涉及数据物理结构描述的是
**答案：C. 内模式**

内模式（存储模式）描述数据的物理结构和存储方式，如数据存储的文件结构、索引、聚簇等。也只有一个内模式。

### 题10 — 涉及用户数据逻辑结构描述的是
**答案：B. 外模式**

外模式（子模式/用户模式）是数据库用户能看到和使用的局部数据的逻辑结构和特征的描述。可以有多个外模式。

### 题11 — 保证数据逻辑独立性的是
**答案：C. 外模式-模式映像**

当模式发生改变时（如增加新的关系、增加新的属性），只需修改外模式-模式映像，使外模式保持不变，应用程序不用修改——这就是逻辑独立性。

### 题12 — 保证数据物理独立性的是
**答案：D. 模式-内模式映像**

当数据库的存储结构发生改变时（如换存储设备、改变索引结构），只需修改模式-内模式映像，使模式保持不变——这就是物理独立性。

### 题13 — 可以有多个的模式是
**答案：B. 外模式**

模式和内模式在数据库中只有一个，而外模式可以有多个（不同用户有不同的数据视图）。

### 题14 — 定义外模式和模式之间关系的是
**答案：C. 外模式-模式映像**

外模式-模式映像定义了外模式与模式之间的对应关系。当模式改变时，数据库管理员修改此映像来保持外模式不变。

### 题15 — 定义内模式和模式之间关系的是
**答案：D. 内模式-模式映像**

注：选项中写的是"内模式-模式映像"，但通常教材称为"模式-内模式映像"，两者指同一个东西——定义模式与内模式之间的对应关系。
