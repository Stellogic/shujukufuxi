# 第11章 数据库恢复 — 题目讲解

---

## 一、题目分析

本章19道单选题围绕**数据库恢复技术**展开，涵盖故障类型分类、冗余数据机制（日志、后备副本、镜像）、恢复操作（REDO/UNDO）、检查点技术、转储分类等。

> **题目类型：** 概念辨析型选择题
> **考点：**
> - 数据库故障的三种类型（事务故障、系统故障、介质故障）
> - 恢复的基础：冗余数据（日志文件 + 后备副本）
> - 日志文件的作用与登记规则
> - REDO（重做）与 UNDO（撤销）操作
> - 检查点技术（目的：改善恢复效率）
> - 转储的分类（静态/动态、海量/增量）

---

## 二、相关知识点梳理

```
📚 数据库故障的三种类型
├── 📍 事务故障 — 事务未运行到正常终止
│   ├── 原因：程序逻辑错误（如除零）、用户中断、死锁被回滚
│   ├── 影响范围：单个事务
│   └── 恢复：UNDO（撤销该事务的操作）
├── 📍 系统故障 — 系统突然停止，所有事务非正常终止
│   ├── 原因：断电、系统崩溃、操作系统故障
│   ├── 影响范围：所有正在运行的事务
│   ├── 内存数据丢失，磁盘数据完好
│   └── 恢复：UNDO未完成事务 + REDO已提交事务
├── 📍 介质故障 — 硬件损坏导致数据全部破坏
│   ├── 原因：磁盘损坏、磁头碰撞
│   ├── 影响范围：整个数据库
│   └── 恢复：装入后备副本 + REDO日志中的更新
└── 📍 计算机病毒 — 有时也列为故障之一

📚 恢复的冗余数据基础
├── 📍 日志文件(Log) — 记录每次更新操作
│   ├── 内容：事务标识、操作类型、更新前后值
│   ├── 原则：先记日志，后写数据库(Write-Ahead Logging)
│   ├── 时间次序：必须严格按照并发事务执行的时间次序
│   └── 不记录：查询操作（SELECT不需要记录）
├── 📍 后备副本(Backup/Dump) — 数据库的定期备份
│   └── 方法：转储（静态/动态、海量/增量）
├── 📍 镜像盘 — 实时维护的磁盘镜像
│   └── 自动将数据写到两个磁盘上，实现实时热备份
└── 📍 缓冲区(Buffer) — 不是冗余数据！
    └── 缓冲区是内存中的数据缓存，属于正常工作机制

📚 REDO与UNDO操作
├── 📍 REDO（重做）→ 以新值代替旧值
│   ├── 使用时机：已提交事务的修改尚未写入磁盘
│   └── 操作：把日志中记录的新值重新写入数据库
└── 📍 UNDO（撤销）→ 以旧值代替新值
    ├── 使用时机：未提交事务的修改已写入磁盘
    └── 操作：把日志中记录的旧值恢复回数据库

📚 检查点(Checkpoint)技术
├── 📍 目的：改善恢复效率
│   └── 限制恢复时需要处理的日志范围（不需要扫描整个日志）
├── 📍 操作：
│   1. 将缓冲区日志刷入磁盘
│   2. 记录检查点记录
│   3. 将缓冲区数据刷入磁盘
│   4. 写入检查点文件的地址
└── 📍 效果：检查点前已完成的事务不需要REDO

📚 转储的分类
├── 📍 按转储时是否允许事务运行：
│   ├── 静态转储 — 转储期间不允许事务运行
│   └── 动态转储 — 转储期间允许事务运行（需配合日志）
├── 📍 按转储数据量：
│   ├── 海量转储 — 每次转储整个数据库
│   └── 增量转储 — 只转储上次转储后变化的数据
└── 组合方式：静态海量、静态增量、动态海量、动态增量

📚 恢复技术
├── 📍 基于日志+后备副本的恢复
├── 📍 影子分页技术
├── 📍 数据库镜像
└── 缓冲区技术不属于恢复技术

📚 恢复的核心思想
├── "恢复的基本思想是建立冗余数据" — 正确
├── "恢复是杜绝故障发生" — 错误！故障无法完全避免，恢复是在故障后修复
└── 恢复 ≠ 预防，恢复 = 事后补救
```

**常见误区提醒：**
- **恢复的基本思想**不是"杜绝故障"（故障无法完全避免），而是"建立冗余"来补救！
- **日志记录什么？** 记录所有更新操作，**不记录查询操作**！
- **缓冲区是不是冗余数据？** 不是！缓冲区是正常运行的一部分，日志/后备副本/镜像才是冗余！
- **后备副本用于什么？** 用于"故障后的恢复"，不是用于"安全性保证"！
- **检查点的目的**不是解决故障，而是**改善恢复效率**！

---

## 三、解题思路与一般方法

**故障类型快速判断：**
```
程序逻辑错误、运算异常 → 事务故障
断电、系统崩溃 → 系统故障
磁盘损坏、硬件故障 → 介质故障
```

**冗余数据快速判断：**
```
日志文件 → 冗余 ✓（记录所有更新）
后备副本 → 冗余 ✓（数据备份）
镜像盘 → 冗余 ✓（实时镜像）
缓冲区 → 不是冗余 ✗（工作内存）
```

---

## 四、见到这种题目的思考框架

```
看到"未运行到正常中止" → 事务故障
看到"硬件损坏/整个数据库被破坏" → 介质故障
看到"系统突然停止/所有事务非正常终止" → 系统故障
看到"记录每次更新" → 日志文件
看到"不是冗余数据" → 缓冲区
看到"恢复的基础/转储的冗余" → 日志文件 + 后备副本
看到"先记日志后写数据库" → 日志优先原则(Write-Ahead Logging)
看到"检查点目的" → 改善恢复效率
看到"新值代替旧值" → REDO（重做）
看到"旧值代替新值" → UNDO（撤销）
看到"是否允许事务运行" → 静态转储 vs 动态转储
看到"转储全部还是部分" → 海量转储 vs 增量转储
看到"恢复不包括" → 缓冲区技术
看到"杜绝故障"或"预防故障"作为恢复思想 → 错误
看到"日志不记录什么" → 不记录查询操作
看到"恢复 ≠" → 安全性、完整性、并发控制（恢复是一个独立概念）
```

---

## 五、本题详细解答

### 题1 — 事务未运行到正常中止
**答案：A. 事务故障**

事务故障指事务在运行过程中因程序逻辑错误、用户中止、死锁等原因未能正常完成而终止，只影响该事务本身。

### 题2 — 硬件损坏导致数据库被破坏
**答案：C. 介质故障**

介质故障指存放数据库的硬件（磁盘等）损坏，导致数据库数据全部或部分丢失。这是最严重的故障类型。

### 题3 — 系统突然停止运行，所有事务非正常终止
**答案：B. 系统故障**

系统故障（软故障）指系统断电或崩溃导致整个系统停止，内存数据丢失但磁盘数据完好，所有正在运行的事务非正常终止。

### 题4 — 记录对数据库的每次更新操作
**答案：C. 日志文件**

日志文件(Log)用于记录对数据库的每一次更新操作（插入、删除、修改），包括更新前后的值，是恢复的基础。

### 题5 — 不是冗余数据的是
**答案：D. 缓冲区**

缓冲区是DBMS在内存中维护的工作区域，用于缓存磁盘数据以提高性能，不是冗余数据。后备副本、镜像盘、日志文件都是为了恢复目的而建立的数据副本/记录，属于冗余数据。

### 题6 — 转储的冗余数据包括
**答案：D. 日志文件、数据库后备副本**

数据库恢复利用的冗余数据主要是日志文件（记录更新细节）和数据库后备副本（定期备份的快照）。

### 题7 — 引入检查点技术的目的是
**答案：A. 改善恢复效率**

检查点技术通过在特定时间点记录一致性快照，使恢复时不需要扫描整个日志文件，只需从最近的检查点开始处理，从而显著提高恢复效率。

### 题8 — 后备副本的用途是
**答案：B. 故障后的恢复**

后备副本是数据库的备份，当数据库因介质故障被破坏时，可以用后备副本来恢复（再加日志REDO）。

### 题9 — 说法不正确的是
**答案：B. 恢复的基本思想是提高系统安全性，杜绝故障发生**

恢复的基本思想是建立冗余数据（日志+备份），在故障发生后进行修复。故障无法完全杜绝（如硬件老化、断电），因此"杜绝故障发生"是不可行的目标。

### 题10 — 按转储时是否允许事务运行分类
**答案：A. 静态转储和动态转储**

静态转储 = 停止所有事务后进行备份；动态转储 = 允许事务同时运行的备份（需配合日志）。

### 题11 — 按转储数据量分类
**答案：D. 海量转储和增量转储**

海量转储 = 一次性备份整个数据库；增量转储 = 只备份上次转储后变化的数据。

### 题12 — 数据库恢复技术不包括
**答案：D. 缓冲区技术**

恢复技术包括基于日志+后备副本的恢复、影子分页、数据库镜像。缓冲区是常规的内存管理技术，不是恢复技术。

### 题13 — REDO操作是
**答案：A. 以新值代替旧值**

REDO（重做）操作：根据日志中记录的新值(After Image)，将修改重新应用到数据库。

### 题14 — UNDO操作是
**答案：B. 以旧值代替新值**

UNDO（撤销）操作：根据日志中记录的旧值(Before Image)，将数据库恢复到修改之前的状态。

### 题15 — 程序逻辑错误可能导致
**答案：A. 事务故障**

程序中的逻辑错误（如除零、数据溢出、死锁、违反约束等）会导致事务无法正常完成，属于事务故障。

### 题16 — 系统断电可能导致
**答案：B. 系统故障**

断电会导致整个系统突然停机，所有内存数据丢失，属于系统故障。

### 题17 — 磁盘损坏可能导致
**答案：C. 介质故障**

磁盘是存储介质，磁盘损坏直接破坏存储在上的数据，属于介质故障。

### 题18 — 说法不正确的是
**答案：B. 日志记录了所有的查询操作**

- A正确：日志记录所有更新操作
- B错误：日志不记录查询操作（SELECT不需要恢复，没有必要记录）
- C正确：日志必须按时间次序记录以保证恢复的正确性
- D正确：这是Write-Ahead Logging原则（先写日志，后写数据库）

### 题19 — 数据库数据被破坏后需要
**答案：D. 恢复**

当数据库中的数据因故障被破坏，需要用之前建立的冗余数据（日志+后备副本）对数据库进行恢复，使其回到一致性状态。
