# 第3章 关系模型与关系代数 — 题目讲解

---

## 一、题目分析

本章20道单选题覆盖两大考点：(1) **关系模型的完整性约束**（实体完整性、参照完整性、用户定义完整性）及其与不同码的关系；(2) **关系代数运算**的分类、特性、以及E-R图向关系模式的转换规则。

> **题目类型：** 概念辨析型 + 特性判断型选择题
> **考点：**
> - 三类完整性约束及其约束对象
> - 码的分类及空值规则
> - E-R图向关系模式的转换规则
> - 关系代数：五种基本运算、一元/二元运算、行/列运算
> - 特殊运算的条件和特性（自然连接需要同名属性、除运算是笛卡儿积的逆运算等）

---

## 二、相关知识点梳理

```
📚 关系模型的三类完整性约束
├── 📍 实体完整性 — 主码不能取空值
│   ├── 约束对象：主码
│   └── 规则：主码值必须唯一且不能为空（因为主码用于唯一标识元组）
├── 📍 参照完整性 — 外码取值受限制
│   ├── 约束对象：外码
│   ├── 规则：外码要么取空值，要么等于被参照关系中某个元组的主码值
│   └── 作用：描述参照关系（表与表之间的引用关系）
└── 📍 用户自定义完整性 — 用户根据业务需求自定义的约束
    └── 例：成绩在0~100之间、性别只能是'男'或'女'

📚 码与空值
├── 📍 主码 → 不能取空值（实体完整性要求）
├── 📍 候选码 → 等价于主码，也不能取空值
├── 📍 外码 → 可以取空值（参照完整性允许）
└── 📍 超码 → 包含主码，也不能取空值

📚 关系的性质
├── 关系中不能有两个完全相同的元组（因为关系是元组的集合）
├── 行的次序不重要（关系是集合，集合无序）
├── 列的次序不重要（属性由属性名标识，不由位置标识）
└── 二维表可以表示关系（但并非所有二维表都是关系）

📚 E-R图向关系模式转换规则
├── 📍 实体 → 转换为一个关系（表）
├── 📍 复合属性 → 展平技术：忽略复合属性本身，直接使用其成分属性
├── 📍 多值属性 → 为其创建一个新的关系模式
├── 📍 派生属性 → 忽略（不存储，用时计算）
├── 📍 一对一联系 → 码可以是任一端实体的码
├── 📍 一对多联系 → 使用多端实体的码作为码
├── 📍 多对多联系 → 使用两端实体码的联合作为码

📚 关系代数运算
├── 📍 五种基本运算：并、差、投影(π)、选择(σ)、笛卡儿积(×)
│   └── 注意：交、连接、除都可以由基本运算推导而出，不属于基本运算
├── 📍 一元运算 — 只涉及一个关系的运算
│   ├── 选择(σ)：从行方向筛选元组 → 属于"行运算"
│   └── 投影(π)：从列方向选取属性 → 属于"列运算"（可能伴随行删除，因为要去除重复元组）
├── 📍 二元运算 — 涉及两个关系的运算
│   ├── 并(∪)：要求R和S属性个数相同、对应属性域相同
│   ├── 交(∩)：由属于R又属于S的元组组成
│   ├── 差(−)：由属于R但不属于S的元组组成
│   └── 笛卡儿积(×)：R(n列)×S(m列) → 结果有n+m列
├── 📍 连接运算
│   ├── θ连接：R⋈S（带任意比较条件）
│   ├── 等值连接：θ连接的特殊情况（条件为"="）
│   └── 自然连接：特殊的等值连接，要求有同名属性，结果中去除重复列
│       必须条件：R和S有同名属性
├── 📍 除运算(÷) — 笛卡儿积的逆运算
│   └── R÷S 的结果：R中与S所有元组都关联的那些元组
└── 📍 集合运算总结
    ├── 并(∪)：属性数目不变
    ├── 交(∩)：属性数目不变
    ├── 差(−)：属性数目不变
    └── 笛卡儿积(×)：属性数目增加(n+m)
```

**常见误区提醒：**
- **五种基本运算不是"并、交、差、选择、投影"！** 交不是基本运算，笛卡儿积才是！基本运算是：并、差、选择、投影、笛卡儿积。
- **选择 vs 投影：** 选择是行运算（横切），投影是列运算（竖切）。投影伴随行删除是因为去掉重复元组。
- **自然连接必须有同名属性**，这是区别于等值连接的关键点。
- **除运算是笛卡儿积的逆运算**，这个关系常常被考到。

---

## 三、解题思路与一般方法

**完整性约束判断题：**
1. 看题目问"限制哪个码"——主码→实体完整性，外码→参照完整性
2. 记住空值规则：主码/候选码/超码不能空，外码可以空

**关系代数题：**
1. 先判断是几元运算：只涉及一个关系=一元（选择、投影），两个关系=二元
2. 判断是行还是列：选择=行，投影=列
3. 集合运算注意属性数目变化：笛卡儿积增加，其他不变

---

## 四、见到这种题目的思考框架

```
看到"主码取值限制" → 实体完整性
看到"外码取值限制" → 参照完整性
看到"可不可以取空值" → 主码/候选码/超码不可空，外码可空
看到"两个完全相同元组" → 错误，关系中不允许
看到"行/列次序重要" → 错误，都不重要
看到"实体转换为" → 关系
看到"多值属性转换" → 新建关系模式
看到"复合属性转换" → 展平技术
看到"派生属性转换" → 忽略
看到"五种基本运算" → 并、差、选择、投影、笛卡儿积（没有交！）
看到"一元运算" → 选择或投影
看到"行运算" → 选择
看到"列运算+行删除" → 投影
看到"同名属性" → 自然连接
看到"笛卡儿积逆运算" → 除运算
看到"属性数目增加" → 笛卡儿积
```

---

## 五、本题详细解答

### 题1 — 对主码取值限制的是
**答案：A. 实体完整性**

实体完整性规则：主码的值必须唯一且不能为空。它专门约束主码。

### 题2 — 对外码取值限制的是
**答案：B. 参照完整性**

参照完整性规则：外码必须要么取空值，要么等于被参照关系主码的某个值。

### 题3 — 可以取空值的是
**答案：B. 外码**

主码、候选码、超码都受实体完整性约束不能取空值。只有外码在参照完整性规则中允许取空值（表示"不引用任何实体"）。

### 题4 — 所有属性不能取空值的是
**答案：A. 主码**

实体完整性明确规定主码不能取空值。外码可以取空值。超码包含主码，所以同样不能取空值的属性是那些属于主码的属性。

### 题5 — 说法正确的是
**答案：D. 二维表可以表示关系**

- A错误：关系中不能有完全相同的元组（集合的性质）
- B错误：行的次序不重要
- C错误：列的次序不重要
- D正确：二维表（满足一定条件）可以表示关系

### 题6 — 可以描述参照关系的是
**答案：B. 外码**

外码建立了两个关系之间的参照（引用）关系，一个关系的外码引用另一个关系的主码。

### 题7 — E-R图中实体转换为
**答案：A. 关系**

E-R图向关系模式转换时，每个实体集转换为一个关系（即一张表）。

### 题8 — 多值属性如何处理
**答案：C. 为其创建一个新的关系模式**

多值属性不能直接放在原关系中（违反1NF），必须单独创建一个关系模式来存放。

### 题9 — 复合属性如何处理
**答案：B. 展平技术，忽略复合属性本身，直接使用其成分属性代替**

复合属性如"地址"可拆分为"省、市、街道"等成分属性，转换时只保留成分属性，忽略复合属性本身。

### 题10 — 派生属性如何处理
**答案：A. 忽略**

派生属性可以从其他属性计算得出（如年龄从出生日期计算），不需要在数据库中存储，直接忽略。

### 题11 — 一对多联系转换后使用什么作为码
**答案：B. 多端实体的码**

在一对多联系中，将一端实体的主码作为外码加入多端实体对应的关系中，该关系的码是多端实体的码。

### 题12 — 多对多联系转换后使用什么作为码
**答案：C. 实体集M和N的码的联合**

多对多联系需要单独创建一个关系，该关系的主码由两端实体主码的联合组成。

### 题13 — 关系代数五种基本运算
**答案：D. 并、差、投影、选择、笛卡儿积**

五种基本运算是：并(∪)、差(−)、投影(π)、选择(σ)、笛卡儿积(×)。交运算可由差运算推导（R∩S = R−(R−S)），不属于基本运算。

### 题14 — 属于一元运算的是
**答案：A. 选择运算**

选择运算只涉及一个关系（从该关系中筛选元组），是一元运算。并、交、除都是二元运算（需要两个关系）。

### 题15 — 必须有同名属性才能进行的是
**答案：D. 自然连接**

自然连接要求两个关系有同名属性，并自动根据所有同名属性的值相等来进行连接，结果中去除重复列。

### 题16 — 属于行运算的是
**答案：A. 选择运算**

选择(σ)从行的角度筛选元组（WHERE条件），投影(π)从列的角度选取属性，笛卡儿积和连接属于行列都涉及的运算。

### 题17 — 属于列运算但伴随行删除的是
**答案：B. 投影运算**

投影选取指定的列，但可能产生重复元组（多个不同元组投影后变得相同），这些重复行会被删除，所以说"伴随行删除"。

### 题18 — 属于R但不属于S的所有元组
**答案：C. R差S**

差运算R−S的结果包含所有在R中但不在S中的元组。

### 题19 — 笛卡儿积的逆运算
**答案：D. 除运算**

除运算(÷)是笛卡儿积的逆运算。如果T=R÷S，那么T×S的结果包含在R中。

### 题20 — 做哪个运算属性数目增加
**答案：D. 笛卡儿积运算**

R有n个属性，S有m个属性，R×S的结果有n+m个属性（两表属性直接拼接）。并、交、差运算要求两表属性数相同，结果属性数不变。
