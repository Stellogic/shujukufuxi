# 第3章 关系模型与关系代数 — 题目讲解

---

## 一、题目分析

本章20道单选题覆盖两大考点：(1) **关系模型的完整性约束**（实体完整性、参照完整性、用户定义完整性）及其与不同码的关系；(2) **关系代数运算**的分类、特性、以及E-R图向关系模式的转换规则。

> **题目类型：** 概念辨析型 + 特性判断型选择题
> **考点：**
> - 三类完整性约束及其约束对象
> - 码的分类及空值规则
> - E-R图向关系模式的转换规则
> - 关系代数：五种基本运算、一元/二元运算、行/列运算
> - 特殊运算的条件和特性（自然连接需要同名属性、除运算是笛卡儿积的逆运算等）

---

## 二、相关知识点梳理

```
📚 关系模型的三类完整性约束
├── 📍 实体完整性 — 主码不能取空值
│   ├── 约束对象：主码
│   └── 规则：主码值必须唯一且不能为空（因为主码用于唯一标识元组）
├── 📍 参照完整性 — 外码取值受限制
│   ├── 约束对象：外码
│   ├── 规则：外码要么取空值，要么等于被参照关系中某个元组的主码值
│   └── 作用：描述参照关系（表与表之间的引用关系）
└── 📍 用户自定义完整性 — 用户根据业务需求自定义的约束
    └── 例：成绩在0~100之间、性别只能是'男'或'女'

📚 码与空值
├── 📍 主码 → 不能取空值（实体完整性要求）
├── 📍 候选码 → 主码是从候选码中选定的一个，候选码也不能取空值
├── 📍 外码 → 可以取空值（参照完整性允许）
└── 📍 超码 → 包含主码，也不能取空值

📚 关系的性质
1. 元组：元组 = 表中的一行记录

├── 关系中不能有两个完全相同的元组（因为关系是元组的集合）
├── 行的次序不重要（关系是集合，集合无序）
├── 列的次序不重要（属性由属性名标识，不由位置标识）
└── 二维表可以表示关系（但并非所有二维表都是关系）

📚 E-R图向关系模式转换规则
├── 📍 实体 → 转换为一个关系（表）
├── 📍 复合属性 → 展平技术：忽略复合属性本身，直接使用其成分属性
├── 📍 多值属性 → 为其创建一个新的关系模式
├── 📍 派生属性 → 忽略（不存储，用时计算）
├── 📍 一对一联系 → 码可以是任一端实体的码
├── 📍 一对多联系 → 使用多端实体的码作为码
├── 📍 多对多联系 → 使用两端实体码的联合作为码

📚 关系代数运算

📚 一、五种基本运算（构成关系代数完备集）
├── 并(∪)、差(−)、投影(π)、选择(σ)、笛卡儿积(×)
├── 这五种运算构成了关系代数的完备集，任何其他关系代数运算都可以由它们组合表达：
│   ├── 交：R∩S = R − (R − S)    ← 用两次差运算实现
│   ├── 连接：R⋈S = σ_条件(R × S) ← 先笛卡儿积再选择
│   └── 除：可由投影、笛卡儿积、差组合推导
└── 注意：交、连接、除都不属于五种基本运算！

📚 二、一元运算 — 只涉及一个关系的运算

├── 📍 选择(σ) — Selection
│   ├── 记法：σ_条件(关系名)  或  σ_条件(R)
│   ├── 含义：从关系R中选取满足条件的元组（行）
│   ├── 条件写法（谓词）：
│   │   ├── 比较运算符：=, ≠, >, ≥, <, ≤
│   │   └── 逻辑运算符：∧(与), ∨(或), ¬(非)
│   ├── 示例：σ_年龄>20(学生) → 选出年龄大于20的所有学生
│   ├── 示例：σ_性别='男' ∧ 年龄>20(学生) → 选出年龄大于20的男学生
│   ├── 本质：从"行"方向上水平切割关系 → 属于"行运算"
│   ├── 结果元组数：≤ 原关系元组数
│   └── 结果属性数：= 原关系属性数（列不变）

├── 📍 投影(π) — Projection
│   ├── 记法：π_属性列表(关系名)  或  π_A,B,C(R)
│   ├── 含义：从关系R中选取指定的属性列，组成新关系
│   ├── 示例：π_姓名,年龄(学生) → 只保留姓名和年龄两列
│   ├── 关键特性 — 自动去重：
│   │   ├── 投影后可能出现完全相同的元组（因为只保留了部分列）
│   │   └── 关系是元组的集合，集合中不允许重复 → 自动删除重复行
│   ├── 本质：从"列"方向上垂直切割关系 → 属于"列运算"
│   │   └── 但伴随"行删除"（去重），所以严格说是"列+去重行"
│   ├── 结果属性数：≤ 原关系属性数（只保留指定列）
│   └── 结果元组数：≤ 原关系元组数（去重可能减少行数）

└── 📍 更名(ρ) — Rename（也属于一元运算）
    ├── 记法：ρ_S(A1,A2,…)(R) → 将关系R重命名为S，属性重命名为A1,A2,…
    ├── 示例：ρ_S(学号,姓名)(学生) → 把"学生"表改名为S，列改名为学号、姓名
    └── 用途：在自连接（表和自己做连接）时必须用到，否则无法区分两份拷贝

📚 三、二元运算 — 涉及两个关系的运算

├── 📍 并(∪) — Union
│   ├── 记法：R ∪ S
│   ├── 含义：结果由属于R或属于S（或同时属于两者）的所有元组组成
│   ├── 前置条件（并相容性）：
│   │   ├── ① R和S的属性个数必须相同（都是n个属性）
│   │   └── ② R的第i个属性与S的第i个属性的域（数据类型）必须相同
│   ├── 结果：属性数 = n（不变），元组数 ≤ R元组数 + S元组数
│   └── 去重：自动去除重复元组（集合运算的性质）

├── 📍 交(∩) — Intersection
│   ├── 记法：R ∩ S
│   ├── 含义：结果由既属于R又属于S的元组组成
│   ├── 前置条件：同并运算，需要并相容性
│   ├── 结果：属性数不变，元组数 ≤ min(R元组数, S元组数)
│   ├── 推导：R ∩ S = R − (R − S) ← 不是基本运算！
│   └── 💡 具体例子：
│       ┌──────────────────────────────────────┐
│       │ 选修数据库的学生表 R                  │
│       │ ┌──────┬────────┐                    │
│       │ │ 学号 │  姓名  │                    │
│       │ ├──────┼────────┤                    │
│       │ │ 001  │ 张三  │                    │
│       │ │ 002  │ 李四  │                    │
│       │ │ 003  │ 王五  │                    │
│       │ └──────┴────────┘                    │
│       │                                      │
│       │ 选修操作系统的学生表 S                │
│       │ ┌──────┬────────┐                    │
│       │ │ 学号 │  姓名  │                    │
│       │ ├──────┼────────┤                    │
│       │ │ 002  │ 李四  │  ← 交集            │
│       │ │ 003  │ 王五  │  ← 交集            │
│       │ │ 004  │ 赵六  │                    │
│       │ └──────┴────────┘                    │
│       │                                      │
│       │ R ∩ S = 两门课都选了的同学：          │
│       │ ┌──────┬────────┐                    │
│       │ │ 学号 │  姓名  │                    │
│       │ ├──────┼────────┤                    │
│       │ │ 002  │ 李四  │                    │
│       │ │ 003  │ 王五  │                    │
│       │ └──────┴────────┘                    │
│       │                                      │
│       │ 关键直觉：两张表都存在的行才留下       │
│       │ SQL：SELECT * FROM R INTERSECT       │
│       │      SELECT * FROM S                 │
│       └──────────────────────────────────────┘
│
├── 📍 差(−) — Difference
│   ├── 记法：R − S（注意是有顺序的！）
│   ├── 含义：结果由属于R但不属于S的元组组成
│   ├── 前置条件：同并运算，需要并相容性
│   ├── 结果：属性数不变，元组数 ≤ R元组数
│   ├── 注意：R − S ≠ S − R（差运算不可交换！）
│   └── 💡 具体例子（续用上面的表）：
│       ┌──────────────────────────────────────┐
│       │ 选修数据库的表 R                      │
│       │ ┌──────┬────────┐                    │
│       │ │ 学号 │  姓名  │                    │
│       │ ├──────┼────────┤                    │
│       │ │ 001  │ 张三  │ ← 只有R有          │
│       │ │ 002  │ 李四  │ ← 两表都有          │
│       │ │ 003  │ 王五  │ ← 两表都有          │
│       │ └──────┴────────┘                    │
│       │                                      │
│       │ 选修操作系统的表 S                    │
│       │ ┌──────┬────────┐                    │
│       │ │ 学号 │  姓名  │                    │
│       │ ├──────┼────────┤                    │
│       │ │ 002  │ 李四  │                    │
│       │ │ 003  │ 王五  │                    │
│       │ │ 004  │ 赵六  │ ← 只有S有          │
│       │ └──────┴────────┘                    │
│       │                                      │
│       │ R − S = 只选了数据库、没选OS的同学：  │
│       │ ┌──────┬────────┐                    │
│       │ │ 学号 │  姓名  │                    │
│       │ ├──────┼────────┤                    │
│       │ │ 001  │ 张三  │                    │
│       │ └──────┴────────┘                    │
│       │                                      │
│       │ S − R = 只选了OS、没选数据库的同学：  │
│       │ ┌──────┬────────┐                    │
│       │ │ 学号 │  姓名  │                    │
│       │ ├──────┼────────┤                    │
│       │ │ 004  │ 赵六  │                    │
│       │ └──────┴────────┘                    │
│       │                                      │
│       │ R − S ≠ S − R，差运算不可交换！       │
│       │ 关键直觉：把两张表里"重叠的行"删掉    │
│       │ 从R做差就去掉R里的重叠部分             │
│       │ SQL：SELECT * FROM R EXCEPT          │
│       │      SELECT * FROM S                 │
│       └──────────────────────────────────────┘

└── 📍 笛卡儿积(×) — Cartesian Product
    ├── 记法：R × S
    ├── 含义：R中每个元组与S中每个元组两两拼接，形成所有可能的组合
    ├── 无须前置条件（任何两个关系都可以做笛卡儿积）
    ├── 结果规模：
    │   ├── 属性数 = n + m（R的所有列 + S的所有列，直接拼接）
    │   ├── 元组数 = R元组数 × S元组数
    │   └── 如果R有100行、S有50行 → 结果有5000行！
    └── 示例：R(A,B) = {(1,2),(3,4)}，S(C,D) = {(5,6),(7,8)}
        R×S = {(1,2,5,6), (1,2,7,8), (3,4,5,6), (3,4,7,8)}

📚 四、连接运算 — 笛卡儿积 + 选择条件的结合

├── 📍 θ连接（Theta Join）
│   ├── 记法：R ⋈_AθB S
│   ├── 含义：R×S后，只保留R.A与S.B满足θ条件的元组
│   ├── θ 可以是 >, <, ≥, ≤, =, ≠ 中的任意一个
│   ├── 示例：R ⋈_R.A>S.B S → 只保留R.A > S.B的那些组合行
│   └── 本质：σ_AθB(R × S) ← 先做笛卡儿积，再做选择

├── 📍 等值连接（Equi-Join）
│   ├── 记法：R ⋈_A=B S
│   ├── 含义：θ连接的θ固定为"="的特殊情况
│   ├── 结果保留所有列（包括两个等值的列，即有重复列）
│   └── 示例：R(A,B) ⋈_A=C S(C,D) → 结果有列(A,B,C,D)，A和C的值相等但都保留

└── 📍 自然连接（Natural Join）⋈
    ├── 记法：R ⋈ S（没有下标！）
    ├── 含义：自动根据所有同名属性做等值连接，且去除重复列
    ├── 必须条件：R和S至少有一个同名属性
    ├── 执行步骤：
    │   ├── ① 找出R和S中所有同名属性
    │   ├── ② 对这些同名属性做等值连接
    │   └── ③ 结果中去掉重复的同名列，每对同名属性只保留一列
    ├── 与等值连接的三个区别：
    │   ├── ① 自然连接自动匹配所有同名属性，等值连接需手动指定
    │   ├── ② 自然连接结果去除重复列，等值连接保留重复列
    │   └── ③ 自然连接必须要有同名属性，等值连接没有这个要求
    ├── 示例：
    │   教师(教师号, 姓名, 系号)
    │   系(系号, 系名)
    │   教师 ⋈ 系 → 结果列：(教师号, 姓名, 系号, 系名)，"系号"只出现一次
    └── 如果没有同名属性 → 自然连接退化为笛卡儿积！
        └── 如果所有属性都同名 → 自然连接退化为交运算！

├── 📍 外连接（Outer Join）— 保留悬浮元组
│   ├── 问题背景：自然连接中，如果R中某元组在S中没有匹配，该元组会被丢弃
│   ├── 外连接就是保留这些"找不到匹配"的元组，缺失部分填NULL
│   ├── 左外连接(⟕)：保留左侧关系R中的所有元组
│   │   └── R中找不到匹配的元组，S的属性全是NULL
│   ├── 右外连接(⟖)：保留右侧关系S中的所有元组
│   │   └── S中找不到匹配的元组，R的属性全是NULL
│   └── 全外连接(⟗)：保留两侧所有元组
│       └── 两侧找不到匹配的元组都保留，对方属性补NULL

📚 五、除运算(÷) — 笛卡儿积的逆运算

├── 📍 记法：R ÷ S
├── 📍 适用场景："满足所有条件的…"
│   └── 如："选修了所有课程的学生" / "供应所有零件的供应商"
├── 📍 前提：S的属性集合必须是R的属性集合的子集
├── 📍 结果：R中那些与S中所有元组都关联的元组（在R的非S属性上的投影）
├── 📍 执行步骤（三步法）：
│   ├── ① T = π_非S属性(R) → 拿到所有候选结果
│   ├── ② U = T × S → 完整组合（候选 × S所有元组）
│   ├── ③ U − R → 找出"缺少的组合"
│   ├── ④ T − π_非S属性(U − R) → 排除缺少组合的候选
│   └── 等价公式：R ÷ S = π_A(R) − π_A((π_A(R) × S) − R)，其中A=R中非S的属性
├── 📍 具体示例：
│   选课表SC(学号, 课号)           课程表C(课号)
│   ┌────┬────┐                    ┌────┐
│   │学号│课号│                    │课号│
│   ├────┼────┤                    ├────┤
│   │ S1 │ C1 │                    │ C1 │
│   │ S1 │ C2 │                    │ C2 │
│   │ S2 │ C1 │                    └────┘
│   │ S2 │ C2 │
│   │ S3 │ C1 │
│   └────┴────┘
│   SC ÷ C → 找出选修了所有课程的学生
│   结果：{S1, S2}（S3只选了C1，被排除）
└── 📍 记忆口诀：R÷S = "找出R中与S所有元组都匹配的那些"

📚 六、各运算属性数目变化总结

运算种类          │ 属性数目变化        │ 元组数目变化
─────────────────┼──────────────────┼──────────────────
选择(σ)          │ 不变               │ 减少或不变
投影(π)          │ 减少或不变          │ 减少或不变（去重）
更名(ρ)          │ 不变               │ 不变
并(∪)            │ 不变               │ ≤ R行数 + S行数
交(∩)            │ 不变               │ ≤ min(R,S)
差(−)            │ 不变               │ ≤ R行数
笛卡儿积(×)      │ n + m（增加）       │ R行数 × S行数
θ连接/等值连接   │ n + m（增加）       │ ≤ R行数 × S行数
自然连接          │ ≤ n + m（去除重复列）│ ≤ R行数 × S行数
除(÷)            │ 减少               │ 减少
```

**常见误区提醒：**
- **五种基本运算不是"并、交、差、选择、投影"！** 交不是基本运算，笛卡儿积才是！基本运算是：并、差、选择、投影、笛卡儿积。
- **选择 vs 投影：** 选择是行运算（横切），投影是列运算（竖切）。投影伴随行删除是因为去掉重复元组。
- **自然连接必须有同名属性**，这是区别于等值连接的关键点。没有同名属性时自然连接退化为笛卡儿积。
- **等值连接 vs 自然连接：** 等值连接保留重复列（同名属性出现两次），自然连接去除重复列（同名属性只保留一次）。
- **除运算是笛卡儿积的逆运算**，这个关系常常被考到。
- **交运算不是基本运算**，可由差推导：R∩S = R−(R−S)。
- **连接运算也不是基本运算**，可由笛卡儿积+选择推导：R⋈S = σ_条件(R×S)。
- **并、交、差都需要并相容性**（属性个数相同、对应属性域相同），笛卡儿积不需要任何条件。
- **差运算不可交换**：R−S ≠ S−R。
- **外连接**保留了在自然连接中会被丢弃的"悬浮元组"，缺失值填NULL。

---

## 三、解题思路与一般方法

**完整性约束判断题：**
1. 看题目问"限制哪个码"——主码→实体完整性，外码→参照完整性
2. 记住空值规则：主码/候选码/超码不能空，外码可以空

**关系代数题：**
1. 先判断是几元运算：只涉及一个关系=一元（选择、投影、更名），两个关系=二元
2. 判断是行还是列：选择=行，投影=列
3. 集合运算注意属性数目变化：笛卡儿积增加(n+m)，自然连接介于n+m之间（去重列），并交差不变
4. 判断连接类型：有"="条件但保留重复列→等值连接；有"="条件、有同名属性、去除重复列→自然连接；有其他比较符(>,<,≥,≤,≠)→一般θ连接
5. 外连接识别：结果中有NULL→外连接（左外连接：左表全保留；右外连接：右表全保留）
6. 除运算：题目中出现"所有"、"全部"关键词→考虑除运算

---

## 四、见到这种题目的思考框架

```
看到"主码取值限制" → 实体完整性
看到"外码取值限制" → 参照完整性
看到"可不可以取空值" → 主码/候选码/超码不可空，外码可空
看到"两个完全相同元组" → 错误，关系中不允许
看到"行/列次序重要" → 错误，都不重要
看到"实体转换为" → 关系
看到"多值属性转换" → 新建关系模式
看到"复合属性转换" → 展平技术
看到"派生属性转换" → 忽略
看到"五种基本运算" → 并、差、选择、投影、笛卡儿积（没有交！）
看到"不是基本运算" → 交、连接、除
看到"一元运算" → 选择、投影、更名
看到"行运算" → 选择
看到"列运算 + 行删除" → 投影
看到"同名属性" → 自然连接
看到"同名属性 + 去除重复列" → 自然连接
看到"保留重复列" → 等值连接
看到"笛卡儿积 + 选择" → 连接的本质
看到"笛卡儿积逆运算" → 除运算
看到"所有 / 全部" → 除运算
看到"属性数目增加" → 笛卡儿积、连接
看到"属性数目不变" → 选择、投影(更名)、并、交、差
看到"结果中有NULL" → 外连接
看到"需要并相容性" → 并、交、差
看到"不需要任何条件" → 笛卡儿积、选择、投影
看到"不可交换" → 差运算（R−S ≠ S−R）
看到"悬浮元组" → 外连接
```

---

## 五、本题详细解答

### 题1 — 对主码取值限制的是
**答案：A. 实体完整性**

实体完整性规则：主码的值必须唯一且不能为空。它专门约束主码。

### 题2 — 对外码取值限制的是
**答案：B. 参照完整性**

参照完整性规则：外码必须要么取空值，要么等于被参照关系主码的某个值。

### 题3 — 可以取空值的是
**答案：B. 外码**

主码、候选码、超码都受实体完整性约束不能取空值。只有外码在参照完整性规则中允许取空值（表示"不引用任何实体"）。

### 题4 — 所有属性不能取空值的是
**答案：A. 主码**

实体完整性明确规定主码不能取空值。外码可以取空值。超码包含主码，所以同样不能取空值的属性是那些属于主码的属性。

### 题5 — 说法正确的是
**答案：D. 二维表可以表示关系**

- A错误：关系中不能有完全相同的元组（集合的性质）
- B错误：行的次序不重要
- C错误：列的次序不重要
- D正确：二维表（满足一定条件）可以表示关系

### 题6 — 可以描述参照关系的是
**答案：B. 外码**

外码建立了两个关系之间的参照（引用）关系，一个关系的外码引用另一个关系的主码。

### 题7 — E-R图中实体转换为
**答案：A. 关系**

E-R图向关系模式转换时，每个实体集转换为一个关系（即一张表）。

### 题8 — 多值属性如何处理
**答案：C. 为其创建一个新的关系模式**

多值属性不能直接放在原关系中（违反1NF），必须单独创建一个关系模式来存放。

### 题9 — 复合属性如何处理
**答案：B. 展平技术，忽略复合属性本身，直接使用其成分属性代替**

复合属性如"地址"可拆分为"省、市、街道"等成分属性，转换时只保留成分属性，忽略复合属性本身。

### 题10 — 派生属性如何处理
**答案：A. 忽略**

派生属性可以从其他属性计算得出（如年龄从出生日期计算），不需要在数据库中存储，直接忽略。

### 题11 — 一对多联系转换后使用什么作为码
**答案：B. 多端实体的码**

在一对多联系中，将一端实体的主码作为外码加入多端实体对应的关系中，该关系的码是多端实体的码。

### 题12 — 多对多联系转换后使用什么作为码
**答案：C. 实体集M和N的码的联合**

多对多联系需要单独创建一个关系，该关系的主码由两端实体主码的联合组成。

### 题13 — 关系代数五种基本运算
**答案：D. 并、差、投影、选择、笛卡儿积**

五种基本运算是：并(∪)、差(−)、投影(π)、选择(σ)、笛卡儿积(×)。交运算可由差运算推导（R∩S = R−(R−S)），不属于基本运算。

### 题14 — 属于一元运算的是
**答案：A. 选择运算**

选择运算只涉及一个关系（从该关系中筛选元组），是一元运算。并、交、除都是二元运算（需要两个关系）。

### 题15 — 必须有同名属性才能进行的是
**答案：D. 自然连接**

自然连接要求两个关系有同名属性，并自动根据所有同名属性的值相等来进行连接，结果中去除重复列。

### 题16 — 属于行运算的是
**答案：A. 选择运算**

选择(σ)从行的角度筛选元组（WHERE条件），投影(π)从列的角度选取属性，笛卡儿积和连接属于行列都涉及的运算。

### 题17 — 属于列运算但伴随行删除的是
**答案：B. 投影运算**

投影选取指定的列，但可能产生重复元组（多个不同元组投影后变得相同），这些重复行会被删除，所以说"伴随行删除"。

### 题18 — 属于R但不属于S的所有元组
**答案：C. R差S**

差运算R−S的结果包含所有在R中但不在S中的元组。

### 题19 — 笛卡儿积的逆运算
**答案：D. 除运算**

除运算(÷)是笛卡儿积的逆运算。如果T=R÷S，那么T×S的结果包含在R中。

### 题20 — 做哪个运算属性数目增加
**答案：D. 笛卡儿积运算**

R有n个属性，S有m个属性，R×S的结果有n+m个属性（两表属性直接拼接）。并、交、差运算要求两表属性数相同，结果属性数不变。
