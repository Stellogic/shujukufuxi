# 第7章 关系规范化理论 — 题目讲解

---

## 一、题目分析

本章22道单选题是数据库课程中**最难也是最重要的章节**，围绕函数依赖和范式展开。题目类型包括：求候选码、判断范式级别、区分主属性/非主属性、极小函数依赖集、模式分解的特性判断。

> **题目类型：** 计算型 + 判断型选择题
> **考点：**
> - 函数依赖与码（候选码、主属性、非主属性）
> - Armstrong公理系统与属性闭包计算
> - 极小函数依赖集
> - 各级范式的定义与判断（1NF→2NF→3NF→BCNF→4NF）
> - 模式的规范化分解（无损连接性、保持函数依赖性）

---

## 二、相关知识点梳理

```
📚 函数依赖基础
├── 📍 函数依赖 X→Y
│   └── 定义：对于R中任意两个元组，若它们在X上相等，则在Y上也相等
├── 📍 完全函数依赖 X─→Y （完全）
│   └── 定义：X→Y成立，且对X的任何真子集X'，X'→Y都不成立
├── 📍 部分函数依赖 X─→Y （部分）
│   └── 定义：X→Y成立，但存在X的真子集X'，使得X'→Y也成立
├── 📍 传递函数依赖
│   └── 定义：若X→Y，Y→Z，且Y↛X，Y⊈X，则称Z对X传递函数依赖
└── 📍 平凡函数依赖 vs 非平凡函数依赖
    ├── 若Y⊆X，则X→Y是平凡函数依赖
    └── 否则是非平凡函数依赖

📚 候选码的求法（属性闭包法）
├── 📍 步骤：
│   1. 将属性分为四类：
│      L类（只出现在左边）→ 必属于候选码
│      R类（只出现在右边）→ 不属于候选码
│      N类（左右都不出现）→ 必属于候选码
│      LR类（左右都出现）→ 可能属于候选码
│   2. 计算L∪N的闭包，若包含所有属性，则是唯一候选码
│   3. 否则，逐步添加LR类属性计算闭包，找到最小集合
├── 📍 主属性：候选码中的任一属性
└── 📍 非主属性：不在任何候选码中的属性

📚 各级范式的定义（递进关系 BCNF⊂3NF⊂2NF⊂1NF）
├── 📍 1NF — 属性都是不可分的数据项（最低要求）
│   └── 违反原因：关系中还有表（多值属性、复合属性嵌套）
├── 📍 2NF — 在1NF基础上，每个非主属性都完全函数依赖于候选码
│   ├── 违反原因：非主属性对码的【部分依赖】
│   └── 不彻底性：未消除非主属性对码的【传递依赖】
├── 📍 3NF — 在2NF基础上，每个非主属性都不传递函数依赖于候选码
│   ├── 违反原因：非主属性对码的【传递依赖】
│   └── 不彻底性：未消除主属性对码的【部分和传递依赖】
├── 📍 BCNF — 在3NF基础上，每个决定因素都包含码
│   ├── 违反原因：存在不包含码的决定因素
│   ├── 针对对象：主属性（消除主属性对码的部分和传递依赖）
│   └── 性质：所有非主属性对码完全函数依赖 + 主属性对不包含它的码也完全函数依赖 + 没有任何属性完全函数依赖非码属性组
└── 📍 4NF — 在BCNF基础上消除非平凡的多值依赖

📚 范式判断的口诀
├── 📍 判断非主属性是否有部分依赖 → 有则1NF，无则至少2NF
├── 📍 判断非主属性是否有传递依赖 → 有则2NF，无则至少3NF
├── 📍 判断所有决定因素是否都包含码 → 否则是3NF，是则BCNF

📚 重要结论（可以直接用）
├── 📍 两个属性的关系 → 最高BCNF（题1）
├── 📍 全码/所有属性都是主属性的关系 → 最高3NF，不一定是BCNF（题17）
├── 📍 极小函数依赖集：1个或多个（题2）
├── 📍 无损连接分解 → 一定可达BCNF（题19）
├── 📍 无损连接+保持函数依赖 → 一定可达3NF，不一定可达BCNF（题20）

📚 模式分解的特性
├── 📍 无损连接性：分解后自然连接能恢复原关系（R = ⋈ρ）
│   └── 判断方法：若R1∩R2→R1−R2 或 R1∩R2→R2−R1，则无损
├── 📍 保持函数依赖性：F在分解后的关系上投影的并集能逻辑蕴含F
│   └── 即原F中的每个FD都能在分解后的某个关系中被验证
└── 📍 两者独立性：无损不一定保持依赖，保持依赖不一定无损
```

**常见误区提醒：**
- **"两个属性一定达到BCNF"** 是常考的结论——因为二元关系中任何非平凡FD的左边必是超码
- **"全是主属性最高3NF"** 也是常考点——全主属性消除不了主属性对码的部分依赖问题（如全码场景）
- **极小函数依赖集不唯一**——可能有多个！不是0个也不是固定1个
- **范式判断需要先求出所有候选码**，否则容易求错

---

## 三、解题思路与一般方法

### 求候选码的通用方法（属性闭包法）

```
Step 1: 对属性分类
  L类（只出现在左边）：候选码必包含的
  R类（只出现在右边）：不可能在候选码中
  N类（左右都不出现）：候选码必包含的
  LR类（左右都出现）：待定

Step 2: 计算 L∪N 的闭包
  若 (L∪N)+ = U（包含所有属性），则 L∪N 是唯一候选码
  否则，依次加入 LR 类属性，计算闭包直到覆盖U
```

### 范式判断的通用方法

```
Step 1: 找出所有候选码（属性闭包法）
Step 2: 确定主属性和非主属性
Step 3: 检查非主属性对候选码是否有部分依赖 → 有=1NF
Step 4: 检查非主属性对候选码是否有传递依赖 → 有=2NF
Step 5: 检查每个FD的左边是否都包含码 → 不是=3NF, 是=BCNF
```

---

## 四、见到这种题目的思考框架

```
看到"候选码" → 分类L/R/N/LR属性 → 求闭包 → 最小覆盖U的集合
看到"主属性/非主属性" → 先求候选码 → 主属性∈候选码，非主属性∉所有候选码
看到"最高范式" → 逐级检查 1NF→2NF→3NF→BCNF
看到"两个属性" → 直接答BCNF
看到"全部是主属性" → 最高3NF
看到"极小函数依赖集个数" → 1个或多个（不唯一）
看到"2NF不彻底性" → 没消除非主属性的传递依赖
看到"3NF不彻底性" → 没消除主属性的部分和传递依赖
看到"哪个范式针对主属性" → BCNF
看到"无损连接可达什么" → BCNF
看到"无损+依赖保持可达什么" → 3NF
看到分解ρ={XY, XZ}且F中有X→Y, Y→Z → 计算判断：无损但不保持依赖
```

---

## 五、本题详细解答

### 题1 — 两个属性构成的模式最高范式
**答案：D. BCNF**

只有两个属性的关系模式必达到BCNF。原因：若X→Y成立，则X必为超码（因为只有两个属性，X的闭包包含Y也就包含全部属性）。没有违反BCNF的可能。

### 题2 — 极小函数依赖集的数量
**答案：D. 1个或多个**

一个函数依赖集F的极小函数依赖集（最小覆盖）**可能不唯一**，所以是1个或多个。这是因为化简过程的顺序选择可能导致不同的结果。

### 题3 — U={X,Y,Z}, F={X→Y}，码是
**答案：D. XZ**

L类：X（只出现在左边）→ 必在候选码中
N类：Z（左右都不出现）→ 必在候选码中
计算(XZ)+：X→Y说明XZ→Y，所以(XZ)+ = XYZ = U
候选码是XZ。

### 题4 — U={X,Y,Z}, F={X→Y, Y→Z}，码是
**答案：A. X**

L类：X → 必在候选码中
计算(X)+：X→Y→Y，然后Y→Z→Z，所以X+ = XYZ = U
候选码是X（唯一）。

### 题5 — U={X,Y,Z}, F={XY→Z}，码是
**答案：C. XY**

L类：X, Y（只出现在左边）
计算(XY)+：由XY→Z得(XY)+ = XYZ = U
候选码是XY。注意X和Y单独都不能决定全部。

### 题6 — U={X,Y,Z}, F={X→Z, Y→Z}，主属性是
**答案：C. X和Y**

先求候选码：L类为空，LR类为X和Y（左右都出现），R类为Z。
尝试X闭包：(X)+ = XZ ≠ U
尝试Y闭包：(Y)+ = YZ ≠ U
尝试XY闭包：(XY)+ = XYZ = U
所以候选码=XY。主属性=X和Y。非主属性=Z。

### 题7 — U={A,B,C,D,E}, F={A→BC, D→E, E→D}，非主属性是
**答案：A. B和C**

L类：A → 必在候选码中
N类：无
LR类：D, E
R类：B, C

计算A+：(A)+ = ABC ≠ U
尝试加入D：(AD)+ = ADEBC = U → AD是候选码
尝试加入E：(AE)+ = AEDBC = U → AE是候选码

候选码：AD和AE。主属性：A、D、E。非主属性：B和C。

### 题8 — 关于BCNF不正确的说法
**答案：D. 一定满足对4NF的要求**

- A正确：BCNF要求所有非主属性对每个码完全函数依赖
- B正确：BCNF也约束了主属性
- C正确：BCNF要求每个决定因素包含码，即不存在属性依赖于非码属性组
- D错误：BCNF不一定满足4NF。4NF还要消除多值依赖，BCNF可能存在多值依赖

### 题9 — 2NF的不彻底性表现在
**答案：B. 非主属性对码的传递依赖**

2NF已消除了非主属性对码的**部分依赖**，但未消除非主属性对码的**传递依赖**，这就是2NF的不彻底性。

### 题10 — 3NF的不彻底性表现在
**答案：C. 主属性对码的部分和传递依赖**

3NF消除了非主属性的部分依赖和传递依赖，但未约束**主属性**对码的依赖，主属性仍可能存在对码的部分依赖和传递依赖。

### 题11 — 哪个范式级别针对主属性提出
**答案：D. BCNF**

BCNF将约束扩展到主属性，要求每个决定因素都包含码（即也约束了主属性对码的依赖）。BCNF由Boyce和Codd提出，是对3NF的修正。

### 题12 — U={X,Y,Z}, F={X→Y, Y→Z}，最高范式
**答案：B. 2NF**

候选码：X（题4经典案例）
主属性：X。非主属性：Y, Z
检查部分依赖：X→Y（完全依赖✓），X→Z通过Y传递（非部分依赖）✓
检查传递依赖：X→Y, Y→Z, Y↛X → Z对X有传递依赖 ✗
有传递依赖 → 最高2NF。

### 题13 — U={X,Y,Z}, F={X→Y}，最高范式
**答案：A. 1NF**

候选码：XZ（题3经典案例）
主属性：X, Z。非主属性：Y
检查部分依赖：候选码是XZ，有XZ→Y，但X→Y也成立
说明非主属性Y对候选码XZ的部分依赖存在（因为X是XZ的真子集）
有部分依赖 → 最高1NF。

### 题14 — U={X,Y,Z}, F={XZ→Y, Y→Z}，最高范式
**答案：C. 3NF**

L类：X
LR类：Y, Z
计算X+：(X)+ = X ≠ U
(XZ)+ = XZY = U → XZ是候选码
(XY)+ = XYZ = U → XY是候选码

候选码：XZ, XY。主属性：X, Y, Z（全部是主属性！）
非主属性：无（全部是主属性）
因为无非主属性，自动满足2NF和3NF。

检查BCNF：FD中XZ→Y的左边XZ是候选码✓，Y→Z的左边Y不是候选码（也不是超码）✗
→ 违反BCNF条件 → 最高3NF。

### 题15 — U={X,Y,Z}, F={Y→X, Y→Z}，最高范式
**答案：D. BCNF**

L类：Y
计算Y+：Y→X, Y→Z → Y+ = XYZ = U
候选码：Y。主属性：Y。非主属性：X, Z

非主属性都完全函数依赖于Y（没有部分依赖）→ 满足2NF和3NF
检查BCNF：Y→X左边Y含码，Y→Z左边Y含码 → 所有FD左边都是超码 ✓
→ 满足BCNF。

### 题16 — R(ABCD), F={B→C, C→D, D→A}，最高范式
**答案：B. 2NF**

L类：B（仅B→C的左边是B）
R类：A（只在右边：D→A）
N类：无
LR类：C, D

B+ = BCDA = U → B是候选码
C+ = CDA ≠ U? C→D, D→A, C+ = CDA? Wait...
Actually let me recompute: B→C, C→D, D→A
B+ = {B, C, D, A} = U. So B is a candidate key.
C+ = {C, D, A} ≠ U
D+ = {D, A} ≠ U

Actually, wait. Let me check more carefully.
B+: B→C gives C, C→D gives D, D→A gives A → B+ = ABCD = U. B is a candidate key.
C+: C→D gives D, D→A gives A → C+ = CDA ≠ U.
D+: D→A gives A → D+ = DA ≠ U.

Is there another candidate key? Let me check if BC, BD, etc. are superkeys.
BC+: B alone is already a key, so BC is a superkey (but not minimal).
What about C, D combinations? (CD)+ = CDA ≠ U.

So single candidate key: B.
Prime attributes: B. Non-prime: A, C, D.

Check partial dependency: B→C (full), B→D (via C), B→A (via D) — all full on B ✓
Check transitive: B→C, C→D, C↛B → D transitively depends on B through C ✗
Has transitive dependency → max 2NF.

### 题17 — 全主属性的模式最高范式
**答案：C. 3NF**

所有属性都是主属性时，不存在非主属性，因此自动满足2NF（没有非主属性可以有部分依赖）和3NF（没有非主属性可以有传递依赖）。但BCNF可能被违反，因为可能存在主属性对码的部分依赖。所以最高一定达到3NF，不一定达到BCNF。

### 题18 — R(ABCD), F={A→B, C→D}，最高范式
**答案：A. 1NF**

L类：A, C
N类：无

(A)+ = AB ≠ U
(C)+ = CD ≠ U
(AC)+ = ABCD = U → AC是候选码

主属性：A, C。非主属性：B, D。
检查部分依赖：AC→B成立，但A→B也成立（A是AC的真子集）→ B对AC有部分依赖 ✗
→ 最高1NF。

### 题19 — 若要求分解具有无损连接性，一定可达
**答案：D. BCNF**

无损连接分解可以把任何关系模式分解到BCNF。但注意：不一定能同时保持函数依赖。

### 题20 — 若要求分解既有无损连接性又保持函数依赖，一定可达
**答案：C. 3NF**

同时满足无损连接和保持函数依赖的分解，最多一定可以达到3NF，不一定能达到BCNF。这是规范化理论的一个重要结论。

### 题21 — R(XYZ), F={X→Y, Y→Z}, ρ={R1(XY), R2(XZ)}
**答案：B. 具有无损连接性，但不保持函数依赖**

无损性判断：
- R1∩R2 = X
- R1−R2 = Y
- 检查X→Y是否成立？F中有X→Y ✓ → 无损。

保持依赖判断：
- Y→Z是否被分解后的关系保持？R1有XY（属性X和Y），R2有XZ（属性X和Z）。Y→Z的左右分别在两个关系中，无法在任何一个关系中被验证 → 不保持。
→ 无损但不保持依赖。

### 题22 — R(XYZ), F={X→Y, Y→Z}, ρ={R1(XY), R2(YZ)}
**答案：A. 既具有无损连接性，又保持函数依赖**

无损性判断：
- R1∩R2 = Y
- R2−R1 = Z
- 检查Y→Z是否成立？F中有Y→Z ✓ → 无损。

保持依赖判断：
- X→Y：在R1(XY)中可验证 ✓
- Y→Z：在R2(YZ)中可验证 ✓
→ 既无损又保持依赖。
