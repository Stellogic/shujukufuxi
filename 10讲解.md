# 第10章 事务与并发控制 — 题目讲解

---

## 一、题目分析

本章19道单选题覆盖**事务的ACID特性**和**并发控制机制**两大主题。题目从基础概念（事务四大特性）到具体技术（封锁协议、死锁/活锁处理、可串行化调度）梯度递进。

> **题目类型：** 概念辨析型选择题
> **考点：**
> - 事务的ACID特性（原子性、一致性、隔离性、持久性）
> - 封锁类型（排他锁X、共享锁S）及其权限
> - 三级封锁协议及各自能解决的问题
> - 并发操作导致的问题（丢失修改、读脏数据、不可重复读）
> - 死锁与活锁的预防/解决策略
> - 可串行化调度与冲突可串行化

---

## 二、相关知识点梳理

```
📚 事务的ACID特性
├── 📍 原子性(Atomicity) — 事务中的操作要么全做，要么全不做
│   └── 由恢复机制保证（日志+undo）
├── 📍 一致性(Consistency) — 事务使数据库从一个一致状态变到另一个一致状态
│   └── 是事务的目标，由其他三个特性共同保证
├── 📍 隔离性(Isolation) — 并发事务相互隔离，互不干扰
│   └── 由并发控制机制保证（封锁等）
└── 📍 持久性(Durability/Permanence) — 事务一旦提交，其修改永久有效
    └── 由恢复机制保证（REDO）

📚 封锁类型
├── 📍 排他锁(X锁/写锁) — 事务获得后，可以读也可以写
│   ├── X锁与任何锁互斥
│   └── 权限：可读+可写
├── 📍 共享锁(S锁/读锁) — 事务获得后只能读，不能写
│   ├── S锁与X锁互斥，但与S锁不互斥（多个事务可同时持有S锁）
│   └── 权限：只可读
└── 锁相容矩阵：
      | S锁 | X锁 |
    --+-----+-----+
    S |  √  |  ×  |
    --+-----+-----+
    X |  ×  |  ×  |

📚 三级封锁协议
├── 📍 一级封锁协议（要求最低）
│   ├── 规则：修改数据前加X锁，事务结束释放
│   ├── 能解决：丢失修改 ✓
│   ├── 不能解决：读脏数据 ✗、不可重复读 ✗
│   └── 记：仅保护写
├── 📍 二级封锁协议
│   ├── 规则：一级 + 读数据前加S锁，读完即释放
│   ├── 能解决：丢失修改 ✓、读脏数据 ✓
│   ├── 不能解决：不可重复读 ✗
│   └── 记：保护写+保护读(读完就放)
├── 📍 三级封锁协议（要求最高）
│   ├── 规则：一级 + 读数据前加S锁，事务结束释放
│   ├── 能解决：全部问题 ✓
│   └── 记：保护写+保护读(事务结束才放)

📚 并发操作导致的三种数据不一致问题
├── 📍 丢失修改(Lost Update) — 两个事务先后修改同一数据，后提交的覆盖了先提交的
├── 📍 读脏数据(Dirty Read) — 读到其他事务未提交的修改（后来可能回滚）
├── 📍 不可重复读(Non-repeatable Read) — 同一事务内两次读取同一数据结果不同
└── 注意：可重复读不是问题！是问题"不包括"的选项！

📚 死锁与活锁
├── 📍 死锁(Deadlock) — 两个事务互相等待对方持有的锁，永远无法继续
│   ├── 预防：一次封锁法（一次性申请全部锁）、顺序封锁法（按固定顺序申请）
│   └── 解决：检测和解除策略（检测等待图是否有环，有则回滚某个事务）
├── 📍 活锁(Livelock) — 事务一直在等待但永远得不到执行
│   └── 解决：先来先服务(FCFS)策略
└── 注意区分：死锁用一次封锁/顺序封锁/检测解除，活锁用先来先服务！

📚 调度与可串行化
├── 📍 串行调度 — 事务一个接一个执行
│   └── 串行调度一定是正确的调度
├── 📍 并发调度 — 多个事务的操作交叉执行
│   └── 并发调度可能导致数据不一致
├── 📍 可串行化调度 — 结果等于某个串行调度的并发调度
│   └── 定义：结果与某个串行调度相同→是正确调度
├── 📍 冲突可串行化 — 通过交换不冲突的操作可转为串行调度
│   ├── 冲突可串行化 ⊂ 可串行化调度
│   ├── 冲突可串行化是正确调度
│   └── 与某个串行调度冲突等价
└── 注意：冲突可串行化 → 可串行化调度 ✓（一个方向）

📚 事务控制语句
├── 📍 COMMIT — 提交事务
├── 📍 ROLLBACK — 回滚事务
└── ABORT/FAIL不是标准SQL事务控制语句
```

**常见误区提醒：**
- **"串行调度一定是正确的"** vs **"可串行化调度才是正确的并发调度"** ——串行调度天然正确，但效率低；可串行化调度既正确又允许并发。
- **一级封锁协议能解决所有问题？** 错！一级只能解决丢失修改。
- **冲突可串行化"不是"可串行化调度？** 错！冲突可串行化是可串行化调度的一种（充分条件）。
- **先来先服务解决死锁还是活锁？** 解决**活锁**，不是死锁！

---

## 三、解题思路与一般方法

**ACID特性判断题：**
```
看到"要么都做要么都不做" → 原子性
看到"一致状态到一致状态" → 一致性
看到"并发事务隔离" → 隔离性
看到"提交后永久改变" → 持久性
```

**封锁协议三级比较：**

| | 一级 | 二级 | 三级 |
|---|---|---|---|
| 丢失修改 | ✓ | ✓ | ✓ |
| 读脏数据 | ✗ | ✓ | ✓ |
| 不可重复读 | ✗ | ✗ | ✓ |

---

## 四、见到这种题目的思考框架

```
看到"原子性" → 全做或全不做
看到"一致性" → 正确状态转换
看到"隔离性" → 事务内部操作不受干扰
看到"持久性/永久性" → 提交后永久有效
看到"X锁" → 可读可写
看到"S锁" → 只能读
看到"commit" → 成功提交事务
看到"rollback" → 回滚事务
看到"丢失修改" → 一级封锁协议
看到"不读脏数据" → 二级封锁协议（但二级不能保证可重复读）
看到"可重复读" → 三级封锁协议
看到"可重复读不是问题" → 选它（题目问"不包括"时）
看到"死锁预防" → 一次封锁法、顺序封锁法
看到"活锁解决" → 先来先服务
看到"先来先服务不能解决什么" → 死锁
看到"串行调度" → 一定正确
看到"可串行化" → 结果等于某个串行调度
```

---

## 五、本题详细解答

### 题1 — 事务的原子性
**答案：A. 事务中包括的所有操作要么都做，要么都不做**

原子性(Atomicity)：事务是不可分割的操作单位，所有操作要么全部完成，要么在失败时全部撤销。

### 题2 — 事务的一致性
**答案：D. 事务必须是使数据库从一个一致性状态变到另一个一致性状态**

一致性(Consistency)：事务的执行必须保证数据库状态的正确性转换。例如转账：A账户减少100，B账户增加100，总金额不变。

### 题3 — 事务的隔离性
**答案：C. 一个事务内部的操作及使用的数据对并发的其他事务是隔离的**

隔离性(Isolation)：并发执行的事务之间互不干扰。隔离级别决定了一个事务能看到其他事务修改的程度。

### 题4 — 事务的永久性
**答案：B. 事务一旦提交，对数据库的改变是永久的**

持久性(Durability)：事务一旦成功提交，其对数据库的修改就永久保存，即使系统故障也不会丢失。

### 题5 — 获得X锁后，T对R可以
**答案：C. 可读也可写**

排他锁(Exclusive Lock，X锁)是写锁，持有X锁的事务既可以读也可以写该数据。

### 题6 — 获得S锁后，T对R可以
**答案：A. 只可读**

共享锁(Shared Lock，S锁)是读锁，只允许读操作。多个事务可以同时持有S锁。

### 题7 — 成功提交事务的短语
**答案：A. commit**

COMMIT语句提交事务，使事务中的所有修改永久生效。

### 题8 — 防止数据库不一致，对并发事务需要
**答案：C. 并发控制**

事务并发执行可能破坏数据一致性（丢失修改、读脏数据、不可重复读），需要通过并发控制机制来保证正确性。

### 题9 — 回滚事务的短语
**答案：D. rollback**

ROLLBACK语句回滚事务，撤销事务中已做的所有操作，恢复到事务开始前的状态。

### 题10 — 并发操作导致的问题不包括
**答案：D. 可重复读**

并发操作可能导致三大问题：丢失修改、读脏数据、不可重复读。"可重复读"是想要达到的目标，不是问题！

### 题11 — 关于死锁正确的说法
**答案：D. 出现并发操作时，才有可能出现死锁**

- A错误：死锁在数据库操作中存在！
- B错误：禁止两个用户同时操作太极端，不现实
- C错误：竞争相同资源正是死锁的诱因
- D正确：并发是死锁的必要条件

### 题12 — 关于串行调度正确的说法
**答案：A. 串行调度一定是正确的调度**

串行调度（一个事务一个事务地执行）不存在并发冲突，每个事务都从正确的状态开始，所以一定是正确的。

### 题13 — 一级封锁协议可以保证
**答案：A. 不丢失修改**

一级封锁协议要求修改数据前加X锁直到事务结束，能保证事务的修改不被其他事务覆盖（不丢失修改）。但不能保证不读脏数据和可重复读。

### 题14 — 二级封锁协议不能保证
**答案：B. 可重复读**

二级封锁协议能保证不丢失修改（继承一级）和不读脏数据（读前加S锁），但不能保证可重复读（因为读完即释放S锁，两次读之间可能被修改）。

### 题15 — 关于封锁协议不正确的是
**答案：D. 一级封锁协议可以解决所有并发执行可能导致的问题**

一级封锁协议只解决了丢失修改，不能解决读脏数据和不可重复读。"解决所有问题"的是三级封锁协议。

### 题16 — 关于并发调度正确的说法
**答案：A. 一个并发调度是正确的如果它和某个串行调度结果相同**

这就是可串行化调度的定义：如果并发调度的执行结果与某个串行调度的结果一致，则它是一个正确的并发调度。

### 题17 — 关于冲突可串行化说法不正确的是
**答案：B. 冲突可串行化是不可串行化调度**

冲突可串行化是更强的条件（能被非冲突交换转化为串行调度），它是可串行化调度的子集。说它"不是可串行化调度"是错误的。

### 题18 — 可以解决活锁的是
**答案：C. 先来先服务**

活锁的本质是"老有人插队"（某个事务永远轮不到执行），先来先服务(FCFS)策略保证等待时间最长的事务优先获得锁。

### 题19 — 不可以解决死锁问题的是
**答案：C. 先来先服务**

- 一次封锁法 → 预防死锁 ✓
- 顺序封锁法 → 预防死锁 ✓
- 检测和解除策略 → 解决死锁 ✓
- 先来先服务 → 解决**活锁**，不能解决死锁 ✗
